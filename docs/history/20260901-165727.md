# Session Log

## User request

The Esi.AI Studio model provider cannot be found in VS Code.

## Initial hypothesis

The provider VSIX is installed, but the already-open VS Code window has not reloaded the extension. A secondary possibility is that the provider does not expose enough visible activation/diagnostic feedback.

## Planned checks

- Verify VS Code version, installation path, and exact extension ID.
- Inspect Extension Host logs and the Language Model API availability.
- Make the smallest provider-side diagnostic/activation improvement if needed.
- Rebuild, reinstall, reload/verify, and document the result.
