# Sitzungsprotokoll

## Benutzeranfrage

Die allgemeinen Models-Seiten-Elemente `MODEL LIBRARY` und `RUNTIME STATUS` können entfernt werden. Der Downloadstatus im Hugging-Face-Tab muss erhalten bleiben.

## Aktionsplan

- [x] Den Header und Runtime-Statusblock aus der Models-Komponente entfernen.
- [x] Nur an diesen Blöcken hängende Status-/Navigationslogik bereinigen.
- [x] Den Hugging-Face-Downloadstatus unverändert erhalten und per Hot Reload prüfen.
- [x] Ergebnis dokumentieren.

## Ergebnis

- `MODEL LIBRARY` und der gesamte `RUNTIME STATUS`-Block wurden aus der Models-Seite entfernt.
- Die Hardware-Erkennung bleibt für den Hugging-Face-Hardwarefilter erhalten.
- Der Hugging-Face-Downloadstatus mit Fortschritt, Pause/Fortsetzen und Abschlussstatus bleibt unverändert im Downloadbereich.
- Razor-Diagnostik und Browserprüfung waren erfolgreich; der kontrollierte Debug-Restart war nötig, weil die laufende Client-Ausgabe die Razor-Änderung nicht per Hot Reload übernommen hatte.
