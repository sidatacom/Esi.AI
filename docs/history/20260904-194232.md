# Sitzungsprotokoll

## Benutzeranfrage
Die veraltete Repository-Struktur in `README.md` soll korrigiert werden. Die Verweise auf die Forks sollen ebenfalls aktualisiert werden.

## Aktionsplan
- [x] Tatsächliche Repository-Struktur und Fork-Verweise im Workspace prüfen.
- [x] Veralteten Abschnitt `Repository Structure` in `README.md` aktualisieren.
- [x] Verweise auf Forks im README auf die aktuelle Struktur und Dokumentation ausrichten.
- [x] Markdown-Links und Pfade validieren.

## Ausführung
- [x] Prüfung
- [x] Änderung
- [x] Validierung: Alle lokalen README-Pfade existieren; `git diff --check -- README.md` war erfolgreich.

## Zusammenfassung
`README.md` beschreibt jetzt die aktuelle `src/`-Struktur, die beiden VS Code-Erweiterungen und die unter `origins/` eingebundenen LiteLLM-, LocalAI-, LLamaSharp-, OpenVINO-CSharp-API- und dotLLM-Quellen inklusive Fork-/Upstream-Verweisen.
