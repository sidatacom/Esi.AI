# Continuation Session

## User Request

Test the vLLM and SGLang backends as well, using the existing backend-neutral OpenAI-compatible API implementation.

## Action Plan

- [completed] Inspect the vLLM/SGLang runtime prerequisites, configuration, and available model/device routes.
- [completed] Run a real vLLM model-load and chat smoke test, including usage and streaming behavior where available.
- [completed] Run a real SGLang model-load and chat smoke test, including usage and streaming behavior where available.
- [in-progress] Stop all supervised runtime processes, record results or precise blockers, and validate the final workspace state.

## Execution Detail

- Verify both isolated Python executables, package versions, and accelerator visibility before starting Studio.
- Use the existing `BackendReferenceModelTests` for backend-specific bridge/model loading and chat generation.
- Configure vLLM with the RTX 4070 route and FlashInfer workaround already required by this host.
- Configure SGLang with the approved Intel Arc Pro B70 XPU route; never select the Intel UHD 630.
- Record model-load, generated text, usage/statistics, and any runtime failure output separately.
- Stop the supervised Studio/debug process and watchdog after both attempts, then run focused workspace checks.

## Results

- vLLM passed with `/home/llm/.cache/esi-ai/models/Qwen2.5-0.5B-Instruct` on `cuda:0` (the NVIDIA RTX 4070 route), using `/home/llm/.venvs/esi-ai-vllm/bin/python`, vLLM `0.28.0`, and `VLLM_USE_FLASHINFER_SAMPLER=0`.
- The vLLM reference test loaded the model and generated a non-empty chat response successfully in 22 seconds.
- SGLang was attempted with `/home/llm/.cache/esi-ai/models/Qwen2.5-0.5B-Instruct` on `xpu:1` (the Intel Arc Pro B70 route), with `CUDA_VISIBLE_DEVICES` empty and the Intel XPU environment selected. `torch.xpu.is_available()` was true.
- SGLang failed before model loading because the installed `sgl_kernel 0.4.6.post1` exposes only a CUDA `sm100/common_ops.abi3.so`; importing it requires `libc10_cuda.so` and `libnvrtc.so.13`, while the selected environment uses `torch 2.13.0+xpu` and has no CUDA torch runtime. Adding the installed NVRTC directory to `LD_LIBRARY_PATH` changed the error to the missing `libc10_cuda.so`, confirming an incompatible CUDA kernel package rather than a missing path alone.
- No SGLang tokens/sec result is claimed. The Intel UHD 630 was not selected.
- Replaced the SGLang XPU requirements with explicit `torch==2.13.0+xpu`, `torchvision==0.28.0+xpu`, `torchaudio==2.11.0+xpu`, `sglang==0.5.18`, and `sglang-kernel-xpu==0.1.0`; retained the official XPU wheel indexes and removed the invalid `--no-deps` requirements-file option.
- Updated `PythonBackendProvisioner` so the SGLang XPU installation invokes pip with `--no-deps` and removes stale `sglang-kernel` and `flashinfer-python` distributions from automatic environments before validation.
- The corrected requirements dry-run resolved successfully, the provisioner test passed 7/7, and the XPU kernel imported from `sglang-kernel-xpu==0.1.0` after removing the old CUDA package.
- A second real SGLang test reached SGLang startup but failed because SGLang 0.5.18 calls `torch.xpu.mem_get_info()`; the installed Intel XPU runtime reports that this Arc Pro B70 device does not support the free-memory query. This is a separate SGLang/XPU runtime compatibility blocker, so no generation or throughput result is claimed.

## Final Status

- vLLM real model-load and chat validation passed.
- SGLang dependency provisioning is corrected, but real model-load validation remains blocked by SGLang 0.5.18's unsupported XPU free-memory query on the Arc Pro B70.
- No active debug session, Studio process, Python bridge, or listener on ports `7010`, `18000`, or `18001` remains.
- Focused provisioner tests passed 7/7, the corrected pip resolution succeeded, C# diagnostics reported no errors, and `git diff --check` passed.

## Prerequisites

- Use the supervised Studio watchdog/debug workflow for Studio.
- Do not use direct `dotnet run`, compiled binaries, or ad-hoc Studio servers.
- Do not revert unrelated tracked workspace changes.
