# Session Log

## User Request

The image test must work with all exposed backends. Continue the Esi.AI Studio
vision validation and repair work, preserving real image handling rather than
silently dropping image data or converting it to text.

## Action Plan

- [complete] Inspect the LLamaSharp, OpenVINO, Python bridge, and dotLLM image
  boundaries.
- [complete] Verify the current capability guards and source diagnostics.
- [complete] Run focused Core tests and the Studio controller test slice.
- [incomplete] Run the live image test against a loaded image-capable model.
- [incomplete] Add vLLM, SGLang, and dotLLM image inference without a proven
  multimodal runtime contract.

## Findings

- LLamaSharp now loads media through MTMD and supports the Vulkan, CUDA, SYCL,
  and CPU routes when the loaded GGUF has a compatible vision projector.
- OpenVINO has an RGB tensor path and remains image-capable when a VLM pipeline
  is loaded.
- The local vLLM/SGLang gRPC bridge is text-only. Its protobuf, C# mapper, and
  Python server currently have no media fields or image lifetime contract.
- dotLLM's in-process runtime and `TextGenerator` are text-only and expose no
  multimodal model or processor API.
- The repository vision instructions require Python and dotLLM image input to
  remain rejected until complete end-to-end implementations and semantic tests
  exist. The controller therefore correctly reports unsupported image input for
  those backends instead of claiming that the test works.

## Validation

- Core focused tests: 11 passed, 0 failed.
- Studio controller tests: 21 passed, 1 failed because the integration test
  could not connect to `127.0.0.1:7010`; no Studio process was running.
- Source diagnostics for the touched LLama session, DataService, and controller:
  no errors.
- With the controlled Studio debug session running, the live image test reached
  `/v1/models` but failed because no loaded image-capable model was reported.
  The local model directory contains no `mmproj` projector file, so no genuine
  LLamaSharp multimodal model was available for the test.
- The Studio debug session and watchdog were stopped cleanly; port 7010 is free.

## Summary

The real LLamaSharp and OpenVINO image paths are structurally validated, but the
requirement that one image test succeed through every exposed backend cannot be
completed honestly in this repository state. vLLM/SGLang need a complete
multimodal bridge and dotLLM needs a genuine multimodal runtime/API. The live
test still requires a controlled Studio debug session plus a loaded compatible
vision model and projector.