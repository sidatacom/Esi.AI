# Session log

User request:
Für Hugging-Face-Suchergebnisse soll ein Filter die erkannte Hardware und die VRAM-Größen der einzelnen Karten enthalten. Benötigte Profile: None, 12 GB, 32 GB und 44 GB als Summe. Wenn ein Profil aktiv ist, sollen nur Modelle angezeigt werden, die bei einer konfigurierbaren Kontextlänge (Standard 128k) in den VRAM passen.

## Action plan
- [x] Extend hardware discovery with per-device memory capacity.
- [x] Add transport-neutral HF fit-filter request fields and server-side metadata-based filtering.
- [x] Add detected hardware, VRAM profile, and configurable context controls to the Models page.
- [x] Add focused tests and validate build and browser behavior.

## Summary
- Added Vulkan device memory capacity to the transport contract and preserved discovered capacity when live LLama status is merged.
- Added conservative Hugging Face fit filtering using repository weight sizes, model config metadata, KV-cache estimation, a 10% reserve, and 90% usable VRAM.
- Added `None`, per-device, and aggregate hardware profiles plus an editable default 128k context length to the Models page.
- Replaced the free context number input with the same `InputSelect` context-size ComboBox used by the LLama backend (`32k` through `1m`).
- Added regression coverage for fitting models, context-limit exclusion, and missing metadata exclusion.
- Validation: Studio build succeeded; all 48 Studio tests passed; browser checks confirmed the hardware profiles, reset behavior, and a rendered `SELECT` with `128k` selected by default.
- Browser search validation: a real `Qwen` search rendered 20 Hugging Face results without an error; selecting `Gesamt · 44 GB` and searching again reduced the rendered list to 3 results. The debug session and watchdog were stopped cleanly afterward.
