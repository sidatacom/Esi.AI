# Session

User request: Remove obsolete EsiMCP debug tools from the workspace rules, skills, agents, and related active configuration because C# Dev Kit is now the debugging workflow.

## Action plan

- [complete] Locate EsiMCP debug/tool references in active customization and workspace configuration.
- [complete] Replace EsiMCP debug instructions with direct C# Dev Kit commands.
- [complete] Remove EsiMCP from test-agent tool lists and active MCP/workspace settings.
- [complete] Validate that no obsolete references remain in active configuration.

## Summary

Removed the legacy EsiMCP debug lifecycle from the active rules while preserving the new C# Dev Kit bridge tools `csharp_devkit_list_commands` and `csharp_devkit_execute_command` in the instruction, debug skill, and five test agents. Restored the EsiMCP server registration in `.vscode/mcp.json` because it hosts those new bridge tools. The legacy readiness setting remains removed from `Esi.AI.code-workspace`. The EsiMCP extension source and historical documentation were left unchanged because they are not active workspace rules.

Validation: no legacy debug-tool names remain under `.github`; the two C# Dev Kit bridge tools are present in all intended active customization files; configuration diagnostics are clean; the live EsiMCP C# Dev Kit catalog reports `ms-dotnettools.csdevkit` active with the expected launch and Hot Reload commands.
