# User request

Generalize the remaining LLama names in the persistence model before continuing the broader model-configuration work.

# Action plan

- [completed] Identify LLama-specific persistence entity and table names.
- [completed] Rename the persisted model entity and DbSet to backend-neutral names.
- [completed] Rename the configuration-profile table to `ModelConfigurationProfiles`.
- [completed] Add a data-preserving EF migration for existing databases.
- [completed] Build and run focused persistence tests.

# Summary

The persisted model entity is now `ModelEntity`, exposed through `ApplicationDbContext.Models` and stored in `Models`. Configuration profiles are stored in `ModelConfigurationProfiles`. Existing `LlamaModels` and `LlamaConfigurationProfiles` tables are renamed through migration `20260830142050_RenameLlamaPersistenceNames`; the generated SQL preserves existing rows. Studio build and backend model persistence test passed.