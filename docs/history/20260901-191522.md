# Session 20260901-191522

## User request
Qwen3 soll im VS-Code-Chat auswählbar sein. Der aktuelle Auftrag lautet: Problem lösen.

## Action plan
- [x] Lokalen Provider und installiertes Extension-Bundle vergleichen.
- [x] Ursache für die fehlende Modellanzeige beheben.
- [x] Extension und laufenden Host validieren.
- [x] Ergebnis zusammenfassen.

## Execution

- [x] Nach dem Hard Reset verifiziert: kein Studio-Prozess, kein Watchdog und kein Listener auf Port `7010`.
- [x] Vorherigen Boot auf OOM-, GPU- und Shutdown-Hinweise geprüft; kein eindeutiger OOM-/NVIDIA-Xid-Eintrag gefunden.
- [x] Verbindliche Watchdog-Startregel in `.github/copilot-instructions.md` ergänzt.
- [x] Dieselbe Startvoraussetzung in `.github/skills/vscode-debug/SKILL.md` ergänzt.
- [x] Studio nach dem Hard Reset nicht erneut gestartet.
- [x] OpenAI-Modellkatalog auf alle lokal erkannten Modelle erweitert.
- [x] Chat-Anfragen auf das ausgewählte geladene lokale Modell geroutet.
- [x] VS-Code-Provider-Bundle gebaut, als schlankes VSIX gepackt und installiert.
- [x] Fokussierte Controller-Tests erfolgreich ausgeführt: 4 von 4.

## Summary

Der vorherige Boot endete ohne normalen Shutdown; der Rechner musste per Hard Reset neu gestartet werden. Der OpenAI-Katalog liefert jetzt alle lokal erkannten Modelle mit lesbarem Namen an den VS-Code-Provider. Für Chat-Anfragen muss das jeweilige Modell in Esi.AI Studio geladen sein. Die Extension `sidatacom.vscode-esi-ai-studio@0.1.0` wurde neu installiert. Esi.AI Studio und der Watchdog laufen aktuell nicht; ein erneuter Studio-/Modellstart sollte erst nach separater Ursachenanalyse und ausschließlich über die überwachte VS-Code-Debug-Konfiguration oder die kombinierte Build-/Watchdog-Task erfolgen.
