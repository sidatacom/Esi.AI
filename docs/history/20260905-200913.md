# Session Request

User requested a root-cause fix for persistent OpenVINO `Unknown error (-17)` failures after the OpenVINO refactoring. The requested behavior is to retain `GenerateWithHistory` only where it is valid and restore direct text generation for plain VLM requests, without relying on a provider-only context truncation workaround.

## Action Plan

- [completed] Inspect and update `OpenVinoChatSession.GenerateWithStats` so pipeline selection is explicit.
  - Keep `LLMPipeline.GenerateWithHistory` unchanged.
  - Route text-only VLM requests without tools or images to direct `VLMPipeline.Generate`.
  - Keep VLM history generation for tools and images.
  - Avoid creating `ChatHistory` before path selection.
- [completed] Remove the provider context truncation workaround and synchronize the provider version.
  - Send the complete message list.
  - Increment the extension patch version.
  - Build, package, and install the VSIX.
- [completed] Validate compilation and tests.
  - Build `Esi.AI.Core`.
  - Run `Esi.AI.Core.Tests`.
  - Record unrelated provider test-suite dependency failures if present.
- [completed] Validate the running Studio API.
  - Start the configured server debug session.
  - Load the affected Qwen OpenVINO model on `GPU.1`.
  - Test text-only streaming, tools, and image requests.
  - Confirm SSE completion with `[DONE]`.
- [completed] Finalize the session record and report changed files and validation results.
- [completed] Repair OpenVINO replacement-load lifecycle state.
  - Serialize model replacement and unload with active generation.
  - Dispose the active pipeline before native replacement creation.
  - Clear the loaded-model projection when replacement creation fails.
  - Verify failed replacement, status reporting, recovery reload, and streaming generation.

## Execution Notes

The direct VLM route was added to `src/Esi.AI/Esi.AI.Core/ModelLoading/OpenVinoModelLoader.cs`. The provider truncation helper was removed from `src/vscode/vscode-esi-ai-studio/src/provider.ts`; package metadata was advanced to `0.1.27`. `GenerateWithHistory` remains selected for tool and image requests.

## Validation

Validation completed: `Esi.AI.Core` and the Studio project built successfully; the focused OpenVINO loader tests reported 20 passed, 0 failed; source diagnostics reported no errors; the provider bundle built and VSIX `0.1.27` was installed. A clean Studio process loaded Qwen OpenVINO on `GPU.1`; a 95-tool request streamed tool calls and ended with `[DONE]`, disproving a fixed 95-tool limit. An intentionally invalid replacement load returned native `-17`, after which `/v1/models` correctly reported `loaded:false` and chat returned `503 No model is currently loaded`; reloading the valid model then succeeded and streaming ended with `[DONE]`. The provider Vitest command remains blocked by the unrelated missing `sql.js` dependency.
