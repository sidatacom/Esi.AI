# Session 20260913-202823

## User request
Continue the Esi.AI Qwen3.8 vLLM XPU BF16-MTP runtime validation after the previous startup attempt.

## Initial state
- No `esi-qwen38-xpu` container is currently running.
- Previous run loaded target and MTP weights and reached torch compilation, but did not reach `/health` before cleanup.
- Continue with the corrected oneCCL socket/OFI/TCP environment and do not reintroduce `ZE_AFFINITY_MASK=0`.

## Results
- A fresh privileged XPU container reached readiness with the corrected environment. Target and MTP weights loaded successfully, and `/health` returned successfully after vLLM compilation.
- Compilation completed without errors: main graph 89.67 s, MTP/eagle head graph 15.06 s, total engine initialization 117.29 s. GPU KV cache size was 141,882 tokens.
- A non-streaming smoke request returned HTTP 200 and produced output through the speculative path.
- Smoke metrics: 2 drafts, 8 draft tokens, 5 accepted tokens.
- Warmed streamed benchmark: 576 prompt tokens and 128 completion tokens per run, five runs. Decode rates were 48.80, 44.81, 49.00, 44.71, and 43.78 tokens/sec; median 44.81 tokens/sec. Median TTFT was approximately 0.414 s.
- Aggregate speculative metrics after warmup and benchmark: 1,176 draft tokens and 475 accepted tokens, or 40.4% draft-token acceptance. The result is below the earlier unpatched decode baseline of approximately 60.7 tokens/sec and does not reproduce the article's 84.56 tokens/sec.
- The container remains available for further diagnosis; no throughput claim beyond these measured values is made.
- OpenAI-compatible client smoke test passed: `POST /v1/chat/completions` returned HTTP 200. With `chat_template_kwargs.enable_thinking=false`, the model returned `Ja, der Chat läuft.` with `finish_reason=stop` and 7 completion tokens.
- After the client test, MTP counters were 305 drafts, 1,220 draft tokens, and 505 accepted tokens, confirming that the client request used speculative decoding.
- Definitive exact benchmark: prompt tokenizer count 512, streamed completion 128 tokens, one warmup plus five measured runs, thinking disabled. Decode rates were 55.02, 55.02, 54.97, 55.06, and 54.94 tokens/sec; median 55.02 tokens/sec. Median TTFT was 368.8 ms.
- MTP counter delta over the warmup plus five measured requests was 988 draft tokens and 515 accepted tokens, corresponding to 52.1% draft-token acceptance. The five measured requests accounted for 640 generated tokens; the extra 128-token delta is the warmup request.
- Article-scheduler comparison was also run with `max_num_seqs=64`, `max_num_batched_tokens=8192`, and `--enforce-eager` to avoid the XPU graph-capture exit at this larger capture range. Exact p512/g128 result: median TTFT 487.0 ms and median decode 25.28 tokens/sec. MTP counters for warmup plus five runs were 984 draft tokens and 528 accepted tokens, or 53.7% acceptance.
- The first attempted cache-disable option was invalid (`--disable-prefix-caching`, vLLM exited with argument error). Omitting `--enable-prefix-caching` did not disable it in this image; the resolved vLLM config still reported `enable_prefix_caching=True`. Therefore the 25.28 tokens/sec run is a valid `max_num_seqs=64` eager comparison, but not proof of the article's exact prefix-cache setting.
- Exact no-prefix configuration was launched with `--no-enable-prefix-caching`, `max_num_seqs=64`, `max_num_batched_tokens=8192`, `max_model_len=131072`, GPTQ, FP16, FP8 KV cache, MTP4, `gpu_memory_utilization=0.88`, `--enforce-eager`, and `VLLM_XPU_ENABLE_XPU_GRAPH=0`. vLLM resolved `enable_prefix_caching=False`, and the `/metrics` cache configuration confirmed the same value.
- Exact no-prefix benchmark used the 512-token prompt, one warmup, and five measured streamed chat requests with `max_tokens=128`, temperature 0, and thinking disabled. The measured runs had TTFT values of 492, 485, 492, 494, and 486 ms; decode rates were 24.05, 24.08, 24.15, 24.27, and 24.38 tokens/sec, median 24.15 tokens/sec. The warmup decode rate was 24.34 tokens/sec. The six requests generated 768 tokens in total according to vLLM metrics.
- Speculative runtime logs reported aggregate MTP acceptance rates between 52.3% and 55.0% across the measured intervals, with mean acceptance length around 3.09-3.20 tokens. The exact no-prefix result is therefore approximately 2.28x slower than the earlier `max_num_seqs=1` result of 55.02 tokens/sec and does not reproduce the article's 84.56 tokens/sec.

## Status
- [completed] Start the corrected BF16-MTP XPU runtime.
- [completed] Wait for graph compilation and verify `/health`.
- [completed] Run a functional smoke request.
- [completed] Run a warmed streaming benchmark and inspect speculative metrics.
- [completed] Launch and validate the exact no-prefix article configuration.
- [completed] Run the exact 512/128 benchmark and record throughput and MTP metrics.
- [completed] Confirm the resolved prefix-cache setting through vLLM metrics.
- [completed] Persist all vLLM form changes through the existing profile database JSON string.

## Persistence change
- The vLLM settings page now calls `SaveSettingsAsync` for model path, context length, tensor parallelism, GPU memory utilization, quantization, dtype, KV cache dtype, MTP tokens, sequence/batch limits, prefix caching, XPU graph mode, BF16 MTP draft mode, and trust-remote-code.
- `SaveSettingsAsync` already serializes the vLLM request into `ModelConfiguration.ConfigurationJson`, which is stored as a database string and restored when the profile is loaded. No schema change was required.

## Agent validation
- The Test vLLM agent sent a real chat request through the direct vLLM OpenAI-compatible endpoint on port 8000 using model `qwen38`. It returned HTTP 200, `finish_reason=stop`, 60 prompt tokens, 37 completion tokens, and 1.386 seconds latency.
- The VS Code Esi.AI Studio provider path could not be completed in this session because port 7010 was not serving Studio. C# Dev Kit launch also failed before startup with `Cannot read properties of undefined (reading 'scheme')`, so the successful request is direct vLLM validation rather than provider validation.
