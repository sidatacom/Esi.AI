# Session Log

## User Request

Create an implementation plan for the remaining Esi.AI Studio five-layer
refactor. The Blazor Interactive Auto reference from Esi.AI.Studio to
Esi.AI.Studio.Client must remain.

## Action Plan

- [x] Stabilize Contracts and DTO ownership without removing the Blazor host
  reference.
- [x] Extract the first Layer 1 catalog boundary: filesystem scanner and
  explicit DI registration.
- [x] Introduce typed Layer 2 runtime adapters and registry.
- [x] Introduce Layer 3 serving and scheduler abstractions.
- [ ] Separate Layer 4 lifecycle, diagnostics, provisioning, and publication.
- [ ] Centralize Layer 5 UI state and SignalR reconciliation.
- [ ] Add architecture tests and documentation.
- [ ] Run staged and final validation under the Studio watchdog rules.

## Architectural Constraint

The server-to-client project reference is intentional for the Blazor Web App
host and Interactive Auto. The target dependency boundary is therefore:
server host -> client UI assembly for rendering, while application contracts,
DTOs, and server services must not depend on client-only service definitions.

## Execution Notes

- `LocalModelScanner.ScanAsync` now observes cancellation immediately, including
  empty or missing directory inputs.
- `BackendRuntimeAdapters.cs` defines typed lifecycle/load adapters for
  LLamaSharp, OpenVINO, the Python bridge, and dotLLM, plus backend alias
  normalization and SGLang-to-Python fallback.
- `ModelRuntime` delegates image capability resolution and typed model loading
  through the adapter registry while retaining its existing public compatibility
  methods.
- Added focused registry tests; all eight selected Core tests pass.
- Explicitly registered `ILocalModelScanner` in Studio DI; the Studio project
  build passes.
- `InferenceService` now owns backend-specific chat generation and multimodal
  preparation; `InferenceScheduler` serializes concurrent generations and is
  registered through Studio DI.
- `DataService` retains chat persistence and stream publication while delegating
  generation to `IInferenceService`; the scheduler contract has a focused test.

## Remaining Work

- Extract the remaining Hugging Face catalog and download queue boundaries.
- Add lifecycle/state, diagnostics/publication, and client state layers.
- Add broader architecture tests and run final solution validation.

## Validation Summary

- Focused local scanner tests: 2 passed.
- Focused runtime registry and ModelRuntime tests: 9 passed.
- Inference scheduler test: 1 passed.
- Full Core test project: 61 passed, 8 skipped.
- Scanner and scheduler Studio tests: 3 passed.
- Studio project build: passed.
- Existing Studio regression filter: 56 passed; one OpenAI picture test requires
  the supervised Studio host on port 7010 and failed with connection refused
  because no host was running.
- `git diff --check` also reports pre-existing trailing whitespace in unrelated
  working-tree changes; those files were left untouched.

Review this summary when the refactor work is complete. Remove this session log
from `docs/history/` before committing if it is only a temporary work record.