# Session inspection log

## Request

Read-only inspect the current codebase and report the smallest complete implementation needed for image requests across every backend named in `OpenAiCompatibleController`: OpenVINO, Vulkan, CUDA, SYCL, CPU, vLLM, SGLang, and dotLLM. Focus on concrete blockers in `ModelRuntime`, `DataService`, the Python gRPC proto/server, and dotLLM library capability. Do not edit application code. State whether each backend can support images with existing dependencies or requires a larger integration.

## Plan

- [x] Inspect `OpenAiCompatibleController` request parsing and backend routing.
- [x] Inspect `ModelRuntime` image capability and inference paths.
- [x] Inspect `DataService` and model/chat contracts for image propagation.
- [x] Inspect Python gRPC proto, C# mapper, and Python vLLM/SGLang server.
- [x] Inspect dotLLM source and dependencies for multimodal capability.
- [x] Read the focused test failure and identify validation gaps.
- [x] Produce a backend-by-backend implementation assessment without source edits.

## Findings

- OpenVINO is wired end to end through VLM pipeline detection, ImageSharp RGB tensor construction, and `VLMPipeline.GenerateWithHistory`.
- Vulkan, CUDA, SYCL, and CPU Llama routes already use LLamaSharp MTMD and optional `mmproj` loading; image support is model/projector dependent.
- vLLM and SGLang runtime packages can support vision models, but the local gRPC schema, C# mapper, and Python server currently transmit text only.
- dotLLM's current message contract, tokenizer path, GGUF model loader, and transformer are text-only and CPU-only in this integration; vision requires a library-level multimodal implementation.
- `DataService` passes images for OpenVINO/Llama chat, rejects them for Python/dotLLM, and requires non-empty text for its browser chat flow.
- The focused test run built successfully and had 21 passing tests; the single failure was the live integration test receiving connection refused on `127.0.0.1:7010`.

## Summary

No application source was edited. The smallest complete implementation is: retain the existing OpenVINO and Llama MTMD paths; extend the Python proto/mapper/server with ordered text/image parts and backend-specific VLM request conversion; and treat dotLLM as a larger library integration (vision tower/projector, preprocessing, multimodal GGUF metadata/forward path, and capability reporting).