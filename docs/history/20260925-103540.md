# Session Request
Create a standalone `Esi.AI.Backend.Vllm.Cuda12` NuGet backend module in `/home/llm/Git/Esi.AI`, implementing `IBackendRuntime` with descriptor `vllm.cuda12`, family `Vllm`, route `CUDA12`. Keep Core out of dependencies. Own/copy only required Python runtime scripts/requirements and gRPC protocol/client code from `Esi.AI.Core` into the package, with local generation contracts sourced from Models; preserve process lifecycle, load, readiness, status and normalized streaming generation, tools as supported. Add tests without starting Python/CUDA hardware: descriptor, DI, invalid variant/config/path and any deterministic mapper behavior. Do not edit existing Studio/Core/solution/docs or other backend packages. Build, test, pack if feasible. Report limitations.

# Action Plan

## Locate the closest backend and source implementation
- [x] Read applicable workspace instructions before editing.
- [ ] Inspect existing VLLM backend module/project and neighboring tests for package conventions.
- [ ] Identify Core-owned Python scripts, requirements, protocol/client, runtime lifecycle, and Models contracts needed for VLLM CUDA12.
- [ ] State/check local hypothesis: a sibling backend module can implement the runtime without Core references; disconfirm by project/test build or dependency graph.

## Implement standalone backend package
- [ ] Add VLLM CUDA12 project and package-owned required runtime assets/client/protocol, relying only on Models and external dependencies.
- [ ] Implement descriptor, DI registration, validation, process lifecycle/readiness/status, and normalized streaming generation with tool support where source supports it.
- [ ] Add focused hardware-free tests for descriptor, DI, validation, and deterministic mapping.

## Validate and summarize
- [ ] Run focused tests, build, and pack the new project as feasible.
- [ ] Inspect changes for prohibited edits and Core dependency leakage.
- [ ] Record outcomes and limitations in this session log.
