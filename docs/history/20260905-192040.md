# Session Log

## User Request
Fix the VS Code Esi.AI Studio provider error: `The selected Esi.AI Studio model does not support this input content type.` The error occurs in `EsiAiStudioProvider.toOpenAiContent` while sending chat input to the selected model.

## Action Plan
- [in progress] Trace provider content conversion and current server model capabilities.
- [ ] Apply a focused provider fix for stale image capability registration and clearer unsupported MIME diagnostics.
- [ ] Increment, build, package, and install the provider version.
- [ ] Reload the Extension Host and validate model registration plus image-capable chat routing.
- [ ] Record results and remaining limitations.

## Constraints
- Preserve the single Studio OpenAI-compatible API and existing backend mappings.
- Provider behavior changes require a patch-version increment, synchronized lockfile/bundle/VSIX, installation, and Extension Host reload.
- Do not revert unrelated worktree changes.
