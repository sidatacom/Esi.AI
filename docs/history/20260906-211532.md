# Session log

Request: Start a debug session, test virtual `readyness`, restart it, then stop it.

Results:
- No active session before start.
- Started `Esi.AI.Studio` with `csdevkit.debug.projectDebugLaunch`.
- Active session: `b536745e-4657-49bd-8634-83e2c2585f29`.
- `csdevkit.debug.check.host.readyness` returned `{ "ready": true }`.
- `csdevkit.debug.restart` timed out after 30 seconds.
- The original session remained active after the restart timeout.
- `csdevkit.debug.stop` returned `{ "stopped": true }`.
- Final active session: `null`.
- Port 7010 is free.

Summary: Readiness and stop work with the configured default `Now ready on:`. Restart still times out after 30 seconds.
