# Session Log

User request context:
- Requirements buttons in the `/backends` GPU compatibility matrix must appear only when requirements are not fully satisfied and an action is necessary.
- This behavior must apply to all supported backends, not only LLama.

Action plan:
- [complete] Inspect existing backend diagnostics and matrix action flow.
- [complete] Implement readiness-aware visibility for all backend/vendor combinations.
- [complete] Build and validate the Studio UI in the browser.

Implementation:
- Matrix buttons are driven by actual prerequisite diagnostics, not only static support labels.
- vLLM and SGLang CUDA/XPU routes are checked independently with their isolated device route.
- OpenVINO uses its accelerator diagnostics; bundled LLama and dotLLM runtimes do not show unnecessary actions.
- The dialog receives and displays the exact backend/vendor pair from the clicked cell.
- The dialog lists the concrete checks for that pair and refreshes them after Solve/Prepare.

Validation:
- Blazor client build passed.
- Full Studio build passed.
- Browser smoke test passed at `http://127.0.0.1:7010/backends`.
- Ready vLLM/CUDA state showed no matrix action button.
- Non-ready SGLang/CUDA state showed the single matching matrix button.
- The SGLang/CUDA dialog displayed only SGLang checks, including the missing Python environment, and exposed Solve requirements.

