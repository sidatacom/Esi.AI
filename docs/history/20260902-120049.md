# Session Log

## User Request
The VS Code model list still shows only Tools and no Vision despite all four model flags being enabled.

## Initial Hypothesis
The running VS Code extension is using an installed or cached bundle that does not contain the `imageInput` mapping, even though the repository source and Studio API contract were corrected.

## Action Plan
- [ ] Identify the actual installed extension bundle used by VS Code.
- [ ] Compare its capability mapping with the repository bundle and live Studio API.
- [ ] Install/reload the corrected provider through the supported project workflow without removing existing features.
- [ ] Validate the displayed model capabilities and run focused checks.

## Status
The installed extension was confirmed to be an older same-version bundle. The corrected provider was installed through the documented workflow. A repository rule was added to `.github/copilot-instructions.md`: every provider behavior change must increment the patch version, synchronize lockfile/bundle/VSIX, install the new version, reload the Extension Host, and validate the full capability path.

Validation: the rule is present and `git diff --check` is clean.
