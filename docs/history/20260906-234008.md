# Session

## User request
The queued model-load fix still does not work.

## Action plan
- [x] Reproduce the current failure and inspect the active load path.
- [x] Identify the actual synchronization failure.
- [x] Implement the smallest root-cause fix.
- [x] Validate with build and a live request if the host is available.
- [x] Record the result and remaining risks.

## Findings
- The browser trace shows the request waits from `AutoLaunch gestartet` until `Konfiguration geladen`; queueing is working.
- The SSE stream receives the initial chunk, then the Studio host disappears after routing to OpenVINO generation.
- The posted EF Core SQL is normal configuration/catalog reads and is not the failure.
- No Linux OOM kill was found. The kernel reported an Intel i915 workqueue warning near the failure.
- The existing fatal inference coordinator shut down the host without logging the original exception.

## Change
- `InferenceFailureCoordinator` now logs the original exception with `LogCritical` before unloading runtimes and stopping the host.

## Validation
- Studio project build passed after the change.
- `get_errors` reported no errors for the changed file.
- `git diff --check` passed.
- A live retry was not possible because the C# debug session is no longer active and port `7010` is down.
- The later debug output identified the native failure as Intel OpenCL `clFinish`, error `-5 CL_OUT_OF_RESOURCES` on the GPU plugin. The process then exits normally because the fatal inference coordinator stops the host.
- `OpenVinoModelLoader` already owns a process-wide `generationLock`, so the remaining failure is not concurrent OpenVINO generation caused by missing serialization.

## Summary

The request queue is functioning; the remaining failure is during native OpenVINO generation after the model is loaded. The next run will expose the original exception in the Studio debug output instead of only reporting an SSE termination. Review this summary and remove this session log from `docs/history/` after confirmation.
