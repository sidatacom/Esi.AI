# Session 20260904-135205

## User request
Extend the existing WebAPI with an `application` area and methods to load and unload models so model lifecycle operations do not require the UI.

## Context
The repository has one OpenAI-compatible controller, an existing `DataService`/`ModelRuntime` lifecycle path, and SignalR methods used by the UI. Preserve the single-controller architecture and avoid duplicating model-loading business logic.

## Status
- Initialization complete.

## Action plan
1. Add a transport-neutral unload request contract in `Esi.AI.Models`.
2. Add `v1/application/models` status and backend-specific load routes to the existing OpenAI-compatible controller.
3. Add a common unload route, delegate all operations to `DataService`, and restrict mutating application routes to loopback callers.
4. Add focused controller tests for status and remote access rejection.
5. Build and run the focused Studio tests, then stop the supervised Studio/watchdog processes.

## Tracking
- [x] Add application API contract.
- [x] Implement application model routes.
- [x] Add lifecycle API tests.
- [x] Validate with focused tests and build.
- [ ] Stop Studio and watchdog cleanly.

## Implementation notes
- Added `ApplicationModelUnloadRequest` to the shared models contract.
- Added `GET /v1/application/models` and backend-specific load routes for Llama, OpenVINO, Python (vLLM/SGLang), and dotLLM.
- Added `POST /v1/application/models/unload` with loopback-only access and consistent validation/error handling.
- The full controller test file had one unrelated live-test infrastructure failure because Studio was stopped; the three new lifecycle tests passed.

## Verification
- Studio build passed after the API and enum-contract changes.
- Focused lifecycle tests passed: 3/3.
- Live `GET /v1/application/models` returned the current model status.
- Live Llama unload returned HTTP 200 with a string backend value.
- Live Llama and OpenVINO load routes reached their existing service validation for invalid paths.

## Summary
The existing OpenAI-compatible controller now exposes a loopback-only `application` model lifecycle API without adding a controller, SignalR method, or duplicate loader logic. The UI remains unaffected and the chat image picker remains outside this request.

## Cleanup
- [x] Stop the supervised Studio debug session and watchdog.

## Follow-up plan: WebAPI before chat images
The chat image picker and upload workflow remain blocked until the WebAPI is proven end to end.

1. Diagnose and fix the native OpenVINO VLM error `UNKNOWN_EXCEPTION (-17)`.
2. Pass the real PNG semantic WebAPI test through OpenVINO.
3. Validate the same structured image request with a live LLamaSharp MTMD model.
4. Verify contracts, error handling, and that vLLM/SGLang behavior remains unchanged.
5. Document the application model lifecycle endpoints with working examples.
6. Stop Studio and watchdog cleanly after validation. Completed.
7. Only then design and implement chat image selection, content-array submission, UI tests, and the complete end-to-end workflow.

## Documentation request
Create reusable `.instructions.md` files for the Esi.AI Studio WebAPI and for image handling per backend. The backend documents must distinguish active image paths from text-only or not-yet-supported paths, and must preserve the WebAPI-first gate for future chat UI work.

## Documentation files
- `esi-ai-studio-webapi.instructions.md`
- `esi-ai-studio-openvino-vision.instructions.md`
- `esi-ai-studio-llamasharp-vision.instructions.md`
- `esi-ai-studio-vllm-vision.instructions.md`
- `esi-ai-studio-sglang-vision.instructions.md`
- `esi-ai-studio-dotllm-vision.instructions.md`

## Documentation completed
- [x] Document the single-controller WebAPI boundary, application model lifecycle routes, shared DTO location, security, error handling, and test requirements.
- [x] Document OpenVINO VLM structured history, RGB NHWC U8 tensor ownership, compatibility checks, and the unresolved native `UNKNOWN_EXCEPTION (-17)`.
- [x] Document LLamaSharp MTMD/mmproj loading, `SupportsVision`, media-marker ordering, cleanup, and session lifetime.
- [x] Document vLLM and SGLang as unchanged text-only Python/gRPC integrations.
- [x] Document dotLLM as the current text-only in-process integration.

## Documentation verification
- Validated all six instruction files have quoted descriptions, `applyTo` frontmatter, and Markdown sections.
- `get_errors` reported no errors in the touched WebAPI contract/controller files.
- `git diff --check` passed after the documentation changes.

## Final summary
Reusable repository instructions now capture the WebAPI contract and the current image-processing knowledge for each backend. OpenVINO image generation remains explicitly unvalidated because native generation still fails with `UNKNOWN_EXCEPTION (-17)`; chat image UI work remains deferred until the WebAPI gate passes.
