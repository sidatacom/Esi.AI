# Session

## Request
Continue reproducing the Qwen3.8-27B BF16 MTP Studio path and resolve the generation-time EngineCore crash.

## Findings
- The first Studio generation crashed because Triton Intel could not start `ocloc`.
- `intel-ocloc` was staged in `/home/llm/.local/opt/intel-ocloc` and verified.
- The XPU worker now adds `ESI_OCLOC_ROOT` or the user-local ocloc root to `PATH` and `LD_LIBRARY_PATH`.
- Studio was restarted through C# Dev Kit using the complete file URI object including `fsPath`.
- The exact persisted profile loaded and real `http://127.0.0.1:7010/v1/chat/completions` succeeded.
- Warm Studio throughput measured approximately 19.7-21.8 tok/s over repeated requests; the 80 tok/s target remains unmet.
- The provider trace showed `/v1/models` polling but no provider chat request during this validation.

## Changes
- Updated `src/Esi.AI/Esi.AI.Core/ModelLoading/PythonInferenceServer.cs` to configure the Intel ocloc runtime for XPU workers.

## Validation
- No diagnostics in the edited C# file.
- Focused `Esi.AI.Core.Tests` command completed without reported errors.
- Studio readiness and real chat generation succeeded after the fix.

## Follow-up
- Determine why BF16 MTP does not approach the article throughput and validate the VS Code provider/agent request path explicitly.
