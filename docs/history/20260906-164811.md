# Session log

User request:
- Use the visible VS Code Run and Debug configuration `C#: Esi.AI.Studio [Default Configuration]` and add that workflow to the `vscode-debug` skill.
- Root-cause requirement: do not use a fallback launch path; repair the C# Dev Kit launch context.

Action plan:
- [complete] Inspect the existing vscode-debug skill and confirm the visible configuration context.
- [complete] Add the Run and Debug configuration selection and root-cause recovery rules to the skill.
- [complete] Validate the documentation diff.

Implementation:
- Updated `.github/skills/vscode-debug/SKILL.md` with the exact configuration `C#: Esi.AI.Studio [Default Configuration]`.
- Added an explicit pre-launch selection step and a refresh workflow that reselects the configuration.
- Added a root-cause rule for missing URI/scheme errors: repair VS Code/C# Dev Kit launch context and retry the same command; do not substitute another launcher.

Validation:
- `git diff --check` passed for the skill and session log.

Verified EsiMCP test:
- Parameterless `csdevkit.debug.projectDebugLaunch` reproduced the missing `scheme` failure.
- A URI string argument was rejected as invalid context and did not start Studio.
- The following serialized file URI object was accepted and started the Studio debug session:
	- `commandId`: `csdevkit.debug.projectDebugLaunch`
	- `arguments[0].scheme`: `file`
	- `arguments[0].path` and `arguments[0].fsPath`: `/home/llm/Git/Esi.AI/src/Esi.AI/Esi.AI.Studio/Esi.AI.Studio.csproj`
	- `authority`, `query`, and `fragment`: empty strings
- The active debug session ID was `931dc629-260d-4242-a2e0-0f94711b5b09`.
- `csdevkit.debug.showHotReloadPanel` and `csdevkit.debug.hotReload` were invoked while the active session remained `931dc629-260d-4242-a2e0-0f94711b5b09`.
- The session remained active after the Hot Reload command. No source change was applied during this command, so the documentation records command availability and session continuity, not a code-patch Hot Reload assertion.

Stop test:
- The active session remained `931dc629-260d-4242-a2e0-0f94711b5b09`.
- `csharp_devkit_list_commands` and the installed `ms-dotnettools.csdevkit@3.20.199` manifest expose no `csdevkit.debug.stop` command.
- The available `mcp_esimcp_debug_stop` tool was invoked and returned `{"stopped":true}`.
- Follow-up verification returned no active debug session and no listener on port `7010`.
- The skill now records `mcp_esimcp_debug_stop` as the verified stop path while preserving that C# Dev Kit itself has no native stop command.

Restart documentation:
- Added a dedicated restart sequence to the vscode-debug skill: inspect session, stop with `mcp_esimcp_debug_stop`, verify session/port shutdown, launch again with the serialized Studio project URI, and verify the new session and host readiness.
