# Session Log

## User Request
Fix unreliable Hot Reload in Esi.AI Studio after `dotnet watch` exited with the Linux inotify limit error.

## Action Plan
- [x] Identify the failing watcher path and confirm the inotify limit is the root cause.
- [x] Configure Studio startup to use the polling file watcher.
- [x] Validate that `dotnet watch` builds and reaches the Studio listener without the inotify failure.
- [x] Confirm the VS Code launch configuration carries the polling setting.

## Summary
`dotnet watch` failed because Linux reached the per-user inotify instance limit. Added `DOTNET_USE_POLLING_FILE_WATCHER=1` to `.vscode/launch.json`. A focused startup check confirmed polling mode, successful build, and `http://localhost:7010` readiness.
