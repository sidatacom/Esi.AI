# Session Request

Read-only exploration of the live Esi.AI Studio browser is unavailable. Inspect workspace source for the Backends page/component and identify the exact button/action used to load the selected LLama model, plus any testable SignalR/API path. Do not edit application source files.

# Action Plan

- [complete] Locate the Backends page/component.
- [complete] Identify the LLama load button label and click handler.
- [complete] Trace the LLama handler to the client service method and request type.
- [complete] Trace the SignalR hub path and method.
- [complete] Trace the HTTP API route and controller method.
- [complete] Return only file paths, labels, method names, and endpoints.

# Summary

The Backends page uses the shared Load model button for the LLama tab. Its click handler dispatches to the LLama-specific LoadModelAsync method, which calls IDataService.LoadModelAsync with a LoadModelRequest. The SignalR path is /hubs/data invoking LoadModel. The HTTP path is POST /v1/application/models/load/llama.