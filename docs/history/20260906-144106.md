# Session Log

User request:
- Investigate the real cause of OpenVINO C# API failures with very large tool sets.
- Inspect the external OpenVINO-CSharp-API implementation, especially ChatHistory, SetTools, JsonContainer, P/Invoke, string marshalling, and error mapping.
- Preserve the exact 91-tool request for validation.
- Do not add workarounds such as tool truncation, tool-count limits, schema reduction, or token caps.

Action plan:
- [completed] Inspect the OpenVINO C# wrapper source and native interop declarations.
- [completed] Correlate wrapper behavior with the exact 91-tool runtime result.
- [completed] Identify the first proven failure boundary and root cause.
- [completed] Determine that no local root-cause fix is justified without changing request semantics or timeout policy.
- [completed] Validate the unchanged 91-tool request and record the result.
- [not-started] Record the final result and remaining external-runtime limitations.

Execution notes:
- The 91-tool asynchronous request is still running and must not be altered or replaced.
- Current source excerpts show SetTools passes an IntPtr directly and native generation returns generic exception status; exact source details are still being verified.

Verified result:
- The exact 91-tool replay completed successfully with 22,374 prompt tokens and approximately 163 seconds elapsed.
- The wrapper has no tool-count or JSON-size limit in ChatHistory.SetTools or JsonContainer.FromJsonString.
- Large tool payloads are rendered by the model chat template and cause very slow GPU prefill.
- The streaming callback catches all managed exceptions and converts them to StreamingStatus.Cancel. If the HTTP/client cancellation arrives during the long prefill or first-token wait, the native layer reports the cancellation through its generic unknown-exception status -17.
- No root-cause code change was made because the observed failure is cancellation/error reporting after the slow request, not a proven wrapper limit or marshaling defect.

Follow-up requested:
- Apply a common timeout policy to every local backend using the complete tool count and estimated prefill size, without changing the request payload.

Implemented follow-up:
- Added per-backend defaults in `InferenceTimeout` configuration and a persisted general Settings page backed by SQLite.
- Added optional per-configuration timeout overrides to `ModelConfiguration`; the active model profile overrides its backend default without changing tools or schemas.
- Added SignalR read/update operations for general settings and a Settings navigation page.
- Added EF migration `20260906125239_AddApplicationAndProfileInferenceTimeouts`.
- Added focused tests for backend-specific defaults and configuration overrides; both pass.
- Full `Esi.AI.Studio.Tests` run passed: 75 passed, 1 skipped, 0 failed.
- Final `Esi.AI.Studio.csproj` build passed.
- Made timeout configuration discoverable from the backend page: global settings link, profile override label, and a direct OpenVINO action for the selected configuration.
- Fixed server-side InteractiveAuto prerendering by registering `IApplicationSettingsEvents` and validating the live `/settings` route in the Studio debug session.
