# Session Request

Implement a standalone `Esi.AI.Backend.Llama.Cuda12` package following the existing Llama Vulkan backend pattern. Add its project, runtime, module, focused tests, and CUDA 12.4 Linux-x64 native asset packaging. Keep dependencies limited to Backend.Abstractions, Models, and LLamaSharp; preserve process-wide native initialization; use `llama.cuda12`, `ConfigurationBackend.Llama`, the established CUDA route, GGUF loading, and normalized chat generation with streaming callbacks. Do not register it in Studio or modify existing Vulkan code/tests, Studio/Core projects, docs other than this session log, or solution files. Build/test the new project(s) if feasible and report changed paths and exact validation.

# Action Plan

- [complete] Create standalone CUDA12 runtime package project with only the three requested project dependencies and Linux-x64 CUDA native assets.
- [complete] Implement module descriptor and DI registration for `llama.cuda12`, Llama configuration, and the established `CUDA` route.
- [complete] Implement CUDA12 runtime lifecycle, GGUF validation/loading, process-wide native configuration, device/status reporting, and normalized streamed generation.
- [complete] Add focused MSTest coverage for descriptor, DI, wrong variant ID, empty model path, and runtime-directory validation.
- [complete] Build, test, and pack the new projects; inspect package contents and dependency manifest.

# Summary

- Added `Esi.AI.Backend.Llama.Cuda12` with a CUDA 12.4 runtime/module and five packaged Linux-x64 `.so` assets. The NuGet manifest depends only on LLamaSharp, Esi.AI.Backend.Abstractions, and Esi.AI.Models.
- Added five focused MSTests; all passed.
- Validation: `dotnet test src/Esi.AI/Esi.AI.Backend.Llama.Cuda12.Tests/Esi.AI.Backend.Llama.Cuda12.Tests.csproj --no-restore` passed (5/5); `dotnet pack src/Esi.AI/Esi.AI.Backend.Llama.Cuda12/Esi.AI.Backend.Llama.Cuda12.csproj --no-restore -o /tmp/esi-ai-cuda12-pack` succeeded; archive inspection confirmed only the five expected `.so` assets at `runtimes/linux-x64/native/cuda12` and no Windows DLLs; `get_errors` reported no errors in either new project.
- No solution, existing Vulkan, Studio, Core, or project documentation files were modified. This process log is retained for review and should be removed from `docs/history/` after review so it is not committed.
