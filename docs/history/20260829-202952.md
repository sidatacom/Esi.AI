# Session log

## User request
Unify the prerequisite/preparation logic so it behaves the same way for all backends.

## Action plan
- [x] Inspect the common backend loading boundary and existing prerequisite paths.
- [x] Define one backend preparation abstraction and migrate backend loading to it.
- [x] Add focused tests for all backend definitions and behavior.
- [x] Update documentation and validate the solution.

## Execution

- Added `BackendPrerequisiteProvisioner` as the common preparation contract.
- Routed Llama, OpenVINO, vLLM, SGLang, and dotLLM model loads through the same prerequisite step.
- Kept Python environment creation delegated to the existing isolated Venv implementation.
- Added native-backend bundled-runtime results and all-backend contract tests.
- Updated Python backend documentation and validated the Studio debug configuration.
- Validation: 36 Core tests with 29 passed and 7 skipped, 9 Studio tests passed, and Studio build succeeded.

## Summary

Every backend now passes through one prerequisite boundary before loading. Native backends report their bundled runtime; vLLM and SGLang prepare their isolated Python environments through the same service.
