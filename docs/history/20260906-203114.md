# Debug command test

User request:
> Kannst du die neuen virtuellen Commands `readyness`, `stop` und `restart` jetzt hier testen? Starte eine Debug-Session, restarte sie dann und danach stoppst du sie.

Action plan:

- [in progress] Verify the registered C# Dev Kit lifecycle commands and current debug state.
- [ ] Confirm port `7010` is free before launch.
- [ ] Start `Esi.AI.Studio` through `csdevkit.debug.projectDebugLaunch`.
- [ ] Verify the active session and host readiness with `csdevkit.debug.active.session` and `csdevkit.debug.check.host.readyness`.
- [ ] Restart the active session with `csdevkit.debug.restart` and verify readiness again.
- [ ] Stop the restarted session with `csdevkit.debug.stop` and verify the session and port are released.
