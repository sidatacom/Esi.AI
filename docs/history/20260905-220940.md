# Session 2026-09-05 22:09:40

## User request
Continue reproducing and fixing the OpenVINO `Unknown error (-17)` with the exact VS Code provider request. Reproduce the failing request in tests and keep testing until the root cause is fixed. Preserve all images, structured tool calls, and the full tool set; do not add arbitrary tool limits or provider-only truncation.

## Action plan
- [in progress] Reproduce the exact 89-tool provider request and capture the deterministic failure mode.
- [ ] Inspect the OpenVINO prompt/template/context boundary responsible for the hang or error.
- [ ] Add a deterministic regression test for the exact request shape, gated from normal GPU-less CI if needed.
- [ ] Implement the smallest root-cause fix without dropping tools or images.
- [ ] Re-run the exact replay, baseline requests, focused tests, and the applicable build.
- [ ] Record final results and remaining risks.

## Findings so far
- Text-only and one-tool OpenVINO requests complete with `[DONE]` in a clean process.
- The exact recorded payload has 89 tools, 3 user messages, and 139,973 JSON bytes.
- The exact replay emits the assistant header and keep-alives, then eventually fails or times out; a later text-only request reports native `Unknown error (-17)`.
- The model declares `text_config.max_position_embeddings = 262144`.

## LocalAI comparison
- LocalAI's OpenAI chat path has no fixed maximum tool count and does not silently truncate tool definitions.
- It converts every OpenAI `tools` entry into its internal `Functions` representation, serializes the complete tool list as JSON, and passes it to the backend together with `tool_choice`.
- With tokenizer templates, the structured messages and full tools are passed separately to the backend so the model's native chat template can render them.
- With classic Go templates, LocalAI renders the tool list through the configured `function` template and the `.Functions` template data.
- Optional context compression counts the complete tool payload and `max_tokens` against the configured context size, but it compresses messages rather than imposing a tool-count limit.
