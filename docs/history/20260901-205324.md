# Session

User request: Esi.AI Studio models are still missing from the VS Code Chat model picker after extension version 0.1.3.

## Action plan

- [x] Inspect the active VS Code extension host and provider output. The host attempted model refresh before Studio was listening and logged connection failures.
- [x] Add the smallest diagnostic or functional fix at the provider boundary. Added a five-second automatic retry until models are loaded and the provider change event is fired.
- [x] Build, package, install, and validate the new version. Version 0.1.4 builds, packages, installs, and has no diagnostics in the touched TypeScript files.
- [x] Verify the runtime model list and report the remaining user action, if any. Studio currently serves 15 models; VS Code must reload its Extension Host to load 0.1.4, after which the retry should populate the picker.

## Summary

The missing picker entries were caused by the extension's initial `/models` request racing Studio startup. The extension now retries automatically every five seconds after a failed refresh and stops once a non-empty model list is received. The installed extension is `sidatacom.vscode-esi-ai-studio@0.1.4`.

The current Studio/watchdog instance is healthy on port 7010 and `/v1/models` returns 15 models. The active VS Code window predates the 0.1.4 installation, so reload the VS Code window or restart VS Code before checking the picker. Review this temporary log and remove it from `docs/history/` after confirming completion.
