# Session 2026-09-04 13:36:49

## User request

Investigate and fix incorrect image understanding in the OpenAI-compatible Studio API for OpenVINO VLM models. Use the real `test-chat-with-picture.png` file, test the running host through `/v1/chat/completions`, and use LocalAI under `origins/localai` as the technical reference. Do not change vLLM or SGLang behavior.

## Action plan

- [ ] Trace LocalAI multimodal request parsing and template marker insertion.
  - Read the OpenAI middleware, inference path, and multimodal template implementation.
  - Identify how image payloads and prompt markers are kept aligned.
- [ ] Trace the local OpenVINO VLM history path.
  - Read the controller image extraction, history construction, and model generation call.
  - Compare the history contract with the Qwen `chat_template.jinja` and OpenVINO bindings.
- [ ] Confirm the controlling root cause with a narrow discriminating check.
  - Verify the current history content and runtime/model configuration used by the API.
  - Prefer a focused unit or integration assertion that exposes missing image markers.
- [ ] Implement the smallest root-cause fix.
  - Preserve existing vLLM and SGLang paths.
  - Keep local Base64 image handling and transport contracts intact.
- [ ] Validate the fix.
  - Run the focused tests and repeat the real WebAPI test against the running host.
  - Check diagnostics and leave no unmanaged Studio/watchdog process from this session.
- [ ] Summarize the outcome and verification in this log.

## Execution tracking

- LocalAI reference inspection: in progress.
- OpenVINO/Qwen contract inspection: in progress.
- Implementation: pending.
- Validation: pending.
- Summary: pending.
