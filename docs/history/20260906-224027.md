# Session Log

User request: Fix the backend Load model flow so a model loaded in the background also appears immediately as a loading entry in the Active Models list after clicking the button.

Plan:
- Trace the pending-model creation and rendered collection path.
- Identify the state synchronization boundary that drops the loading item.
- Apply the smallest root-cause fix.
- Validate diagnostics and reproduce the click flow.

Completed:
- Found that the rendered collection discarded the local pending item when an older matching model existed.
- Changed the component to keep the pending item visible and suppress only the duplicate server item.
- Changed SignalR reconciliation to clear the pending item only after a matching non-loading status arrives.
- Razor diagnostics passed.
- Hot Reload applied successfully.
- Browser reproduction showed `0 active, 1 loading` immediately after clicking `Load model`.
- Fixed unload reconciliation by applying the authoritative status returned from the unload operation and rerendering immediately.
- Razor diagnostics passed again and Hot Reload was applied for the unload fix.
