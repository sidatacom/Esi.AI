# Session 20260829-092755

## User request
Den Titel einer Page im Header anzeigen. Aktuelle Referenzseite: `/backends`, Titel `Backends`.

## Action plan
- [x] Header in `MainLayout.razor` identifizieren und aktuelle Route verfügbar machen.
- [x] Seitentitel für die Studio-Routen zentral im globalen Header anzeigen.
- [x] Studio-Projekt bauen; Browser-Snapshot geprüft, laufender Prozess war noch nicht aktualisiert.

## Notes
- Der aktuelle obere Header enthält nur den statischen Link `About`.
- Die Seiten verwenden bereits eigene `<PageTitle>`-Elemente, deren Inhalt nicht automatisch im Layout verfügbar ist.

## Summary

`MainLayout.razor` ist die einzige Quelle für den Titel: Es zeigt für die Studio-Routen `Backends`, `Models`, `Chats`, `WebApi`, `Authentication` oder `Home` im oberen Header und im Browser-Tab an. Die doppelten `<PageTitle>`-Elemente wurden aus den sechs Studio-Pages entfernt. Der Titel wird bei interner Navigation über `NavigationManager.LocationChanged` aktualisiert. Der Build von `Esi.AI.Studio.csproj` war erfolgreich.

Der bereits geöffnete Browser-Prozess muss neu gestartet oder aktualisiert werden, damit die Änderung geladen wird. Bitte diese Zusammenfassung prüfen und die Datei danach aus `docs/history/` entfernen, damit sie nicht ins Repository gelangt.
