# Session Log

## User Request
The PC froze again while using Esi.AI Studio.

## Initial Hypothesis
The Studio process and watchdog are no longer running and port 7010 is free, so the freeze likely caused a workload or driver-level hang during model loading or generation rather than a currently active server process. The discriminating check is recent system/GPU error logging and the latest Studio runtime output.

## Action Plan
- [x] Inspect recent system, GPU, and Studio error evidence.
- [x] Identify whether the workload caused an OpenVINO/GPU resource or driver failure.
- [x] Apply the smallest preventive change if the cause is confirmed.
- [x] Build and validate without leaving a heavy runtime active.

## Status
The machine rebooted between boots at 12:34 and 12:36, with no clean Studio shutdown or managed exception recorded. The previous boot contained repeated Intel graphics workqueue CPU-hog warnings. Studio and the watchdog are currently stopped. OpenVINO defaults were reduced from 512 to 128 maximum new tokens in the API contract, model loader options, and Backends reset/default values. The Studio build succeeded, 13 focused OpenVINO tests passed, and diagnostics found no errors. The heavy Qwen model was not restarted after the freeze.
