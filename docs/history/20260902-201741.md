# Session log

User request:
Wenn ein Filter gesetzt ist und erneut angeklickt wird, soll er wieder deselektiert werden. Das soll für alle Filterbuttons in allen Gruppen gelten. Zusätzlich wird ein Button benötigt, um alle Filter zu löschen.

## Action plan
- [x] Update filter selection so clicking the active option clears it.
- [x] Add a control that resets every filter state.
- [x] Validate the component and focused build/checks.

## Changes
- Filter buttons now toggle their own selection. Clicking an active option clears that filter value.
- Added `Alle Filter löschen` beside the filter-group tabs.
- The reset clears all filter values and both boolean filter toggles (`Base only` and `Inference Available`).
- The reset button is disabled when no filters are active.

## Validation
- Controlled Studio debug restart completed successfully and the host reported ready.
- Browser check confirmed select, deselect, multi-filter reset, and checkbox reset behavior.
- `Models.razor` has no editor diagnostics.
- `git diff --check` passed.
