# Session

## Request
Continue pursuing 80 tokens per second for the Qwen3.8-27B vLLM XPU path.

## Findings
- Existing benchmark history proves MTP4 is active and reports accepted-token statistics.
- With `max_num_seqs=64`, the article-style configuration measured about 24-25 tok/s.
- With `max_num_seqs=1`, the same MTP path measured about 55 tok/s.
- Studio's `PythonForm.ToRequest` hardcoded `EnforceEager: true`, so enabling the separate XPU graph checkbox could never activate graph execution.

## Change
- Changed `Backends.razor` to send `EnforceEager: !EnableXpuGraph`.
- This makes graph mode and eager mode mutually consistent.

## Validation
- Studio build passed with no errors.
- The next graph-enabled single-sequence benchmark did not produce a verified result and was not treated as evidence.

## Next
Run a clean Studio or direct benchmark with `max_num_seqs=1`, XPU graph enabled, eager disabled, and MTP acceptance counters. The current 55 tok/s baseline is the honest target to improve before claiming 80 tok/s.
