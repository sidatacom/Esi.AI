# Session

## Request
Assess why the observed Studio throughput dropped from the article's 84.56 tok/s to approximately 50 and then 20 tok/s.

## Findings
- The current Studio measurement is not directly comparable to the article benchmark.
- No verified MTP acceptance or speculative decode statistics appear in the Studio/runtime/provider logs.
- The current runtime evidence indicates target-only decode or an engine startup/runtime failure, not confirmed active MTP4.
- Studio throughput includes end-to-end request timing, while the article value is likely a decode-only benchmark with specific prompt, concurrency, and flag settings.
- Provider trace showed mostly `/v1/models` polling and no verified provider chat request for this measurement.

## Conclusion
The fall to approximately 20 tok/s is a real regression relative to the desired target, but the main comparison is currently invalid. First verify active MTP4 and measure decode-only warm throughput with the article's exact flags before further optimization.
