# Session Log

## User Request
Vollständigen Test durchführen, einschließlich Prüfung, ob Modelle tatsächlich geladen werden.

## Testplan
- [x] Aktive Studio-Debugsession und Watchdog/Port prüfen.
- [x] Vorhandene fokussierte Unit- und Integrationstests ausführen.
- [x] Studio-Build und Native-Runtime-Artefakte validieren.
- [x] Studio über die überwachte Debug-Konfiguration starten oder vorhandene Instanz verwenden.
- [x] Backend-Status und Modellkatalog im Browser prüfen.
- [x] Ein Modell laden und eine echte Modellantwort/Chat-Anfrage prüfen.
- [x] Ergebnis, Einschränkungen und verbleibende Hardware-Abhängigkeiten dokumentieren.

## Results
- Studio build: successful.
- Core tests: 61 total, 53 passed, 8 skipped, 0 failed.
- Studio tests without a loaded image model: 61 passed, 1 failed because no image-capable model was loaded.
- SYCL: `tinyllama-1.1b-chat-v1.0.Q2_K.gguf` loaded successfully on `LLama / SYCL` with 20 GPU layers and 0.45 GiB; `/v1/chat/completions` returned a real response at about 31.3 tokens/s.
- OpenVINO: `Qwen3.8-27B-int4-ov` loaded successfully on `GPU.1` (Intel Arc Pro B70) with 14.68 GiB VRAM; `/v1/chat/completions` returned a real response at about 20.5 tokens/s.
- Studio tests with the OpenVINO image-capable model loaded: 62 passed, 0 failed. The image-capability test also passed independently.
- CUDA 12: native libraries are present and NVIDIA driver visibility was confirmed, but the TinyLlama load through the UI failed without runtime output.
- Vulkan: the first attempt correctly failed with zero enabled Vulkan devices; after enabling the RTX 4070 explicitly, the load still failed. No model was left loaded.
- OpenVINO `Qwen3-0.6B-int4-ov`: load failed with OpenVINO status `-17`; its directory contains the older `openvino_model.*` export and lacks the current `openvino_tokenizer.*` artifacts. This is a model-package compatibility issue, not evidence against the Arc runtime.

## Cleanup
- The test session used the watchdog-backed `Esi.AI Studio (Server Debug)` configuration.
- The Studio process and watchdog were stopped cleanly after the final validation; port 7010 is free.
