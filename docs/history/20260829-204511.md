# Session log

## User request
Implement the Python backend prerequisite process like OpenVINO, including requirement tiles/cards in the UI.

## Action plan
- [ ] Read applicable Blazor and C# instructions.
- [ ] Inspect OpenVINO requirement tile UI and Python backend UI/service contracts.
- [ ] Implement a common backend prerequisite status/repair model for Python backends.
- [ ] Add vLLM/SGLang requirement tiles with checks and repair actions.
- [ ] Validate builds and tests.

## Execution
- [x] Read applicable Blazor and C# instructions.
- [x] Inspected the existing OpenVINO diagnostic tile and SignalR service path.
- [x] Added shared prerequisite DTOs in `Esi.AI.Models`.
- [x] Added non-mutating Python diagnostics for requirements file, isolated environment, imports, CUDA, and optional XPU.
- [x] Added SignalR/DataService operations for diagnostics and authorized Python preparation.
- [x] Added common requirement tiles for LLama, dotLLM, vLLM, and SGLang; OpenVINO keeps its existing diagnostic tiles.
- [x] Added Core tests for native diagnostics and missing Python environments.
- [x] Built Studio successfully and ran Core (33 passed, 7 skipped) and Studio (9 passed) tests.
- [x] Verified the vLLM tile in the local browser at `http://127.0.0.1:7010/backends`; the isolated vLLM environment, imports, and CUDA were detected.
- [x] Switched the same UI tile to SGLang; its isolated environment, imports, and CUDA path reached `Ready` as well.

## Summary
Backend requirements now use one shared diagnostic contract and SignalR path. Python backends can be explicitly prepared from the Requirements tile, while unavailable optional Intel XPU hardware is shown as informational and does not block a ready CUDA backend. System-level OpenVINO driver repair remains on the existing authorized path.
