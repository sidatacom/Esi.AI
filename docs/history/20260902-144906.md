# Session 20260902-144906

## User Request
Continue troubleshooting the OpenVINO Qwen first-message exception.

## Action Plan
- [completed] Confirm the current VLM call path.
- [completed] Confirm the supervised Studio host and loaded model.
- [completed] Run a real OpenAI-compatible streaming request.
- [completed] Verify SSE completion and model stability.

## Findings
The current Esi.AI VLM path uses `ChatHistory` and `GenerateWithHistory`; the direct prompt streaming overload is not used by `OpenVinoChatSession`.

Qwen3.8-27B-int4-ov was loaded successfully on the running supervised Studio instance. A correctly encoded request to `/v1/chat/completions` with `stream: true` returned HTTP 200 and 47 SSE chunks. The response ended with `finish_reason: stop` and `data: [DONE]`. The model remained loaded afterward.

An earlier request in this session sent the model path with literal quote characters and correctly returned the expected model-not-loaded 503; that was a test command quoting error, not an application failure.

## Outcome
The current OpenVINO VLM and OpenAI-compatible streaming path is working in a live Qwen request. Studio and its watchdog remain running for continued provider testing.
