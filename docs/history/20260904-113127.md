# Session History

## User Request
Start implementation of multimodal image + text support for the OpenAI-compatible Studio path. The support must work for the Llama backend as well as existing OpenVINO vision support; vLLM and SGLang remain out of scope.

## Action Plan

- [complete] Inspect the current controller, model contracts, Llama loader/session, and LLamaSharp MTMD API signatures.
	- Read the controller validation and dispatch path.
	- Identify the current chat DTOs and the loaded-model configuration source.
	- Verify the referenced LLamaSharp version and the available MTMD APIs in the vendored origin.
- [complete] Add transport-neutral multimodal message data and parse OpenAI `text`/`image_url` content safely.
	- Preserve string content compatibility.
	- Decode only local `data:` image URLs and reject unsupported content with an OpenAI-compatible error.
	- Keep remote URL fetching out of the server boundary.
- [complete] Thread multimodal data through OpenVINO and Llama execution paths, including optional `mmproj` configuration for Llama.
	- Keep vLLM and SGLang unchanged.
	- Preserve text-only behavior for models without image capability.
	- Load and dispose MTMD resources with the Llama runtime.
- [complete] Add focused unit tests for parsing, backend capability rejection, and Llama/OpenVINO message preservation.
	- Add tests at the narrowest existing test seam available.
	- Avoid requiring downloaded model assets for parser/contract tests.
- [complete] Build and run focused tests, then clean up any project-owned debug processes.
	- Stop an active Studio debug session before a separate build/test if required by repository rules.
	- Validate the changed projects and report any environment-only gaps.

## Dependencies And Prerequisites

- The shared OpenAI-compatible request DTO must remain usable by the one allowed HTTP controller.
- The Core project must reference an LLamaSharp build exposing the MTMD types; if it does not, package/version work is a prerequisite rather than a controller-only edit.
- A real end-to-end image test requires a compatible GGUF model and matching `mmproj` file; unit tests should cover all behavior that does not require those assets.
- The Studio watchdog/debug lifecycle must be respected for any Studio build or runtime validation.

## Hypothesis And Discriminating Check

Hypothesis: the failure is caused by `OpenAiCompatibleController` rejecting array content before the existing backend adapters can receive it; Llama additionally lacks a loaded MTMD projector and a multimodal core contract. The cheapest check is to inspect the current request path and LLamaSharp package/API references before editing, then compile a focused change against those signatures.

## Completed Work

- Added transport-neutral `ChatImage` data and optional `MmprojPath` model configuration.
- Added local-only base64 `data:` URL parsing for OpenAI text/image content arrays; remote URLs are rejected without server-side fetching.
- Added image capability checks for OpenVINO and Llama while leaving vLLM and SGLang unchanged.
- Connected OpenVINO requests to the existing VLM pipeline and connected Llama requests to LLamaSharp MTMD media loading, markers, and projector lifecycle management.
- Added an optional Llama projector path to the backend configuration UI with adjacent-file auto-discovery for `mmproj*.gguf` files.
- Added focused controller coverage for image decoding and remote URL rejection.

## Validation

- `dotnet build src/Esi.AI/Esi.AI.Studio/Esi.AI.Studio.csproj --no-restore` passed.
- Focused controller tests: 15 passed, 0 failed.
- Relevant Core and Studio tests: 98 passed, 0 failed.
- Touched files report no diagnostics and `git diff --check` passes.
- A real model-backed multimodal inference smoke test was not run because no compatible local VLM/MTMD model asset was available.

## Final Status

Implementation is complete. The session created an active Studio process during build; the watchdog stop task was run and the orphaned process was then terminated, leaving port 7010 free. Review this summary and remove this session log from `docs/history/` when complete so it is not added to the repository.
