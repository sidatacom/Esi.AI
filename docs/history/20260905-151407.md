# Session request

User requested removal of the custom Studio watchdog and reconfiguration of VS Code Run and Debug according to Microsoft's recommended Blazor Web App setup for Interactive Auto, WebAssembly debugging, server debugging, and Hot Reload.

## Action plan

- [complete] Stop the existing supervised Studio session and verify no stale process remains.
- [complete] Remove watchdog startup gates, lease, process-isolation coupling, script, and VS Code task references.
- [complete] Configure standard server debugging, browser/WASM attach debugging, launch profiles, and Hot Reload support.
- [complete] Update repository and debug-skill instructions so they describe the new lifecycle.
- [complete] Build the affected Esi.AI Studio project and validate diagnostics and runtime readiness.
- [complete] Record the result and remaining validation limits.
- [complete] Rebuild after making Studio disposal idempotent.
- [complete] Run the complete MSTest suite after the disposal fix.
- [complete] Validate all Studio browser routes, HTTP APIs, OpenAPI, Swagger, and SignalR negotiation.
- [complete] Verify clean startup and controlled shutdown with no orphaned process or port listener.

## Execution notes

- Microsoft guidance confirms that the server project is the startup project, `inspectUri` enables integrated WASM debugging, `UseWebAssemblyDebugging()` is required, and `dotnet watch` is the CLI Hot Reload path.
- The existing `launchSettings.json` already contains `inspectUri` in both profiles.

## Summary

- Removed the custom watchdog script, PID gate, hosted lease, and Linux process-isolation class.
- Replaced the watchdog task chain with a normal Studio build task and CoreCLR server launch configuration.
- Added an explicit Chromium `blazorwasm` attach profile and changed client pages to `InteractiveAuto`.
- Updated workspace and debug-skill instructions to describe the Microsoft-style Blazor lifecycle.
- `dotnet build src/Esi.AI/Esi.AI.Studio/Esi.AI.Studio.csproj` passed.
- Final debug validation passed with one Studio process, port `7010` readiness, HTTP 200 for `/` and `/backends`, and a refreshed browser page.
- The debug proxy was not tested with an IDE breakpoint or WebSocket handshake; its normal HTTP probe is not a valid proxy session.

## Post-fix validation

- The rebuilt Studio host started from `src/Esi.AI/Esi.AI.Studio` and listened on `http://localhost:7010`.
- The controlled shutdown completed without `ObjectDisposedException`, `Unhandled exception`, or a remaining Studio process/listener.
- The complete test run produced 123 passing tests and one conditional failure: `OpenAiCompatibleApi_WhenPictureIsProvided_DescribesVisibleTools` requires a loaded image-capable model, which is not available in this environment.
- Browser routes `/`, `/backends`, `/models`, `/webapi`, `/chats`, and `/provider` rendered successfully without the Blazor error UI.
- HTTP checks returned 200 for all browser routes, `/v1/models`, `/openapi/v1.json`, and `/swagger/index.html`.
- SignalR `/hubs/data/negotiate?negotiateVersion=1` returned 200 with WebSockets, Server-Sent Events, and Long Polling transports.
- `/v1/models` returned a valid list with seven entries; OpenAPI returned version `3.1.1` titled `Esi.AI.Studio | v1`.
- The remaining test limitation is environment data, not the removed watchdog, debug configuration, build, or host lifecycle.

## Documentation follow-up

- Replaced the two README Studio images with fresh screenshots from the running `/backends` page.
- Expanded `README.md` with current Studio capabilities, documentation links, repository structure, build/debug instructions, and a complete referenced-project table.
- Added greetings and explicit upstream/fork or external-runtime relationships for Esi.AI, Fluent UI Blazor, LLamaSharp, llama.cpp, OpenVINO-CSharp-API, dotLLM, LiteLLM, LocalAI, vLLM, and SGLang.
- Verified all local README links, focused README whitespace, PNG validity, and the live Studio page used for the captures.
