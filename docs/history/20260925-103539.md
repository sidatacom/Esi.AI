# User Request
Create a standalone `Esi.AI.Backend.Vllm.Xpu` NuGet backend module in `/home/llm/Git/Esi.AI`, implementing `IBackendRuntime` with descriptor `vllm.xpu`, family Vllm, route XPU. Keep Core out of dependencies. Own/copy only required Python runtime scripts/requirements and gRPC protocol/client code from Esi.AI.Core into the package, with local generation contracts sourced from Models; preserve process lifecycle, XPU environment/bootstrap, load/readiness/status and normalized streaming generation, tools as supported. Add deterministic tests without Python/XPU hardware. Do not edit existing Studio/Core/solution/docs or other backend packages. Build, test, pack if feasible. Report limitations.

# Action Plan

1. [complete] Identify the closest standalone backend package pattern, IBackendRuntime contracts, and the exact Core Python/gRPC runtime dependencies. Read applicable C# instructions; hypothesis: the existing vLLM backend module offers a copyable standalone implementation and a narrow set of Core contracts/scripts, while the package can use Models directly. Cheap disconfirming check: inspect package project references and runtime descriptor/contracts before editing. Confirmed: no existing vLLM module; abstractions and Models provide the right package boundaries, while Core contains a separable bridge/provisioner and the Models project contains the proto source contract.
2. [todo] Add only a new `Esi.AI.Backend.Vllm.Xpu` package and its tests. Copy/adapt only required owned runtime scripts, requirements, protocol/client code, and local Models-sourced generation contracts; avoid touching existing modules or solution/docs.
3. [todo] Run focused deterministic tests, build and pack the new module where feasible; verify Core is absent from dependency references and report any blocked checks/limitations.
