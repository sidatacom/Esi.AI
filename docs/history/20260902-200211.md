# Session 2026-09-02 20:02:11

## User request

Bei einem fehlgeschlagenen SGLang-Ladevorgang soll im Backend-Log nur die eigentliche Fehlermeldung erscheinen, nicht der Python-Stacktrace, Wrapper-Meldungen oder der gesamte Start-Log. Für den konkreten Fall soll der `torch.OutOfMemoryError: CUDA out of memory...`-Block angezeigt werden.

## Action plan

- [x] Die Python-Bridge so ändern, dass sie nur die Root-Cause-Exception zurückgibt.
- [x] Den C#-Wrapper so ändern, dass keine zusätzlichen Start- und Wrapper-Logs an die Fehlermeldung angehängt werden.
- [x] Die Fehleranzeige und den gespeicherten Backend-Log für vLLM/SGLang auf den bereinigten Fehler begrenzen.
- [x] Build, Tests und Browserdarstellung validieren.
- [x] Debugsession und Watchdog nach der Validierung kontrolliert beenden.

## Summary

`inference_server.py` extrahiert bei vLLM-/SGLang-Ladefehlern nur die letzte Root-Cause-Exception und deren mehrzeilige Fehlermeldung. `PythonInferenceServer` hängt keine Bridge-Startlogs mehr an und bereinigt unerwartete Prozessausgaben ebenfalls. `DataService` schreibt nur diese bereinigte Ursache in `LoadLog`; die UI zeigt den Text ausschließlich in der Backend-Load-Log-Section und hält den globalen Status kurz.

Validierung:

- Python-Syntaxcheck erfolgreich.
- Studio-Build erfolgreich.
- `BackendCatalogIntegrationTests` und `PythonBackendProvisionerTests`: 38 bestanden, 0 fehlgeschlagen.
- Browserprüfung bestätigte die `BACKEND LOAD LOG / SGLang output`-Section und den kurzen Status `SGLang could not be loaded.`.
- Aktive Debugsession und Watchdog kontrolliert beendet; Port 7010 freigegeben.
