# Session History

## User Request
- Add a model to the Active Runtimes collection as soon as loading begins.
- Remove the separate loading log from the backend tab.
- Show which models are currently being loaded.

## Action Plan
- [completed] Inspect the shared runtime status and Backends component load paths.
- [completed] Add a backend-neutral pending model status to the runtime collection.
- [completed] Render pending models with loading state and remove the separate tab log.
- [completed] Build and run Core and Studio regression tests.
- [completed] Verify the rendered Backends page and stop the temporary verification server.

## Summary
`LoadedModelStatus` now includes an optional `IsLoading` flag. `ModelRuntime` tracks in-progress LLama, OpenVINO, vLLM, SGLang, and dotLLM loads and exposes them through the shared `LoadedModels` collection until completion or failure. The Backends page refreshes the shared status during OpenVINO loading, shows pending models with their path and runtime, distinguishes active/loading counts, and no longer renders the separate tab loading-log panel. Studio build, 37 Core tests, 26 Studio tests, and editor diagnostics passed. Browser verification confirmed the separate loading-log panel is absent.
