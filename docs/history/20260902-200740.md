# Session 2026-09-02 20:07:40

## User request

Der alte `Stop backend`-Button im Python-Backend-Bereich ist veraltet, weil das Entladen jetzt zentral oben in `Loaded models` über `Unload` erfolgt. Den alten Button entfernen.

## Action plan

- [x] Den veralteten Python-Stop-Button aus `Backends.razor` entfernen.
- [x] Den dadurch ungenutzten Handler entfernen, ohne den zentralen `Loaded models`-Unload-Weg zu ändern.
- [x] Razor-Diagnosen und Build validieren.
- [x] Debugsession und Watchdog nach der Validierung kontrolliert beenden.

## Summary

Der veraltete `Stop backend`-Button aus dem Python-Backend-Konfigurationsbereich und der ausschließlich dafür verwendete `StopPythonModelAsync`-Handler wurden entfernt. Der zentrale `Unload`-Button in der globalen `Loaded models`-Section bleibt unverändert und ist weiterhin der Entladeweg für geladene Modelle.

Validierung:

- Studio-Build erfolgreich.
- `Backends.razor`: keine Editorfehler.
- `StopPythonModelAsync` und `Stop backend` kommen im Component-Markup nicht mehr vor.
- Debugsession kontrolliert beendet, Watchdog gestoppt und Port 7010 freigegeben.
