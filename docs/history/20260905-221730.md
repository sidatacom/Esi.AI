# Session History

## User Request

Establish the standards-based tool handling for OpenVINO and extend the same correct approach to all other Esi.AI backends. Preserve complete tool definitions without arbitrary limits or prompt duplication, and verify the exact provider request path.

## Action Plan

- [ ] Define one transport-neutral tool contract shared by all local backends.
- [ ] Route OpenAI tool definitions through OpenVINO ChatHistory.SetTools.
- [ ] Route tools through dotLLM chat-template options.
- [ ] Extend the Python gRPC contract and mapper for structured tools.
- [ ] Add native or documented backend handling for LLamaSharp, with parser fallback where runtime support is absent.
- [ ] Remove backend-independent textual tool injection and update focused tests.
- [ ] Build and run focused tests, then replay the exact provider request if the runtime is available.

## Tracking

- Status: in progress
- User expanded scope to all backends during investigation.
