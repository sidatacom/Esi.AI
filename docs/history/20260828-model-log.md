# Current Request

Create a dedicated directory for Copilot process logs and create a new file for every session so the log does not become oversized. Record this behavior in the logging rule.

# Action plan

- [done] Create the dedicated `Copilot-Processing/` log directory.
- [done] Archive the accumulated root log without deleting its history.
- [done] Create a separate log file for the current session.
- [done] Update the logging rule to require a new timestamped file for each session.

# Final Summary

Process logs now live in `Copilot-Processing/`. The accumulated root log was preserved as `20260828-history.md`, and this request uses a separate session file. Future sessions must create a new timestamped Markdown file in that directory instead of appending to an existing log.
