# Session log

## User request
Eine gemeinsame Runtime-Ordner-Logik fuer alle Backends: LLama, Python-Backends und OpenVINO sollen ihre isolierten Runtime-Verzeichnisse ueber dieselbe zentrale Regel aufloesen.

## Action plan
- [x] Bestehende LLama-, Python- und OpenVINO-Pfadaufloesung lokal pruefen.
- [x] Zentrale `BackendRuntimePaths`-Aufloesung fuer Backend-Routen und Python-Root einfuehren.
- [x] LLama und OpenVINO an die gemeinsame Aufloesung anbinden.
- [x] Regressionstests fuer Route- und Python-Root-Aufloesung ergaenzen.
- [x] Core-Build und fokussierte Tests ausfuehren.

## Result
`Esi.AI.Core` baut erfolgreich. Die fokussierten Core-Tests laufen mit 78 bestanden, 8 uebersprungen und 0 fehlgeschlagen. Eine OpenVINO-Regression durch einen noch nicht existierenden lokalen Runtime-Ordner wurde behoben.

## Changed files
- `src/Esi.AI/Esi.AI.Core/ModelLoading/BackendRuntimePaths.cs`
- `src/Esi.AI/Esi.AI.Core/ModelLoading/PythonBackendProvisioner.cs`
- `src/Esi.AI/Esi.AI.Core/ModelLoading/LlamaModelLoader.cs`
- `src/Esi.AI/Esi.AI.Core/ModelLoading/OpenVinoModelLoader.cs`
- `src/Esi.AI/Esi.AI.Core.Tests/PythonBackendProvisionerTests.cs`

## Follow-up
Die zentrale Resolver-Logik ist vorhanden; die eigentliche Vervollstaendigung und Provisionierung aller nativen Bibliotheksdateien in den Zielordnern bleibt ein separater Packaging-Schritt.

## SYCL16 database validation
The SQLite database contains two LLama configurations: a CUDA browser test configuration and `Qwen3.8-27B-UD-Q4_K_S.SYCL16`. The latter uses `Backend: SYCL`, `GpuLayerCount: -1`, and `Devices: ["SYCL1"]`. The focused `LoadReferenceModel_LlamaSycl_GeneratesResponse` test passed with `/home/llm/.cache/esi-ai/models/Qwen3.8-27B-UD-Q4_K_S.gguf`; native loading reached the model successfully with no missing dependency error.

## UI load error
The Blazor page was catching every `ArgumentException` from the SignalR load workflow and replacing it with the misleading message `Enter a valid GGUF model path and GPU layer count.` The catch now displays the original exception message. The load path remains `IDataService`/SignalR; no direct HTTP API was introduced.

## Database correction
The SYCL16 model configuration in `src/Esi.AI/Esi.AI.Studio/Data/app.db` was corrected from the invalid `SYCL1`/`Vulkan1` selection to `SYCL0`/`Vulkan0`. The persisted configuration remains `Backend: SYCL` and `GpuLayerCount: -1`.

## Neutral LLama device identifiers
The LLama UI and persisted device weights now use neutral `GPU0`, `GPU1`, etc. identifiers. Backend-specific names are generated only at the native load boundary.

## Neutral status contract
The runtime status contract now exposes `DeviceStatus`, `Devices`, and `FoundGpuCount` instead of Vulkan-specific status names. Legacy `VulkanDeviceSetting` and `VulkanDeviceWeights` persistence names remain unchanged for database compatibility.
