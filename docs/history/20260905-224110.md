# Session history

## User request
Continue the structured tool-calling implementation using the verified LocalAI architecture. Preserve OpenAI-compatible tools, tool_choice, messages, images, assistant tool calls, and tool results across Esi.AI backends. Do not reintroduce universal `# Tools` prompt injection. Complete the remaining backend handling, tests, and documentation where feasible.

## Action plan

- [completed] Verify the Python bridge syntax and structured gRPC response contract.
- [completed] Add OpenVINO-native tool_choice handling without synthetic prompt text.
- [completed] Add focused OpenVINO and LLamaSharp capability tests.
- [completed] Document backend-specific template, parser, and capability behavior.
- [completed] Resolve the direct vLLM parser gap with an explicit capability rejection for active structured tools and text-only support for `tool_choice: "none"`.
- [completed] Run focused executable validation and static diagnostics.

## Work completed

- Confirmed `inference_server.py` compiles and the Python gRPC C# tests pass.
- Added OpenVINO tool selection mapping: `none` removes native tools, a specific function narrows the native list, and `auto`/`required` preserve it.
- Added regression tests for OpenVINO tool selection and existing structured-tool preservation/LLamaSharp rejection behavior.
- Added `docs/backend-tool-calling.md` and extended `docs/vllm-grpc.md` with the structured contract and backend matrix.
- Prevented direct `AsyncLLMEngine` vLLM requests with active tools from being returned as unstructured text. The bridge now reports the unsupported capability explicitly; `tool_choice: "none"` remains text-only.

## Validation

- `python3 -m py_compile src/Esi.AI/Esi.AI.Core/Python/inference_server.py`: passed.
- `dotnet test src/Esi.AI/Esi.AI.Core.Tests/Esi.AI.Core.Tests.csproj --no-restore --filter "FullyQualifiedName~PythonInferenceGrpcTests"`: 8 passed.
- Focused Studio tests for structured-tool preservation, 89-tool preservation, LLamaSharp rejection, and OpenVINO tool selection: 5 passed.
- Static diagnostics for all changed C# files: no errors.
- `git diff --check`: clean.

## Remaining limitations

- Direct vLLM `AsyncLLMEngine` tool parsing is not implemented because the native parser is exposed by the OpenAI server path, not this engine API. The limitation is explicit rather than silently flattening tool calls.
- Full model-backed Python runtime smoke tests remain environment-dependent and were not run in this session.

## Summary

Structured tool handling now follows the native template/parser model, with explicit backend capability boundaries and documentation. No universal `# Tools` system-prompt injection was reintroduced.
