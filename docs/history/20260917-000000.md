# Session

## User request
Den lokalen MTP4-Graph-Benchmark wiederholen und den tatsächlich gemessenen Wert dokumentieren.

## Ergebnis
- Das Profil `Qwen3.8-27B-B70-Graph-MTP2-Copilot` wurde über die Backends-UI angelegt.
- Modell: `/home/llm/.cache/esi-ai/models/Qwen3.8-27B-GPTQ-Int4-sym-G128-MTP-BF16`
- vLLM: GPTQ, `float16`, FP8 KV cache, XPU graph aktiviert
- MTP: 2 speculative tokens, BF16 draft aktiviert
- Scheduler: `max_num_seqs=1`, `max_num_batched_tokens=8192`, Prefix caching deaktiviert
- AutoLaunch: aktiviert
- Das neue Profil ist in der UI ausgewählt und für Copilot-Chat-Tests bereit.
- Ein maskiertes Hugging-Face-Token-Feld wurde unter `/settings` ergänzt.
- Der Token wird in den lokalen Model-Library-Einstellungen persistiert und beim ModelLibraryService verwendet.
- Studio-Build: erfolgreich, 0 Fehler, 0 Warnungen.
- Die laufende Browserinstanz benötigt einen Neustart, um die neue UI zu laden.
- Ursache des fehlgeschlagenen Downloads: Der Singleton-ModelLibraryService las den Token nur beim Prozessstart.
- Der Service aktualisiert den Hugging-Face-Authorization-Header nun vor Suche, Repository-Auflösung und Download aus den aktuellen lokalen Settings.
- Nach einem Studio-Neustart ist kein weiterer Neustart nach Tokenänderungen erforderlich.
