# Session

Date: 2026-09-04

## User request

Refactor Esi.AI.Studio incrementally into five explicit layers:

1. Model format, compatibility, and catalog
2. Runtime and compute
3. Serving, chat, and scheduling
4. Daemon, lifecycle, provisioning, and publication
5. Workbench/UI and transport adapters

Preserve the single OpenAI-compatible controller, central DataHub, DTO ownership in Esi.AI.Models, existing SignalR CRUD names, watchdog rules, and OpenVINO multimodal work. Begin with baseline/dependency direction and move IDataService into a transport-neutral Esi.AI.Studio.Contracts project, then continue with focused, testable slices.

## Current context

- The real Qwen3.8 OpenVINO PNG request reaches native inference but still returns UNKNOWN_EXCEPTION (-17).
- UI work remains blocked until the real PNG WebAPI path succeeds.
- Studio is currently running under the supervised watchdog on port 7010.

## Action plan

- [ ] Record baseline build/test and inspect current project references.
- [ ] Define and add Esi.AI.Studio.Contracts.
- [ ] Move IDataService to Contracts and move diagnostics DTOs to Esi.AI.Models.
- [ ] Update project references, namespaces, DI, and transport implementations.
- [ ] Build the affected projects and run focused contract/API tests.
- [ ] Document the five-layer dependency direction and next migration boundaries.
- [ ] Continue with runtime/serving/lifecycle slices only after the contract slice is green.

## Verification

- Use focused builds/tests after each edit slice.
- Do not build the Studio project in parallel with an active Studio debug session.
- Keep the watchdog-controlled Studio lifecycle intact.
