# Session History

## User Request
- Show the complete loaded model name in chat instead of a truncated name.
- Make chats deletable.
- Validate the implementation end to end.

## Action Plan
- [completed] Inspect the existing chat client, SignalR contract, server service, and persistence model.
- [completed] Add full model-name formatting to the chat model selector and assistant metadata.
- [completed] Add chat deletion through IDataService, SignalRDataService, DataHub, DataService, and the Chats page.
- [completed] Add a persistence regression test for deleting a chat and its messages.
- [completed] Run the focused deletion test and verify the UI in the running Studio application.

## Execution Notes
- The first test fixture used an unsupported backend and was corrected to avoid inference entirely by seeding a persisted message directly.
- The focused integration test passed, including an explicit assertion that the chat message count is zero after deletion.
- Browser verification confirmed that a newly created chat can be deleted and the UI selects the next chat.
- The browser's existing historical messages still display their stored model name `Qwen3`; newly persisted messages use the full model path supplied by the chat request.

## Summary
Chat deletion is wired through the required SignalR architecture and covered by a passing integration test. The chat page formats model names from the complete model path, and the delete control was verified in the browser. The running app was started with the existing SQLite database connection supplied through the environment.
