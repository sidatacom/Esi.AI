# Session 2026-09-06

## User request
Continue the OpenVINO native `Unknown error (-17)` investigation in Esi.AI Studio. Preserve the exact 91-tool request for primary validation, avoid workarounds and flick fixes, and identify the actual failure boundary.

## Action plan
- [in progress] Restore one clean Esi.AI Studio debug session and verify host readiness.
- [in progress] Load the OpenVINO model on `GPU.1` asynchronously.
- [pending] Replay the unchanged request with 91 tools and `max_tokens=65536`.
- [pending] Replay the same prompt with only `max_tokens=512` as the discriminating capacity check.
- [pending] Compare stream completion, native error, process lifecycle, and runtime logs.
- [pending] Implement focused diagnostics or the proven root-cause fix, without reducing tools or rewriting requests.
- [pending] Run focused validation and record the result.

## Findings so far
- `SetTools` accepts a JSON array and the model chat template iterates all tools; no tool-count or byte limit was found.
- The C wrapper collapses exceptions from `GenerateWithHistory` to status `-17`.
- The exact 91-tool request on a fresh load emitted only the assistant role chunk and keep-alives, then the client cancellation triggered the intended fatal Studio shutdown.
- A replacement orphan process was removed; one configured debug session is active again and host readiness is true.

## Summary
Pending until the clean model load and differential replay complete.
