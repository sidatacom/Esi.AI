# Session Log

## User request
Repair the OpenAI-compatible WebAPI path so VS Code Copilot Chat works again after confirming the OpenVINO backend works in the Studio UI.

## Action plan
- [x] Correlate provider logs with the OpenAI controller and current model state.
- [x] Reproduce one real streaming `/v1/chat/completions` request against the loaded OpenVINO model.
- [x] Identify the WebAPI regression and make the smallest compatible fix.
- [x] Add or update focused regression coverage for the WebAPI path if practical.
- [x] Build, test, and validate the monitored Studio/provider path.

## Findings
- OpenVINO UI generation works with greedy decoding (`temperature: 0`, `DoSample: false`).
- The WebAPI mapping left `DoSample` at its default `true` when `temperature: 0` was requested.
- The native VLM then failed with `OpenVinoSharp.OVException` status `-17`; `temperature: 0.7` succeeded.
- WebAPI mapping now disables sampling when temperature is zero.
- Added focused controller regression tests for zero-temperature greedy decoding and positive-temperature sampling.
- Built the Studio test project successfully; both new regression tests passed.
- Restarted the monitored Studio session, loaded `Qwen3.8-27B-Uncensored-int4-awq-g128-ov` on `GPU.1`, and verified the real streaming API request returned HTTP 200, content deltas, usage, a stop chunk, and `data: [DONE]`.
- The existing provider trace contains a Copilot-shaped streaming request using `temperature: 0.7` against the same model.

## Summary
- Restored the OpenAI-compatible OpenVINO WebAPI path by mapping zero temperature to greedy decoding (`DoSample: false`).
- Preserved the UI's `GenerateWithHistory(...)` path so the collapsed thinking display remains intact.
- Validation completed with focused MSTest coverage, a successful native-backed SSE request, and no diagnostics in the changed files.
