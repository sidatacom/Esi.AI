# Session

## Request
Correct the misleading Studio throughput measurement that reported approximately 20 tok/s compared with the article's 80+ tok/s.

## Plan
- Change the vLLM bridge metric from total request time to warm decode time.
- Keep the gRPC response contract unchanged.
- Validate the edited Python bridge and focused tests.
- Document the measurement distinction and remaining MTP validation gap.

## Implementation
- The vLLM bridge now starts its throughput timer on the first engine output event, excluding prompt formatting and prefill from `tokens_per_second`.
- The gRPC response contract is unchanged.
- The first post-build reload was inconclusive because Studio returned `BrokenPipeError` before the model became loaded; this is separate from the metric change.

## Validation
- `python3 -m py_compile src/Esi.AI/Esi.AI.Core/Python/inference_server.py` passed.
- `git diff --check` passed.
