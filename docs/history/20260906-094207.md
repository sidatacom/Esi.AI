# Session history

## User request
The user explained that the test agent has the correct OpenVINO model and asked whether Copilot Chat can be tested directly. They then requested that a separate agent can be created for each model so the other backends can also be tested directly.

## Plan

- [ ] Verify the existing OpenVINO test agent and provider diagnostics.
- [ ] Load the exact OpenVINO model configured by the test agent.
- [ ] Run a real OpenVINO chat smoke test through the Studio/provider contract.
- [ ] Add backend-specific test agents for LLama, dotLLM, vLLM, and SGLang.
- [ ] Report which backend agents are immediately testable and which require model/runtime loading.

## Initial findings

- The provider base URL is `http://127.0.0.1:7010/v1`.
- Provider logs showed successful `/v1/models` polling but no chat request yet.
- The model configured by `test-openvino.agent.md` is registered with tool, image, agent, and thinking capabilities and was initially unloaded.

## Agent capability update

- Added `web` and `agent` to all backend test agents while retaining `execute`, `read`, `search`, and `todo`.
- Kept `edit` excluded because these agents are test runners and must not change production code during validation.

## Realistic test-agent tool contract

- The user supplied the realistic tool contract used by the OpenVINO test agent: VS Code control, execution, reading, sub-agents, GitHub pull-request operations, Python environment operations, editing, search, web, browser, EsiMCP, Fluent UI MCP, Microsoft documentation MCP, Pylance MCP, and todo tracking.
- Synchronized `test-llama.agent.md`, `test-dotllm.agent.md`, `test-vllm.agent.md`, and `test-sglang.agent.md` with the same full `tools:` declaration.
- The OpenVINO agent already contained this declaration and was preserved.
- Validation: all five agent files report no diagnostics; targeted tool declarations are identical.

## OpenVINO VLM runtime fix

- Reproduced the configured Qwen3.8 VLM failure with a minimal request that had no tools, images, or reasoning options. Both streaming and non-streaming requests returned OpenVINO `Unknown error (-17)`.
- Sampling mode was not the cause; `temperature: 0` and `temperature: 0.7` failed the same way.
- Changed `OpenVinoChatSession` so text-only VLM requests use `ChatHistory` with `VLMPipeline.GenerateWithHistory`, matching the current OpenVINO GenAI VLM API and sample path. The obsolete direct `Generate(..., Array.Empty<Tensor>())` branch was removed.
- Validation: the focused OpenVINO/model-runtime tests passed (`27/27`), the Esi.AI Core test project passed (`70/78`, `8` skipped), the Studio test project passed (`71/72`, `1` skipped), and the Studio project build passed.
- A post-restart live model request could not be rerun because the current Studio database exposes no stored model catalog entries, so the configured model was not loaded automatically after the controlled debug restart.

## Repeated live OpenVINO validation

- Loaded `Qwen3.8-27B-Uncensored-int4-awq-g128-ov` from the Studio OpenVINO configuration on `GPU.1`.
- Minimal streaming chat succeeded through `/v1/chat/completions`; the response emitted text, ended with `finish_reason: stop`, and sent `[DONE]`.
- Copilot-equivalent streaming request with one function definition and `tool_choice: auto` succeeded; the model emitted a valid `get_weather` tool call with `city: Berlin` and `finish_reason: tool_calls`.
- The previous OpenVINO `Unknown error (-17)` was not reproduced after the VLM text-only routing fix.

## LLamaSharp Qwen runtime validation

- CUDA 12 model: `Qwen3.8-27B-GSQ-RCO-IQ3_XXS.gguf`.
- CUDA `/v1/models` reported the GSQ model as loaded after replacing the previously loaded Q6 model.
- CUDA non-streaming chat smoke test returned HTTP 200, `finish_reason: stop`, and a non-empty response. The model emitted reasoning before the 32-token limit, so the exact sentinel was truncated.
- SYCL 16 model: `Qwen3.8-27B-Uncensored-Q6_K.gguf`.
- After a controlled Studio debug restart, SYCL loaded the Q6 model; `/v1/models` reported it as loaded.
- SYCL non-streaming chat smoke test returned HTTP 200, `finish_reason: stop`, and a non-empty response. The model emitted reasoning before the 32-token limit, so the exact sentinel was truncated.
- The Backends page retained stale Vulkan discovery telemetry after the runtime switch, while `/v1/models` and successful inference reflected the active loaded model. API state was used as the authoritative verification source.
