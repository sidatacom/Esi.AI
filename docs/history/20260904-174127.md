# Session

## User request
Remove the "Bibliothek aktualisieren" button from the Models page and remove the related scan function.

## Plan
- [x] Locate the button and page scan handler.
- [x] Remove the manual refresh button.
- [x] Keep initial local-model loading under a non-scan name.
- [x] Validate the Studio build.
- [x] Verify the updated page in the monitored Studio session.

## Summary
Removed the manual library refresh action and the page-local scan handler from `Models.razor`. Initial model loading remains available directly during component initialization, while the shared server-side local model scan remains because other application paths depend on it. The Studio build completed successfully, and the browser confirmed that both the button and its visible text are absent.
