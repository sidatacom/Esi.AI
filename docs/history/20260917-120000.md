# Session

## Request
Continue optimizing Qwen3.8-27B vLLM XPU toward 80 tokens/second.

## Plan
- Run a sequential graph-enabled single-sequence MTP4 benchmark.
- Run an identical eager baseline only after the graph process is fully cleaned up.
- Compare decode-only throughput and MTP acceptance.
- Apply only validated settings to Studio.

## Status
- Studio has an explicit interactive optimization action.
- Prior concurrent A/B attempt was invalid because both engines exhausted XPU memory.
- Graph mode loaded successfully with `VLLM_TARGET_DEVICE=xpu` and the project memory fallback.
- Corrected graph benchmark: 46.898, 46.862, and 46.859 tok/s; median 46.862 tok/s for 132 completion tokens per run.
- Graph capture and MTP4/BF16 initialization succeeded. The harness was corrected to use `google.protobuf.empty_pb2.Empty` for readiness.
- Graph benchmark cleanup succeeded. The `libtorch_cuda.so` deep-gemm warning is non-fatal on XPU.
