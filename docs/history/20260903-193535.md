# Session Log

## User request
Continue the investigation and resolution of the current Esi.AI Studio/provider SSE stream-abort issue.

## Context
- Workspace: `/home/llm/Git/Esi.AI`
- Current provider version: 0.1.23
- Main symptom: HTTP 200 and an initial SSE chunk arrive, then the request ends with `TypeError: terminated` before `[DONE]`.
- Suspected boundaries: OpenVINO generation, server-side SSE exception handling, provider stream-reader error handling, and large `max_tokens` behavior.

## Working hypothesis
The observed `TypeError: terminated` is a secondary symptom. A stale Studio process can stop servicing requests while the provider keeps retrying `/v1/models`; independently, a native generation failure after the SSE response starts is not currently recorded with enough context, and a provider `reader.read()` rejection escapes as an opaque error.

## Discriminating check
Run a fresh watchdog-backed Studio instance, then compare a small no-tool streaming request with a small tool request. Capture the HTTP stream and Studio/provider traces. A no-tool failure isolates the host/runtime; a no-tool success followed by a tool failure isolates structured OpenVINO generation.

## Action plan
- [complete] Inspect current code, trace, debug state, and port ownership.
- [complete] Stop the stale debug session and remove the unresponsive Studio process.
- [complete] Add focused diagnostics/error propagation at the server and provider stream boundaries.
- [complete] Build and run focused OpenVINO/provider tests after edits.
- [complete] Start exactly one watchdog-backed Studio session and validate minimal SSE probes.
- [complete] Record the result and final change summary.

## Changes
- `OpenAiCompatibleController.StreamCompletionAsync` now emits flushed SSE comment heartbeats every 15 seconds while a generation produces no delta. This keeps long buffered OpenVINO tool generations alive through idle-sensitive clients or proxies.
- The provider now reports an unexpected stream-reader failure with context and rejects an SSE response that ends without `[DONE]`, instead of treating a truncated response as successful.
- Provider version advanced to `0.1.24`; bundle and VSIX were built and installed.

## Validation
- Studio build succeeded.
- Focused Studio/OpenVINO tests: 31 passed.
- Provider TypeScript build succeeded.
- VSIX `vscode-esi-ai-studio-0.1.24.vsix` installed successfully.
- Fresh watchdog-backed host responded to `/v1/models`.
- Loaded Qwen OpenVINO no-tool and structured tool SSE probes both returned deltas and `[DONE]`.
- A separate fresh load of `Qwen3.8-27B-int4-ov` did not finish within five minutes; no code change was made for that independent model-loading issue.

## Cleanup
The final debug session and watchdog were stopped after validation. The generated session log should be reviewed and removed from `docs/history/` before committing repository changes.
