# Session Request

User reported that OpenVINO `Unknown error (-17)` still occurs from Copilot Chat after the previous lifecycle change. The screenshot shows the failure on model `Qwen3.8-27B-Uncensored-int4-awq-g128-ov` through the installed provider `0.1.27`.

## Action Plan

- [in-progress] Correlate the current provider request, native error, model state, and installed build.
- [ ] Reproduce the exact failing request after a clean host/model lifecycle.
- [ ] Identify and fix the controlling root cause without provider truncation or a fixed tool-count workaround.
- [ ] Validate with focused build/tests and a live API/provider replay.
- [ ] Record the final diagnosis and validation.
