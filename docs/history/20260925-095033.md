# User Request
Optimize and clarify backend architecture by enforcing clearer layers and dedicated interfaces at layer boundaries. Establish a unified backend interface with one backend implementation per variant, following the LocalAI pattern: llama.vulkan, llama.cuda12, llamasycl, openvino, vllm.cuda12, and vllm.xpu.

# Action Plan
1. Inspect current backend implementation and LocalAI reference. Status: complete. Dependency: none.
   - Identify the existing model/backend selection and implementation boundary. Status: complete.
   - Compare LocalAI's variant-specific backend structure with the current project. Status: complete.
2. Define a package-facing backend contract. Status: complete. Dependency: action 1 findings.
   - Add `Esi.AI.Backend.Abstractions` with module registration and runtime lifecycle/generation interfaces. Status: complete.
   - Move `GenerationResult` to `Esi.AI.Models` so it is independent of Core. Status: complete.
3. Extract implementations into the six requested backend assemblies. Status: incomplete. Dependency: action 2.
   - Refactor `ModelRuntime` and inference services to consume the normalized runtime interface. Status: not started.
   - Move the LLama Vulkan, CUDA12, and SYCL implementations and assets into their own projects. Status: not started.
   - Move OpenVINO, vLLM CUDA12, and vLLM XPU implementations and assets into their own projects. Status: not started.
   - Remove engine-specific references from `Esi.AI.Core` after extraction. Status: not started.
4. Validate the completed backend extraction. Status: partial. Dependency: action 3.
   - Build `Esi.AI.Backend.Abstractions` and `Esi.AI.Core`. Status: complete.
   - Pack the abstractions NuGet and inspect its dependencies. Status: complete.
   - Run focused runtime registry tests. Status: complete, 7 passed.
   - Add and run focused tests for each extracted backend module. Status: not started.

# Summary
Created a NuGet-packable engine-neutral backend contract assembly and moved the common generation result to Models. The abstraction package packed successfully with dependencies only on Models and Microsoft.Extensions.DependencyInjection.Abstractions. Core build passed and 7 backend registry tests passed. The requested six runtime implementation assemblies remain outstanding: existing loaders and engine references are still in Core. See `docs/backend-assemblies.md` for the target dependency graph and migration order.
