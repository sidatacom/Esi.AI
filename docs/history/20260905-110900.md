# Session 20260905-110900

## User request
Add a concise main rule stating that only projects under `src/` in the `Esi.*` namespace are compiled and tested, while everything under `origins/` is reference/template material only.

## Action plan
- [x] Update `.github/copilot-instructions.md` with the repository build/test scope.
- [x] Validate the rule file and record completion.

## Result
- Added the concise repository-wide rule: only `src/` projects in the `Esi.*` namespace are compiled and tested; `origins/` is reference/template material only.
- `git diff --check` passed.
- The rule was confirmed at lines 20-23 of `.github/copilot-instructions.md`.

Review this summary and remove this session log from `docs/history/` when it is no longer needed in the repository.
