# Session log

Request: Live-test virtual commands `readyness`, `restart`, and `stop`; start a debug session, restart it, then stop it.

Checks:
- C# Dev Kit 3.20.199 is active.
- Virtual commands are registered: `csdevkit.debug.check.host.readyness`, `csdevkit.debug.restart`, `csdevkit.debug.stop`.
- Port 7010 is free.

Blocked:
- `mcp_esimcp_csharp_devkit_execute_command` first reported the command as not contributed, then was disabled by the user/tool permission state.
- No debug session was started, restarted, or stopped because the required C# Dev Kit command bridge was unavailable.

Summary: Live lifecycle test could not be executed until the C# Dev Kit EsiMCP command tool is enabled again.
