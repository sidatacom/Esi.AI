# Session 20260830-122501

## User request
Enable Intel Arc Pro B70 support for the SGLang backend through its backend-specific requirements, validate real model load/chat and tokens/sec, and never use the Intel UHD 630.

## Action plan
- [x] Update SGLang requirements with the official XPU PyTorch stack.
- [x] Validate XPU runtime visibility with Arc-only routing and CUDA disabled.
- [x] Correct application XPU environment routing if required by the runtime.
- [ ] Run the real SGLang model-load/chat measurement on Arc Pro B70.
- [x] Record the exact remaining blocker.

## Follow-up request
GPU selection must work for every backend, including multi-GPU and vendor-specific driver requirements.

## Completed changes
- Added backward-compatible `Devices` routes to Python inference requests and the gRPC contract.
- Python UI now supports known-device checkboxes plus arbitrary additional routes such as `cuda:1, cuda:2`.
- CUDA routes are combined into `CUDA_VISIBLE_DEVICES`; XPU routes use the Arc-safe `ONEAPI_DEVICE_SELECTOR=level_zero:gpu` and clear CUDA visibility.
- CUDA and XPU routes are rejected in one vLLM/SGLang worker because those workers require one device platform and driver stack.
- OpenVINO device diagnostics expose vendor and plugin-driver metadata; Vulkan selection remains multi-vendor and weight-based.
- UHD 630 remains excluded from the UI and XPU routing.
- Added a backend support matrix with NVIDIA/CUDA, Intel/XPU, AMD/ROCm, multi-GPU, multi-vendor, and driver-strategy columns.
- Python route checkboxes are filtered by backend: vLLM exposes CUDA, while SGLang exposes CUDA and XPU.

## Validation
- `dotnet build src/Esi.AI/Esi.AI.Studio/Esi.AI.Studio.csproj --no-restore` passed.
- `dotnet test src/Esi.AI/Esi.AI.Core.Tests/Esi.AI.Core.Tests.csproj --no-restore` passed: 33 passed, 7 inconclusive.
- Python bridge syntax and protobuf multi-device smoke checks passed in the SGLang environment.
- Studio build passed after the matrix and route-filter changes; browser verification confirmed the matrix renders and backend-specific routes are visible.

## Remaining blocker
The real SGLang Arc model test is still blocked before model loading. SGLang 0.5.18 installs the CUDA-only `sglang-kernel==0.4.6.post1`, which imports `libnvrtc.so.13`. The documented XPU replacement is `sgl-kernel-xpu` from source, but this host environment has no C/C++ compiler, so its build cannot currently complete.

## Device-driven Python requirements
- Added `vllm-xpu-requirements.txt` with XPU Torch wheels, vLLM 0.28.0, and `vllm-xpu-kernels==0.1.14.1`.
- Added `sglang-cuda-requirements.txt` so SGLang CUDA and Intel XPU routes use separate Torch environments.
- Python backend provisioning now selects the requirements file and isolated environment from the model's selected device routes.
- CUDA and XPU routes remain rejected together in one worker; mixed selections now return a clear prerequisite diagnostic.
- XPU diagnostics require `torch.xpu.is_available()` and vLLM XPU diagnostics also require the `vllm_xpu_kernels` module. CUDA is optional for an XPU route and XPU is optional for a CUDA route.
- The support matrix now exposes vLLM Intel XPU as an installation option, while the Intel UHD 630 remains excluded.

## Latest validation
- Focused XPU diagnostic test passed.
- Focused mixed-device diagnostic test passed.
- Full Core test suite passed: 37 passed, 7 skipped/inconclusive, 0 failed.
- Studio build passed and both Python requirements profiles are present in build output.
- pip dry-run resolved the XPU Torch wheels and `vllm-xpu-kernels` without modifying the CUDA environment.
- Browser verification confirmed the matrix, vLLM CUDA route, vLLM Intel XPU route, isolated XPU environment name, clear mixed-vendor rejection, and exclusion of the UHD 630.

## Still pending
- The fresh `esi-ai-vllm-xpu` environment has not been installed yet.
- Real vLLM Intel Arc model load/chat and tokens-per-second measurement remain pending.
- The SGLang Intel XPU kernel/compiler blocker remains unresolved.
