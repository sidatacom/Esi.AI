# Session History

## User Request
Generalize backend-specific model settings and model collections. Replace the split `LlamaSettings`/`OpenVinoSettings` and `LlamaModel` application contracts with generalized model settings and model inventory contracts while preserving backend-specific behavior and persisted data. Keep collection operations aligned with `<Entity>_<Operation>` across `IDataService`, `DataService`, `DataHub`, SignalR, UI, and persistence. Add focused tests and validate with build/tests.

## Action Plan
- [in-progress] Inspect concrete DTOs, service contracts, persistence entities, migrations, and backend UI mappings.
- [pending] Define and implement a transport-independent `ModelSettings` contract with backend-specific configuration payloads.
- [pending] Define and implement the generalized `Model` collection contract across service, hub, client, and UI.
- [pending] Consolidate persistence access while preserving existing settings and model data.
- [pending] Update focused integration tests and add coverage for generalized contracts.
- [pending] Build, run focused tests, inspect diagnostics, and summarize the result.

## Detailed Tasks
1. Inspect `IDataService`, `DataService`, `DataHub`, `SignalRDataService`, shared DTOs, entities, migrations, and `Backends.razor` mappings.
2. Establish common model identity fields and a backend discriminator; keep backend-specific option records as serialized configuration payloads.
3. Replace public split settings methods with `ModelSettings_Create`, `ModelSettings_Read`, and `ModelSettings_Update` contracts as appropriate for the persisted singleton/settings collection.
4. Replace `LlamaModel_*` public collection operations with `Model_*` operations and update all callers/events/startup code.
5. Map legacy `LlamaSettings` and `OpenVinoSettings` storage into the generalized settings source without dropping existing data.
6. Update tests using `MethodName_Condition_ExpectedResult()` names and cover Llama/OpenVINO round trips plus generalized model reads/updates.
7. Run focused tests, build the Studio project, check diagnostics, and record completed actions and residual risks.

## Local Hypothesis
The split settings APIs are an outdated persistence boundary: both settings concepts share model path/profile identity and can be represented by one model-settings collection with a backend discriminator and JSON payload. The model inventory is already backend-neutral in `BackendModel`, so the old GGUF-only `LlamaModel` collection should be removed from the public application contract.

## Discriminating Check
The concrete DTOs/entities confirm shared identity fields and backend-specific payload fields. The implementation should compile after changing the central service contracts; any remaining compiler errors identify stale callers or persistence boundaries.
