# Session Log

## User Request
Continue the existing investigation and fix the Studio chat streaming failure so SignalR streaming, persistence, and end-to-end browser behavior work reliably. Test until verified.

## Action Plan
- [x] Reproduce the long OpenVINO streaming path with the exact loaded model.
- [x] Identify whether token count or runtime lifecycle causes the host termination.
- [x] Increase watchdog tolerance for long OpenVINO prefill operations.
- [ ] Restart Studio, reload the model, and verify browser streaming and persistence.
- [ ] Run final focused validation and summarize residual risks.

## Findings
- The exact Uncensored OpenVINO model passes isolated 512-token streaming.
- Studio host termination occurs during browser generation while the watchdog uses a 30-second failure window.
- The watchdog default was increased to 60 checks at 3 seconds each (3 minutes).
