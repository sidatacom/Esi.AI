# Session 20260830-115303

## User request
Die Tests für vLLM und SGLang sollen realistisch mit Model-Load und Chat durchgeführt werden. Die Messungen sollen abwechselnd auf NVIDIA RTX 4070 und Intel Arc Pro B70 erfolgen. Die Intel UHD 630 (Onboard-GPU) darf keinesfalls verwendet werden.

## Action plan
- [x] Verfügbare Python-Umgebungen, vLLM/SGLang-Pakete und kompatible Modelle prüfen.
- [x] PCI-/Runtime-Gerätezuordnung verifizieren und einen expliziten Selector für RTX 4070 bzw. Arc Pro B70 bestimmen.
- [x] Falls erforderlich, die Laufzeit so erweitern, dass die Auswahl pro Test kontrolliert und die UHD 630 ausgeschlossen wird.
- [x] Einen echten vLLM-Model-Load und Chat auf der RTX 4070 durchführen und Tokens/sec erfassen.
- [x] vLLM beenden und einen echten SGLang-Model-Load und Chat auf der Arc Pro B70 durchführen; der Lauf ist wegen fehlendem XPU-Runtime-Support fehlgeschlagen.
- [x] SGLang-Tokens und Tokens/sec aus belastbaren Telemetriedaten erfassen oder die Bridge dafür ergänzen; für den blockierten Intel-Lauf gibt es keine Messung.
- [x] Ergebnisse, verwendete Geräte und eventuelle Blocker mit Original-Logs dokumentieren.

## Current status
- UHD 630 is explicitly excluded.
- Intel GPU selector tools were not found in PATH.
- Existing worktree contains unrelated/in-progress changes; they must be preserved.

## Final validation

- The shared model-level device route is now passed through the Python reference integration tests via `ESI_VLLM_DEVICE` or `ESI_SGLANG_DEVICE`.
- The vLLM reference test passed on the NVIDIA RTX 4070 with `CUDA_VISIBLE_DEVICES=0`, using `/home/llm/.cache/esi-ai/models/Qwen2.5-0.5B-Instruct` and vLLM 0.28.0.
- Direct gRPC bridge measurement also passed on the RTX 4070: response `vLLM bridge passed.`, 7 generated tokens, 48.50 tokens/sec.
- vLLM requires `VLLM_USE_FLASHINFER_SAMPLER=0` in this environment because FlashInfer otherwise attempts JIT compilation without a usable CUDA/Ninja setup. No UHD 630 device was selected.
- The SGLang reference test was attempted with `ESI_SGLANG_DEVICE=xpu:1`, `CUDA_VISIBLE_DEVICES=''`, `ONEAPI_DEVICE_SELECTOR=level_zero:1`, and `ZE_AFFINITY_MASK=1`. It failed before model loading because PyTorch 2.13.0+cu130 reports `torch.xpu.is_available() == false`; SGLang reports `No accelerator ... is available`.
- No Intel Arc Pro B70 tokens/sec measurement is claimed. The Intel UHD 630 remains excluded.
- The focused Core test project build passed after the test routing change.
