# Session Request

User supplied a detailed video description and sources about Alibaba's purported Qwen3.8-Flash-Next model, including the claimed Qwen4ExpForConditionalGeneration configuration, MoE architecture, 51B N-gram embedding table, host-memory offloading, training methods, benchmarks, and licensing.

The supplied Hugging Face model card and raw `config.json` confirm `model_type: qwen4_exp`, architecture `Qwen4ExpForConditionalGeneration`, a 125B/6B MoE core, 51B n-gram embedding parameters, 4B MTP parameters, 512 experts, 10 routed experts per token, 48 layers, and 262,144 native context. The checkpoint is Transformers BF16/Safetensors and is documented for vLLM, SGLang, and TokenSpeed rather than the Studio project's GGUF or OpenVINO loading paths.

## Action Plan

- [completed] Determine the intended task from the supplied material and current Studio context.
  - Inspect the active backend page and model/backend configuration surfaces.
  - Check whether the supplied model should be added, documented, or fact-checked.
  - Ask for clarification if no concrete implementation target is present.
- [completed] Implement a shared C# process manager and Python launcher for vLLM and SGLang.
- [completed] Integrate loading, status, stopping, chat generation, DTOs, and SignalR operations.
- [completed] Add the Python engines tab to the Studio backend page.
- [completed] Run focused validation for the Core and Studio projects plus Python syntax/deployment checks.

## Summary

Implemented vLLM and SGLang as Python inference-server backends. C# starts `Python/inference_server.py` as an isolated child process, selects the engine, waits for `/v1/models`, exposes status, stops the process tree, and sends chat requests through the OpenAI-compatible API. DTOs and SignalR methods keep the existing Studio architecture intact. The `/backends` page now exposes engine, model ID/path, Python executable, port, context length, tensor parallelism, GPU memory utilization, and start/stop controls.

Validation: `dotnet build src/Esi.AI/Esi.AI.Core/Esi.AI.Core.csproj --no-restore`, `dotnet build src/Esi.AI/Esi.AI.Studio/Esi.AI.Studio.csproj --no-restore`, and `python3 -m py_compile src/Esi.AI/Esi.AI.Core/Python/inference_server.py` succeeded. Runtime testing requires installed Python plus vLLM/SGLang and their accelerator dependencies.

## Follow-up

- Added runtime telemetry panels inside the Python engines and dotLLM tabs, matching the existing OpenVINO tab layout.
- Each panel reports its backend-specific executable, model, endpoint/device, and ready/not-running state.
- Validation completed with the Studio build and no diagnostics in `Backends.razor`.
- Kept `Esi.AI.Studio` as the only externally reachable API: backend engine processes bind to loopback, and the Studio OpenAI-compatible controller now routes requests to the active LLama, OpenVINO, Python, or dotLLM runtime.
- Replaced the dotLLM server-process integration with direct in-process GGUF loading and generation through the dotLLM Core, Models, and Engine projects.
- Removed dotLLM executable, host, and port controls from the Studio UI and load contract.
- Studio build completed successfully after the in-process integration.
- vLLM/SGLang remain process-based because no embeddable Python engine API or Python runtime bridge exists in the repository; replacing those requires a separate Python embedding design.
