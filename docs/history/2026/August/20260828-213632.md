# Session Log

## User Request
Beim Klick auf `Load model` in der Backends-Seite erscheint `failed`.

Nachtrag: Die Lösung soll aufgeräumt werden und eine saubere Trennung zwischen LLama und OpenVINO erhalten; beide Pfade sollen vollständig getestet werden.

## Action Plan
- [complete] Backend-Grenzen, aktuelle Änderungen und Tests inventarisieren.
- [complete] Trennung in UI, Services und Verträgen bereinigen.
- [complete] Tests für beide Backendpfade ergänzen oder korrigieren.
- [complete] Builds, Tests und Browserpfade für beide Backends ausführen.

## Summary
Die Browser-Aktionen laufen über `IDataService`/`SignalRDataService` und `DataHub`; die Anwendungsseiten verwenden keine `EditForm`- oder Submit-Handler mehr. LLama und OpenVINO sind in UI, Service-Routing und Persistenz getrennt. LLama akzeptiert ausschließlich `.gguf`; OpenVINO verwendet eigene Load-/Settings-Verträge und die neue `OpenVinoSettings`-EF-Tabelle.

Validierung:
- Studio-Build erfolgreich.
- `Esi.AI.sln`-Build erfolgreich.
- Core-Tests: 16 erfolgreich, 2 optionale Hardware-/Modelltests übersprungen, 0 fehlgeschlagen.
- OpenVINO- und LLama-Fokustests: 18 insgesamt, 16 erfolgreich, 2 übersprungen.
- Der zusätzliche VLM-Kompatibilitätstest verhindert den nativen Qwen3.8-Load ohne kompatible Runtime; die vorhandene lokale OpenVINO-2026.4-Runtime wird automatisch erkannt.
- EF-Migration `20260828195332_AddOpenVinoSettings` beim Hoststart erfolgreich angewendet.
- Browser: `/backends` und `/chats` ohne Formulare oder Submit-Buttons; LLama zeigt nur GGUF-Auswahl, OpenVINO zeigt den gespeicherten IR-Pfad separat.

Der direkte native Qwen3.8-VLM-Load wurde reproduziert: Er beendete den Host mit Exit 139, wenn er an die inkompatible 2026.3-Runtime weitergereicht wurde. Der Loader wählt nun automatisch die vorhandene kompatible OpenVINO-2026.4-Runtime aus dem Esi.AI-Cache; ohne kompatible Runtime bleibt die erklärende Vorabprüfung aktiv. Die native LLama-Ladeprüfung benötigt weiterhin ein verfügbares lokales GGUF-Modell.
