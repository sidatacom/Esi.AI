# Session 20260829-120926

## User request
Run both a browser smoke test and focused integration tests for the shared backend model/configuration integration, while cleaning up duplicate LLama/OpenVINO controls.

## Action plan
- [completed] Remove duplicate model/profile controls and keep one shared backend toolbar.
- [completed] Add backend-aware local model filtering and focused DataService integration coverage.
- [completed] Run focused tests, Studio build, and browser smoke checks.
- [completed] Record final results and remaining limitations.

## Execution notes
- Removed the old LLama and OpenVINO model/profile controls from `Backends.razor`.
- Centralized model loading in the shared backend toolbar while retaining backend-specific stop/unload actions.
- Filtered local GGUF catalog entries to LLama, OpenVINO, and dotLLM; vLLM and SGLang remain available for manual Hugging Face/path input.
- Added `Esi.AI.Studio.Tests` coverage for catalog filtering and backend-specific profile persistence.

## Validation
- Focused integration tests: 2 passed, 0 failed.
- Core tests: 17 passed, 0 failed, 0 skipped in the selected run.
- Studio build: passed with no warnings.
- Browser smoke test on `http://127.0.0.1:7010/backends`: all four tabs switched successfully; one shared model picker, profile picker, and load button per tab; old picker IDs absent.
- Workspace diagnostics: no errors in modified source and test files.
- `git diff --check`: passed.

## Remaining limitations
- OpenVINO directory discovery and remote Hugging Face model registration are still separate catalog expansions.
- vLLM/SGLang worker transport remains internal HTTP; gRPC migration is not part of this validation pass.
