# Session log

## User request
Implement a solution that automatically prepares all prerequisites needed by vLLM and SGLang, analogous to the existing OpenVINO driver preparation, so the selected backend can be used.

## Action plan
- [x] Read applicable repository and C# instructions.
- [x] Trace Python backend startup, contracts, services, and tests.
- [x] Implement isolated per-backend environment provisioning and readiness validation.
- [x] Integrate status/error reporting without adding HTTP endpoints.
- [x] Add focused tests and documentation.
- [x] Build and run focused validation.

## Execution

- Added `PythonBackendProvisioner` to create and validate isolated backend environments.
- Added pinned `vllm-requirements.txt` and `sglang-requirements.txt` files.
- Updated `PythonInferenceServer` to prepare the selected environment before bridge startup.
- Added backend-specific environment overrides and corrected the Studio launch configuration.
- Added provisioning tests and updated `docs/vllm-grpc.md`.
- Validation: 32 Core tests passed/expected skipped, 9 Studio tests passed, Studio build succeeded, and both existing environments passed the import check.

## Summary

Default `python3` now prepares `~/.venvs/esi-ai-vllm` or `~/.venvs/esi-ai-sglang` on first backend load. Explicit Python executables are validated but not modified. OS-level NVIDIA drivers and CUDA remain host prerequisites.
