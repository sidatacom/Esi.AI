# Session Log

User request:
- OpenVINO model loading appears not to work.
- The UI log remains completely empty.

Initial hypothesis:
- The OpenVINO load path may be failing before its result is copied into `openVinoLoadLog`, or the UI may be reading a different status field than the loader updates.
- A focused disconfirming check is to inspect the OpenVINO button handler, `DataService.LoadModelAsync(OpenVinoLoadRequest)`, and `OpenVinoModelLoader` status/log updates.

Plan:
- [completed] Trace the OpenVINO load and log propagation path.
- [completed] Fix the missing load feedback for validation and transport failures.
- [completed] Build, test, and smoke-test the OpenVINO path.

Implementation:
- Keep the OpenVINO load output visible after loading when output exists.
- Show the server result message when the native loader returned no log entries.
- Capture client-side load exceptions in the visible OpenVINO load output.
- Surface the unsupported-accelerator early return in the load output.
- Select the first compatible OpenVINO accelerator after a configuration reset so the load action is enabled.
- Normalize saved and profile-based OpenVINO settings when they contain no enabled compatible device.
- Query the OpenVINO Intel GPU plugin's `GPU_MEMORY_STATISTICS` after a successful load.
- Surface the measured OpenVINO VRAM usage in the shared loaded-model summary and load output.

Validation:
- Studio build passed with `dotnet build src/Esi.AI/Esi.AI.Studio/Esi.AI.Studio.csproj --no-restore`.
- `BackendCatalogIntegrationTests`: 24/24 passed.
- OpenVINO core tests: 13/13 passed.
- Browser smoke test at `http://127.0.0.1:7010/backends`: the Qwen3.8-27B-int4-ov model loaded successfully on `GPU.1`.
- The UI showed `MODEL LOADED` and retained the load output through completion.
- The live OpenVINO load reported `15111.93 MiB` and the model summary displayed `14.76 GiB VRAM` on `GPU.1`.
- Focused validation after the VRAM change: 37/37 tests passed.
- After the final Razor change, a fresh browser load showed `Qwen3.8-27B-int4-ov | OpenVINO | 14.76 GiB VRAM`.
- The detailed device row showed `GPU.1 15111.93 MiB VRAM`; no OpenVINO `0 layers` text remained.

Outcome:
- The native OpenVINO load path is working in the current environment.
- The empty-log symptom was caused by the load action being disabled for an incomplete saved route, combined with insufficient visible output while the request was in progress.
- OpenVINO VRAM now comes from the runtime's GPU memory statistics, not from the model directory's disk size.
- OpenVINO no longer reuses LLama-only summary fields: the summary omits the device route and GPU-layer count, while the detailed device row retains the selected route.
- The shared loaded-model display is backend-neutral; LLama-only layer information is rendered only for LLama models.
- Remaining test gap: no dedicated automated test covers the Blazor OpenVINO output fallback or route normalization.
