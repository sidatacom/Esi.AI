# Session history

## User request
- Generalize `LlamaSettings` and `OpenVinoSettings` to `ModelSettings`.
- Rename `ModelConfigurationProfiles` to `ModelConfigurations`.
- Complete the related SignalR/DataService/client refactor and validate it.

## Action plan
- [in-progress] Define the shared `ModelSettings` contract and map existing backend persistence.
- [ ] Generalize model collection operations from `LlamaModel_*` to `Model_*`.
- [ ] Rename configuration profile contracts, services, hub operations, entities, and DbSet/table mapping.
- [ ] Add/update persistence migration and focused tests.
- [ ] Build, restart Studio, and validate the refactor in the browser.
- [ ] Record completion and remaining test gaps.
