# Session Log

User request context:
- The Backends page is very slow.
- Determine whether requirement checks are implemented as a background service.
- Requirement state should be pushed over SignalR at most once per second.

Initial hypothesis:
- Page initialization currently waits for multiple backend prerequisite checks, causing the slow load.
- A cheap disconfirming check is to inspect `InitializeAsync` and the requirement refresh method for awaited Python diagnostics and any hosted background service or throttled SignalR state publisher.

Action plan:
- [completed] Inspect current requirement-check lifecycle, SignalR flow, and hosted services.
- [completed] Move requirement polling/diagnostics to a background service with cached state and one-second update cadence.
- [completed] Keep Backends page rendering non-blocking and validate with build/tests/browser timing.

Implementation summary:
- Added `BackendRequirementMonitor` as a hosted singleton service.
- Added cached `BackendRequirementState` and `BackendRequirementSnapshot` DTOs.
- Diagnoses run out of the page lifecycle, one route at a time, on a 15-second cycle or after a requested refresh.
- SignalR broadcasts only changed states and throttles broadcasts to at most one per second.
- Added DataHub snapshot access and the dedicated client-side requirement event contract.
- Backends page now reads the cache during initialization and updates from SignalR; the matrix no longer starts four Python diagnostics during first render.

Validation:
- `dotnet build src/Esi.AI/Esi.AI.Studio/Esi.AI.Studio.csproj --no-restore` passed.
- Studio integration test file passed: 24 tests.
- Studio watchdog restarted and `/backends` loaded at `http://127.0.0.1:7010/backends`.
