# Review Session

User request: Read-only review of the current Esi.AI code, focused on LLama/OpenVINO separation in `Backends.razor`, `DataService.cs`, `Esi.AI.Models` contracts, and related tests. Identify concrete bugs or coupling that could make clicking Load model fail or route to the wrong backend. Recommend the smallest compatible fixes and exact tests/commands. Do not edit application files.

Plan:
- [complete] Read applicable repository and component instructions.
- [complete] Trace LLama and OpenVINO load handlers through `IDataService`, SignalR, `DataHub`, and `DataService`.
- [complete] Inspect model contracts, discovery/persistence, initialization, and chat backend selection.
- [complete] Inspect related tests and run focused build/test validation.
- [complete] Produce a read-only review with concrete findings and smallest compatible fixes.

Validation:
- `dotnet build /home/llm/Git/Esi.AI/src/Esi.AI/Esi.AI.Studio/Esi.AI.Studio.csproj` passed.
- `OpenVinoModelLoaderTests` passed: 12/12.
- Editor diagnostics for reviewed files: none.

Summary:
- Confirmed wrong-tab routing for saved OpenVINO vision or generic IR directories because `Backends.razor` recognizes only `openvino_language_model.xml`.
- Confirmed LLama/OpenVINO model paths are mirrored through shared `LlamaSettings`, and OpenVINO settings are not independently persisted.
- Confirmed OpenVINO discovery/picker is backed by GGUF-only `ScanLlamaModelsAsync`.
- Confirmed `DataHub` duplicates LLama load/unload logic instead of delegating through `DataService`.
- Confirmed no Studio/DataHub/DataService tests cover backend routing or the page load click path.