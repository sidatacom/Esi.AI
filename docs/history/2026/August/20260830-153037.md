# Session Log

User request:
- After adding a model to the download queue, disable the corresponding Hugging Face action while that HF ID remains in the queue.

Initial hypothesis:
- `Models.razor` already receives live download updates into `downloads`, but the model-row action does not consult that dictionary. A focused check is to inspect the row button markup and `DownloadAsync`/`HandleModelDownloadUpdatedAsync` state transitions.

Plan:
- [completed] Inspect the Hugging Face action markup and queue state.
- [completed] Add an HF-ID queue predicate and disable the corresponding action after enqueue.
- [in progress] Build and verify the Models page behavior.

Implementation:
- Track download requests immediately by HF model ID so the button disables without waiting for the first SignalR update.
- Use live `DownloadStatus` updates to keep the action disabled while the matching download is queued, active, paused, or otherwise incomplete.
- Remove the pending marker on completion, cancellation, or a failed start request.
- Load incomplete persisted downloads explicitly through `IDataService` and `DataHub` during Models page initialization, covering reconnects that miss the initial SignalR event.
- Remove a cancelled download locally after the server call succeeds, so the UI does not depend on delivery of the cancellation broadcast to update.
