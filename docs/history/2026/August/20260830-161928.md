# User request

Hide the runtime load-output section after a model has loaded successfully because the result is already shown above.

# Action plan

- [completed] Inspect the runtime log render condition.
- [completed] Render the log section only while a runtime is loading.
- [completed] Build the Studio project.

# Summary

Changed `Backends.razor` so the runtime load-output section is rendered only while a backend load is active. The loaded-model summary remains visible after success. The Studio build passed.