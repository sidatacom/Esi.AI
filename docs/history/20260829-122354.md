# Session 20260829-122354

## User request
Add one reference model for each backend so every backend can be tested.

## Action plan
- [complete] Define five verified reference model entries and setup instructions.
- [complete] Add optional backend reference integration tests.
- [complete] Run focused validation and update the session summary.

## Design checks
- Reference models are test fixtures, not new domain aggregates.
- Local paths are used for LLama, OpenVINO, and dotLLM; vLLM and SGLang use a Hugging Face model ID.
- No new controller, endpoint, or transport is introduced.

## Implementation summary
- Added the central model library workflow with separate `Meine Model Library` and `Hugging Face suchen` tabs.
- Added Hugging Face library filtering for GGUF, OpenVINO, and Transformers repositories.
- Added format-aware local model metadata so OpenVINO directories appear in the library without being synchronized as LLama/GGUF models.
- Kept downloads on the existing `IDataService -> SignalRDataService -> DataHub -> DataService` path.

## Validation
- Studio build: successful.
- Core tests: 17 successful, 7 skipped, 0 failed.
- Studio tests: 4 successful, 0 failed.
- Browser smoke test: both model tabs and the OpenVINO search flow verified on `http://127.0.0.1:7010`.
- Native backend reference tests remain environment-dependent and are inconclusive without the documented model paths and Python runtimes.

## UI download verification
- Selected `OpenVINO` in the Hugging Face library filter.
- Searched for `Qwen3` and confirmed OpenVINO results were returned.
- Started `OpenVINO/Qwen3-0.6B-int4-ov` through the visible `OpenVINO laden` action.
- Confirmed the transfer panel reported active progress, increasing from `5 KiB` to approximately `201.7 MiB` during observation.

## UI refinement
- Replaced the legacy `ACTIVE MODEL` card with a compact `RUNTIME STATUS` bar.
- The status now distinguishes no model, selected model, and loaded model states with format-independent wording.
- Kept backend configuration and chat actions available while removing the oversized empty panel.
- Verified the rebuilt page in the browser: the legacy `.active-model` element is gone and `.runtime-status` is rendered.
