# Sitzungsprotokoll

## Anfrage
EF-Core-Meldungen sollen über die `appsettings` im Debug-Log abgeschaltet werden.

## Aktionsplan
- [x] Vorhandene `appsettings`-Dateien und EF-Core-Loggingkonfiguration lokalisieren.
- [x] Die passende Debug-Konfiguration minimal anpassen.
- [x] JSON- und projektbezogene Validierung ausführen.

## Ergebnis
In `src/Esi.AI/Esi.AI.Studio/appsettings.Development.json` wurde
`Logging:LogLevel:Microsoft.EntityFrameworkCore` auf `None` gesetzt.
Die JSON-Prüfung mit `jq` war erfolgreich.