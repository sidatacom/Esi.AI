# User Request
Implement `Esi.AI.Backend.Llama.Vulkan` as a separate backend package, without modifying Studio host routing. Keep one unified backend interface for normalized chat/WebAPI generation. Ensure backend classes/projects use `Esi.AI.Backend.*` namespaces and rename `Esi.AI.BackendWorker` to `Esi.AI.Backend.Worker`.

# Action Plan
1. Trace existing LLama Vulkan runtime loading, chat generation, package assets, and host composition. Status: complete. Dependency: none.
2. Implement standalone Linux-x64 `Esi.AI.Backend.Llama.Vulkan` module using the unified `IBackendRuntime` interface. Status: complete. Dependency: action 1.
3. Rename the worker project, assembly, source folder, solution references, and entry-point namespace to `Esi.AI.Backend.Worker`. Status: complete. Dependency: user follow-up.
4. Add focused tests for module identity, service registration, variant checks, and invalid paths. Status: complete, 4 passed. Dependency: action 2.
5. Build, pack, inspect package assets/dependencies, and document remaining limitations. Status: complete. Dependency: actions 2-4.

## Summary

- Implemented and packed `Esi.AI.Backend.Llama.Vulkan` as a standalone Linux-x64 package using the shared `IBackendRuntime` contract; Studio host routing remains deliberately unchanged.
- The package contains the backend assembly and Linux `.so` runtime assets only. Its dependencies are LLamaSharp, `Esi.AI.Backend.Abstractions`, and `Esi.AI.Models`; it has no `Esi.AI.Core` dependency.
- Renamed the worker project and namespace to `Esi.AI.Backend.Worker` and updated Studio's project reference and sandbox assembly path.
- Updated `docs/backend-assemblies.md` with implemented and pending migration state.
- Validation: Vulkan backend build succeeded, Vulkan tests passed 4/4, worker build succeeded, Studio build succeeded with 19 dependency warnings, and NuGet pack/content inspection succeeded.
