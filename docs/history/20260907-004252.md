# Session

User request:
- Investigate whether repeated OpenVINO Core creation logs indicate duplicate model loading.
- Analyze three Core creation messages and an ArgumentException.

Action plan:
- Trace every OpenVINO Core construction path.
- Compare diagnostic calls, model pipeline loading, and VRAM inspection.
- Check active Studio processes.
- Report whether the model is loaded multiple times and identify the exception path.

Status:
- Core construction paths identified in diagnostics, pipeline internals, and VRAM inspection.
- No active Esi.AI.Studio process found during inspection.
- The model is not loaded three times. Two Core instances are diagnostic calls; the third is the VRAM property query after pipeline creation.
- The `ArgumentException` is likely raised by an unsupported `GPU_MEMORY_STATISTICS` property and is caught by the VRAM diagnostic path.

Summary:
- Repeated Core logs indicate repeated lightweight OpenVINO Core handles, not repeated model pipeline loads.
- The diagnostic Core is created by `OpenVinoDiagnosticsService.Diagnose`; the post-load Core is created by `OpenVinoModelLoader.TryGetVramUsageMiB`.
- Follow-up UI issue: local OpenVINO chat history now defaults to an explicit `high` reasoning template context when no effort was supplied, matching the provider path.
- Validation: OpenVINO loader tests passed 20/20 and the Studio build succeeded.
