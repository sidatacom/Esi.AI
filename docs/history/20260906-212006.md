# Session log

Request: Start a debug session, test virtual `readyness`, restart it, then stop it.

Results:
- Installed EsiMCP version: `1.0.28`.
- Started `Esi.AI.Studio` with `csdevkit.debug.projectDebugLaunch`.
- Active session: `7647c61d-e10d-461c-9ee4-b8871d2f591c`.
- Initial `csdevkit.debug.check.host.readyness`: `{ "ready": true }`.
- `csdevkit.debug.restart`: `{ "restarted": true }`.
- Same session ID remained active after restart.
- Readiness after restart: `{ "ready": true }`.
- `csdevkit.debug.stop`: `{ "stopped": true }`.
- Final active session: `null`.
- Port 7010 is free.

Summary: The complete virtual debug lifecycle passed with EsiMCP 1.0.28.
