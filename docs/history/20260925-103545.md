# Request
Continue migrating the packaged backends to the shared runtime interface and wire the available variants through Studio.

# Action Plan
- [x] Inspect current backend modules, host composition, persisted variant identity, generation paths, and XPU test status.
- [x] Propagate stable variant IDs through backend status and normalized chat requests.
- [x] Register all six packaged variants in Studio and inject the runtime resolver into ModelRuntime.
- [x] Route configured loads, runtime lifecycle, SignalR chat, and OpenAI-compatible generation through `IBackendRuntime` for packaged variants.
- [x] Add focused Core, Studio, and backend registration regression tests.
- [x] Run complete Core and Studio test projects; fix any regressions in the changed paths.
- [x] Review diagnostics/diff and finalize architecture/session documentation.

# Summary
Studio now registers the six available packaged backend modules. Persisted profile loading uses `BackendVariantId`, `ModelRuntime` routes load/status/unload/stop/generation through the shared runtime resolver, and both chat entry points use `IBackendRuntime.GenerateAsync` for packaged variants while retaining Core adapters for not-yet-extracted CPU LLama, SGLang, and dotLLM. Stable variant IDs are included in packaged runtime status DTOs and can be carried through API/SignalR unload requests. The six backend test projects passed (37 tests total); the full Core suite passed 80 tests with 9 skipped, and the full Studio suite passed 75 tests with 1 skipped. The final focused controller suite passed 31 tests with 1 skipped. Studio builds continue to report 19 pre-existing package-resolution/security warnings from Elsa dependencies.