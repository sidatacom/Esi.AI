# Sitzung 2026-09-04 13:14:06

## Benutzeranfrage
Prüfen, ob die bereitgestellte Bilddatei verwendet wird, und sie bei Verwendung nach `src/Esi.AI/Esi.AI.Studio.Tests` verschieben.

## Aktionsplan
- [x] Die bereitgestellte Datei und bestehende Testreferenzen prüfen.
- [x] Die Bilddatei in den Testprojektordner verschieben.
- [x] Zielpfad und Arbeitsbaum validieren.

## Ausführung
- `test-chat-with-picture.png` als PNG mit 495 x 240 Pixeln identifiziert.
- Die Datei wurde nach `src/Esi.AI/Esi.AI.Studio.Tests/test-chat-with-picture.png` verschoben.
- Der Multimodal-Test lädt jetzt genau diese PNG-Datei, kodiert sie als lokale Base64-`image_url` und prüft den erzeugten OpenVINO-Tensor.
- Das Testprojekt kopiert die PNG-Datei mit `CopyToOutputDirectory` in das Testausgabe-Verzeichnis.
- Ein zusätzlicher `OpenVINO.Integration`-Test ruft die laufende OpenAI-kompatible WebAPI über `/v1/models` und `/v1/chat/completions` auf und sendet das echte PNG im OpenAI-Content-Array.

## Ergebnis
Die Datei liegt unter `src/Esi.AI/Esi.AI.Studio.Tests/test-chat-with-picture.png`. Der alte Pfad ist frei. Der Tensor-Test besteht mit `1/1`. Der WebAPI-Integrationstest erreicht den Endpoint, schlägt aber bewusst bei der semantischen Prüfung fehl, weil das aktuelle VLM das Bild nicht korrekt beschreibt. Keine Diagnosen im Testfile.
