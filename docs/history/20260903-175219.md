# Session Log

## User Request
Make `maxInputTokens` and `maxOutputTokens` configurable in the Esi.AI Studio VS Code provider. The default should be 32K.

## Action Plan

- [in-progress] Verify the provider's current token settings and request path.
  - Dependency: provider source and package manifest.
  - Check: confirm input/output settings are configurable and identify current defaults.
- [not-started] Change the provider output-token default to 32768 while preserving input-token configurability.
  - Dependency: current provider settings confirmed.
  - Check: source and manifest expose both settings with 32768 defaults.
- [not-started] Increment, build, package, and install the provider version.
  - Dependency: provider change.
  - Check: build succeeds and the installed extension has the new version.
- [not-started] Validate the generated request configuration and update this log.
  - Dependency: installed provider.
  - Check: provider uses the configured model output limit and no new diagnostics remain.

## Status

The provider already exposes `esiAiStudio.maxInputTokens` with a default of 32768 and uses it when registering models. It exposes `esiAiStudio.maxOutputTokens`, but its default is currently 4096 and that value is used as the normal request `max_tokens` limit. The focused fix is to change the output default to 32768 and ship a new provider patch version.
