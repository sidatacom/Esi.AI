# Session Log

## User Request
Change the OpenAI-compatible API to expose configurations instead of raw models. Add an `AutoLaunch` option to configurations, defaulting to true. When an API request selects a configuration, load that configuration before inference. Keep the request alive with a heartbeat while loading.

## Action Plan
- [ ] Inspect model configuration contracts, catalog endpoint, API request resolution, and existing model lifecycle operations.
- [ ] Add persisted/default `AutoLaunch` configuration behavior and expose configurations through the OpenAI model contract.
- [ ] Resolve API requests to configurations and auto-launch the selected configuration before inference.
- [ ] Preserve/extend SSE heartbeat behavior during auto-launch and update trace layer messages.
- [ ] Add focused tests and validate build plus live API behavior.

## Execution

## Summary
