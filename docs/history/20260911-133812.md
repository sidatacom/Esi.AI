# Session Log

## Request
Keep failed model-load items visible instead of removing them automatically after a delay, and add a Clear button for the list.

## Plan
- Remove delayed failed-item cleanup from the Backends page.
- Add an explicit Clear action for failed optimistic load items.
- Validate the Razor component diagnostics and confirm no delayed cleanup path remains.

## Completed
- Removed the delayed cleanup task and its version state.
- Added a Clear button that removes only the retained failed load item.
- Failed load cards now show Clear instead of Unload.
- Confirmed no diagnostics in Backends.razor.
- Confirmed no delayed cleanup symbol or delay remains in Backends.razor.
