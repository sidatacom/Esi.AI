# Session

User request:
VS Code chat through Esi.AI Studio is failing again. Error: SSE stream terminated before completion; Esi.AI Studio request failed with Unknown error (-17). The user reports that a recent change broke a flow that had previously worked.

## Action plan
- [x] Inspect the provider SSE request/response path and recent changes.
- [x] Identify the concrete owner of the premature stream termination.
- [x] Reproduce with the narrowest available validation.
- [x] Apply the smallest root-cause fix.
- [x] Run focused validation and record results.

## Summary
The SSE failure was caused by OpenVINO tool-call parsing an emitted JSON array as if it were an object. `OpenAiToolCallParser` now accepts both a single JSON tool-call object and a non-empty array, and safely rejects non-object array elements. Added a regression test for the array format.

Validation:
- Focused parser tests: 3 passed, 0 failed.
- Editor diagnostics: no errors in changed files.
- C# Dev Kit Studio debug session: host readiness confirmed.
- Separate runtime issue observed in the supplied log: Intel GPU.1 reported `CL_OUT_OF_RESOURCES` for the selected 27B OpenVINO model. This is distinct from the parser fix and requires a compatible device/model configuration.
- Follow-up: `InferenceFailureCoordinator` now requests host shutdown before runtime cleanup and bounds cleanup to five seconds, so a native OpenVINO cleanup hang cannot suppress the shutdown request. Coordinator tests passed.
- Additional request: before loading an OpenVINO GPU model, the loader now reads `GPU_MEMORY_STATISTICS`, estimates the model directory/file footprint plus reserve, and rejects the load with a clear error when free memory is insufficient or telemetry is unavailable. Studio startup now owns a named `Esi.AI.Studio` mutex and exits before host construction when another instance already owns it.
- Additional validation: focused memory/cancellation/device tests passed (`5 passed, 0 failed`); changed files have no diagnostics. Hardware-dependent real-model tests were not runnable.
- Follow-up fix: the OpenVINO page was replacing the server's detailed load failure with the generic "The model could not be loaded." message. It now displays the returned exception/memory-admission message so rejected loads are visible to the user.
- Follow-up fix: when OpenVINO diagnostics disable the Load button before a request is sent, the page now shows the concrete unavailable checks next to the button instead of failing silently.
- Root-cause hardening: OpenVINO diagnostics no longer create or query a native `OpenVinoSharp.Core` in the Studio process. They use OS-level render-device checks and defer the native probe to the guarded model load, preserving the detailed load exception for the UI.
- Root-cause hardening: OpenVINO load, unload, and shutdown now share the same process-wide operation gate, preventing overlapping native lifecycle operations. Core project build and focused diagnostics/gate tests passed.
- Follow-up: the host exposed no `/sys/class/drm/*/device/mem_info_vram_*` telemetry, so the admission check was rejecting every model before OpenVINO. The check now records an explicit warning and delegates the final resource decision to the native load; native failures remain visible through the existing DTO/UI error path.
