# Session

## Request
Implement the next vLLM XPU performance optimization steps toward 80 tok/s.

## Plan
- Benchmark single-sequence MTP4 with XPU graphs enabled.
- Benchmark the identical configuration with eager execution.
- Compare decode-only throughput after warmup.
- Apply the better configuration to Studio only if validated.

## Status
- Started from existing MTP4/BF16 implementation and Studio graph/eager fix.
- Added an explicit `Optimize interactive` action for selected vLLM/SGLang profiles.
- The action persists `max_num_seqs=1`, `max_num_batched_tokens=8192`, prefix caching off, XPU graph on, BF16 MTP draft on, MTP4, and 88% GPU utilization.
- Razor diagnostics and the Studio project build passed.
- A direct graph/eager A/B attempt was invalid because both 27B engines were started concurrently and exhausted XPU memory; no throughput claim was made.
