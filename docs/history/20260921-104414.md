# Session log

User request: `/run-test-all`

Goal: Execute the complete Esi.AI Studio browser test plan in the prescribed navigation order, using the shared browser session, recording PASS/FAIL/BLOCKED outcomes, stopping on data loss, security issues, unhandled exceptions, or hangs, and cleaning up disposable artifacts.

## Action plan

- [x] Confirm Studio host readiness and browser reachability.
- [x] Read the individual test prompts and identify prerequisites and disposable-data constraints.
- [ ] Execute prompts 01 through 09 in order, preserving required browser state. Stopped early after a security issue.
- [x] Record exact failures, blocked prerequisites, and cleanup actions.
- [x] Produce the final report using the required table format.

## Execution notes

## Results

- Host build succeeded; C# Dev Kit session started and host readiness returned true.
- Overview rendered without alerts and showed the expected headings and empty-runtime state.
- Overview refresh returned to idle but exposed no observable busy or disabled state: FAIL for that check.
- Backends rendered the matrix, but the required Qwen2.5 Instruct reference artifacts were not present. Existing Qwen3.8 artifacts were not substituted: active backend rows BLOCKED.
- Navigation to `/chats` changed the URL while leaving Web API content visible during enhanced navigation. Direct `/chats` navigation rendered correctly: navigation-shell FAIL.
- Provider rendered its empty state without alerts. Model-dependent provider checks BLOCKED.
- `/webapi` rendered only the API documentation link and no API configuration controls: configuration checks FAIL.
- `/settings` exposed a sensitive Hugging Face token as a populated password input value in the page DOM. The test run was stopped immediately as required. The value was not copied into this log or final report.
- Studio debug session stopped cleanly; no process remained on port 7010.

## Cleanup

- No models, downloads, settings, chats, or other test artifacts were created or deleted.
- No credentials, tokens, cookies, request bodies, or personal data were recorded.

## Final status

STOPPED due to a security issue. Checks after `/settings` were not executed and must not be considered PASS.
