# Session Log

## User Request
Restore VS Code Copilot Chat against the Esi.AI OpenAI-compatible OpenVINO backend. Preserve temperature handling, remove the provider-side fixed temperature, and restore Qwen tool-calling behavior. The user explicitly requested a root-cause repair and allowed stepping back one or two commits if needed.

## Action Plan
- [completed] Compare the current controller behavior with the direct parent commit.
- [completed] Remove the locally introduced hard rejection of tool requests.
- [completed] Restore truthful model capability reporting.
- [completed] Update the focused regression test to match restored behavior.
- [completed] Run focused controller tests.
- [completed] Restart the supervised Studio debug session and verify host readiness.
- [completed] Verify whether the OpenVINO adapter translates structured tool requests and responses.
- [completed] Implement and test the smallest root-cause fix for OpenVINO tool calling if the adapter is incomplete.
- [completed] Run final focused validation and summarize residual limitations.

## Findings
- Commit `dbb455d` introduced the explicit local tool-call rejection.
- The controller currently forwards only text prompts to OpenVINO and emits text-only OpenAI response deltas.
- The direct parent behavior no longer rejects tool requests but does not yet provide structured tool-call translation.
- Focused controller tests pass after removing the rejection.
- The supervised Studio host restarted successfully and reported ready.
- OpenVINO now receives native tool definitions and Qwen XML/JSON tool calls are projected to OpenAI `tool_calls` responses.
- The remaining multi-turn failure was caused by OpenAI `assistant` tool-call messages using `content: null`; native OpenVINO history requires an explicit empty content string. Tool-call arguments are also normalized from the OpenAI string representation to the Qwen template's object representation.
- Focused normalization test passed, Studio build passed, and the supervised host restarted successfully.
- Live non-streaming and SSE multi-turn requests with assistant tool-call history and a tool response both passed against `Qwen3.8-27B-Uncensored-int4-awq-g128-ov` on `GPU.1`.
- Process correction: an earlier Core build was run while the Studio debug session was still active. Subsequent lifecycle operations stopped the session, verified port 7010 was free, built, and restarted only through the supervised configuration.

## Summary
- Restored truthful Qwen tool-calling end to end, including native tool metadata, parser output, OpenAI response projection, and multi-turn history normalization.
- Preserved OpenAI-compatible argument strings at the API boundary while supplying Qwen's required object arguments internally.
- Verified focused tests, clean diagnostics, successful Studio build, supervised startup, and live non-streaming/SSE multi-turn requests.
