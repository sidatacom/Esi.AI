# Sitzungsprotokoll

## Anfrage
Der Chat mit Esi.AI Studio funktioniert nicht. VS Code meldet, dass Esi.AI Studio unter `http://127.0.0.1:7010/v1` nicht erreichbar ist. Zu prüfen ist, ob eine vorherige Änderung den Start beschädigt hat.

## Aktionsplan
- [x] Prüfen, ob Port 7010 und der Studio-Prozess aktiv sind.
- [x] Lokalen Start-/Buildpfad und relevante Änderungen untersuchen.
- [x] Ursache gezielt beheben oder den fehlenden Startschritt ausführen.
- [x] `/v1/models` und den Chatpfad fokussiert validieren.
- [x] Ergebnis und verbleibende Risiken zusammenfassen.

## Befund bisher
- Zeitpunkt: 2026-09-11 10:28:29.
- Beim ersten Check lauschte Port 7010 nicht; Esi.AI Studio war nicht aktiv.
- Nach dem Neustart lauscht `Esi.AI.Studio` auf `127.0.0.1:7010`.
- `curl http://127.0.0.1:7010/v1/models` antwortet danach mit HTTP 200 und einer Modellliste.
- In den verfügbaren VS-Code-/C#-Logs wurde kein `Unhandled`, Crash-, Signal- oder Core-Dump-Hinweis gefunden.
- Arbeitsbaum enthält eine bereits vorhandene Änderung an `src/Esi.AI/Esi.AI.Studio/Data/app.db`; diese wird nicht verändert.

## Zusammenfassung
Der gemeldete Providerfehler war ein Erreichbarkeitsfehler, weil der Studio-Host zum Zeitpunkt der Anfrage nicht lief. Es gibt keinen Beleg dafür, dass eine Codeänderung den Chat zerstört hat. Die konkrete Ursache für das Beenden des Hostprozesses ist mangels Crash-Log nicht nachweisbar; nach dem Neustart ist der Host erreichbar.
