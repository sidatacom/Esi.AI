# Session log

## User request

The Backends page no longer updates model status after models are loaded and requires a full page reload. The server-side status monitoring is acceptable; restore reliable event-driven client updates without introducing polling or UI flicker.

## Action plan

- [completed] Trace the Backends page status event subscription and initialization read.
- [completed] Identify the race where an event can be overwritten by an in-flight initialization read.
- [completed] Increment the status version when LoadedModel events arrive.
- [completed] Remove the duplicate OpenVINO status read that could overwrite event state.
- [completed] Build and validate the Studio project.
- [completed] Start the approved watched Studio debug session and verify the live loading status in the browser.
- [completed] Stop the Studio debug session and watchdog.

## Summary

Updated `src/Esi.AI/Esi.AI.Studio.Client/Pages/Backends.razor` so incoming `LoadedModel_Create`, `LoadedModel_Update`, and `LoadedModel_Delete` events increment `loadedModelStatusVersion`. The initialization read now cannot replace a newer event payload. Removed the duplicate successful OpenVINO `LoadedModel_ReadAsync` call.

Validation: the Studio project built successfully, the Razor file reported no errors, `git diff --check` passed, and the live Backends page immediately rendered `LOADING MODEL` with the pending model after clicking Load model without a full-page reload. The debug session and watchdog were stopped afterward.
