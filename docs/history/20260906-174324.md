# Session 20260906-174324

## User request
Update the VS Code debug workflow so agents may use only `csharp_devkit_*` tools for C# Dev Kit/debug actions and `vscode_terminal_*` tools for terminal actions.

## Plan
- Inspect customization rules and affected workspace references.
- Update the vscode-debug skill and any agent tool restrictions/documentation that still use legacy MCP-prefixed names or omit the allowed terminal namespace.
- Validate that only the requested public tool families are referenced for these operations.

## Completed
- Added an explicit allowed-tool policy to `.github/skills/vscode-debug/SKILL.md`.
- Replaced legacy C# Dev Kit MCP-prefixed names in the debug skill.
- Updated the Esi.AI Studio C# Dev Kit instruction with the approved C# and terminal tool families.
- Added `esimcp/vscode_terminal_list_commands` and `esimcp/vscode_terminal_execute_command` to the five model test-agent definitions.
- Validated legacy-name search and `git diff --check`.

## Follow-up verification
- No active `.github` workflow uses `vscode_debug_*` as an allowed agent tool.
- `vscode_debug_*` remains in the EsiMCP implementation, tests, README/EsiMCP documentation, and generated bundle because the generic public debug wrapper still exists.
- Older `vscode_debug_*` and `mcp_esimcp_debug_*` references in `docs/history/` are historical records.
