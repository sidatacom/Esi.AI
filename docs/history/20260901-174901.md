# Session Log

## User request

Show that Qwen can be used from Visual Studio Code through the Esi.AI Studio provider with OpenVINO.

## Verification

- Started `Esi.AI Studio (Server Debug)` on `http://localhost:7010`.
- Selected OpenVINO with device route `GPU.1`.
- Loaded `Qwen3.8-27B-Uncensored-int4-awq-g128-ov` successfully.
- `GET /v1/models` returned model id `Qwen3`.
- `POST /v1/chat/completions` with `stream: true` returned HTTP 200 and `text/event-stream`.
- The response contained streamed content, a stop chunk, and `data: [DONE]`.

## Result

Qwen is running through OpenVINO and the Esi.AI Studio OpenAI-compatible streaming API used by the VS Code provider. The Studio debug session remains running for interactive VS Code validation.