# Sitzungsprotokoll

## Benutzeranfrage
Die neue EsiMCP-Debug-Console-Diagnose soll sauber in die Debug-Anleitung integriert werden, damit der Copilot Design Agent sie verwendet.

## Status
Initialisierung abgeschlossen.

## Aktionsplan
- [x] Bestehende Debug-Anleitungen und den EsiMCP-Diagnosevertrag prüfen.
- [x] `output` und `lastLine` in die Debug-/Hot-Reload-Anweisungen integrieren.
- [x] Die neuen Commands und Felder gezielt in allen betroffenen Dateien verifizieren.

## Umsetzung
- `.github/skills/vscode-debug/SKILL.md` beschreibt nun `csdevkit.debug.output.diagnostics` als verbindliche Diagnose nach Hot Reload, Restart oder fehlgeschlagenem Browsercheck.
- Die Anleitung dokumentiert den aktuellen Rückgabevertrag mit `output` und `lastLine` sowie das Verhalten `null` bei fehlender Session.
- `.github/instructions/esi-ai-studio-csharp-devkit.instructions.md` und `.github/copilot-instructions.md` enthalten dieselbe Kurzregel für den Copilot-/Design-Agenten.
- Eine gezielte Textprüfung bestätigt die Commands und Felder in allen drei Dateien.

## Zusammenfassung
Der Copilot Design Agent soll künftig die gepufferte Debug-Console über `csdevkit.debug.output.diagnostics` auslesen, `lastLine` für die letzte Zeile verwenden und `output` nur für Kontext heranziehen. `showHotReloadPanel` bleibt eine interaktive UI-Aktion und wird nicht mehr als Textquelle behandelt.
