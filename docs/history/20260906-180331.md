# Session 20260906-180331

## User request
Test the new virtual readiness, stop, and restart commands by starting a debug session, restarting it, and stopping it. Then fix the readiness race: the readiness signal may be emitted in a fast-starting active `dotnet:` terminal before `csdevkit.debug.check.host.readyness` is invoked; inspect active terminals whose names start with `dotnet:` only.

## Action plan
- [ ] Inspect the current terminal output/readiness implementation and existing tests.
- [ ] Implement active `dotnet:` terminal readiness handling without including inactive terminals.
- [ ] Add focused regression coverage for the fast-start readiness race.
- [ ] Run focused and full EsiMCP tests plus formatting/type checks.
- [ ] Summarize the result and remaining live lifecycle validation.
