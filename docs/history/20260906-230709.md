# Session log

## User request
Behebe den Esi.AI Studio Provider-Fehler aus dem Copilot-Aufruf: `The selected Esi.AI Studio model does not support input content type 'stateful_marker'.` Danach den echten Copilot-Provider-Pfad validieren.

## Action plan
- [completed] Provider-Konvertierung und installierte Bundle-Version prüfen.
- [completed] Marker-Konvertierung minimal korrigieren und Provider-Version synchronisieren.
- [completed] Bundle/VSIX bauen und Extension installieren.
- [in progress] Extension Host neu laden und echten Copilot-Aufruf validieren.
- [ ] Ergebnis und verbleibende Risiken dokumentieren.

## Validation
- `npm run build` succeeded for provider `0.1.30`.
- `scripts/install.sh` packaged and installed `vscode-esi-ai-studio-0.1.30.vsix`.
- `code --list-extensions --show-versions` reports `sidatacom.vscode-esi-ai-studio@0.1.30`.
