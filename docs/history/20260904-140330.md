# Session 20260904-140330

## User request
Continue fixing vision support in the Esi.AI Studio WebAPI, with priority on the unresolved OpenVINO image-generation failure `UNKNOWN_EXCEPTION (-17)`.

## Constraints
- Keep the single `OpenAiCompatibleController` architecture and existing `/v1` contract.
- Use the existing `DataService`/`ModelRuntime` lifecycle path.
- Do not implement chat image UI until WebAPI image generation is proven.
- Start and stop Esi.AI Studio only through the supervised VS Code debug/watchdog workflow.

## Action plan
1. Locate the exact OpenVINO chat-generation call, tensor lifetime, and native wrapper API.
2. Reproduce the smallest failing image request and collect native/runtime/model diagnostics.
3. Compare the implementation with the current OpenVINO C# wrapper examples and identify the root cause.
4. Apply the smallest runtime fix with a focused regression test.
5. Build, run focused tests, and perform a supervised live WebAPI PNG request.
6. Update this history entry with results and stop the supervised host/watchdog.

## Tracking
- [ ] Locate and reproduce the failure.
- [ ] Identify the root cause.
- [ ] Implement the focused fix and test.
- [ ] Validate build, tests, and live WebAPI image generation.
- [ ] Stop Studio and watchdog cleanly.
