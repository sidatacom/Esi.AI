# Session log

User request: Embed Elsa's workflow editor in the Esi.AI Flow page, keep Esi.AI as the owner of persistence/routing, and make edits persist losslessly while maintaining the Esi.AI routing projection.

Plan:
- Preserve the full Elsa flowchart root in each Esi.AI DefinitionJson payload.
- Maintain the existing `nodes` projection consumed by FlowRoutingService.
- Add a focused MSTest regression covering activities, connections, designer metadata, and route target projection.
- Build and run the focused test, then validate the Studio build if lifecycle constraints allow.

Status:
- In progress.
