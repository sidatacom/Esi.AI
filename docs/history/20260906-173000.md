# EsiMCP Dotted Debug and Terminal Commands

## User request

Add the virtual C# Dev Kit command `csdevkit.debug.active.session`, using the same handler as the Debug command area.

Rename Debug and Terminal command IDs to use dots instead of underscores for consistency, while keeping the four public VS Code list/execute tool names unchanged.

## Action plan

- [in-progress] Update command catalogs and shared active-session handler.
- [pending] Add the virtual C# Dev Kit command and route execution to the shared DebugManager handler.
- [pending] Update tests and command references.
- [pending] Run focused tests, bundle generation, and diff checks.
- [pending] Record validation results and final summary.

## Constraints

- Keep public tools `vscode_debug_list_commands`, `vscode_debug_execute_command`, `vscode_terminal_list_commands`, and `vscode_terminal_execute_command` unchanged.
- Preserve existing command behavior and unrelated worktree changes.
