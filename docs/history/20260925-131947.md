# Session Request

User request: behebe die migration probleme (fix the migration problems).

# Action Plan

1. Identify the failing migration command, project, and exact error from the current workspace and available terminal context. Status: complete (EF reported model changes after a successful Studio build).
2. Trace the failing migration path to its owning project/configuration or migration code and form a specific root-cause hypothesis. Status: complete (entity nullability contradicted the nullable migration and snapshot).
3. Apply the smallest migration-focused correction, preserving unrelated user changes. Status: complete (made the persisted variant ID nullable to match legacy rows and schema).
4. Run the narrowest relevant migration/build validation and any focused tests available. Status: complete (EF build succeeded; no pending model changes; migration discovery and SQL script generation succeeded).
5. Record the outcome and any remaining limitation. Status: complete.

Dependencies: identify the actual failing command/error before editing; validate the same affected project after the correction.

# Summary

The Studio EF model had drifted from its migration snapshot: `ModelConfigurationEntity.BackendVariantId` was non-nullable in C# while the new migration intentionally adds it as nullable for existing rows. Changed the entity property to `string?`; `DataService.ToConfiguration` already resolves null/blank values to a backend variant, preserving compatibility for existing records.

Validation:
- `dotnet ef migrations has-pending-model-changes` built Studio successfully and reported no model changes since the last migration.
- `dotnet ef migrations list --no-connect` discovered `20260925110000_AddBackendVariantId`.
- `dotnet ef migrations script 20260921111504_AddFlowDefinitions 20260925110000_AddBackendVariantId --no-build` generated the expected nullable `BackendVariantId` column operation.
- The existing `Data/app.db` was inspected read-only and not modified; it is still at `20260921111504_AddFlowDefinitions`, so the new migration remains to be applied by the normal startup migration flow.
