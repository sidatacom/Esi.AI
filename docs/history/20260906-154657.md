# Session 20260906-154657

## User request
EsiMCP zuerst erweitern, damit der Debug-Lifecycle die C#-Dev-Kit-Konfiguration wirklich verwendet und `debug_start`, `debug_restart` sowie `debug_stop` eine verwaltete Session steuern.

## Initial findings
- `.vscode/launch.json` contains `Esi.AI Studio (Server Debug)` with `type: coreclr` and `dotnet watch` arguments.
- Build and `dotnet watch` work, but EsiMCP `debug_start` previously returned `false` and no active debug session was reported.
- EsiMCP source is under `src/vscode/vscode-esi-mcp/src/debug` and `src/vscode/vscode-esi-mcp/src/mcp/tools`.

## Action plan
- [completed] Inspect EsiMCP debug manager, MCP tools, and tests.
- [completed] Add the EsiMCP C# Dev Kit command area and command allowlist.
- [completed] Add focused tests and validate the EsiMCP extension test suite.
- [completed] Verify Microsoft guidance for dynamic in-memory C# Dev Kit launch configurations.
- [completed] Add workspace instructions for C# Dev Kit debugging and Hot Reload.
- [completed] Remove active references to the obsolete `launch.json`/`tasks.json` workflow.

## Summary
- Added `.github/instructions/esi-ai-studio-csharp-devkit.instructions.md` with the C# Dev Kit and Hot Reload rules.
- Updated `.github/copilot-instructions.md` and `.github/skills/vscode-debug/SKILL.md` to use `csdevkit.debug.projectDebugLaunch` and `csdevkit.debug.hotReload`.
- Removed the obsolete `esimcp.debugConfigurationName` workspace setting.
- Updated `docs/studio-development.md` and `README.md` to document dynamic C# Dev Kit configurations.
- Validation passed: workspace JSON, instruction frontmatter, diagnostics, and `git diff --check`.

## Remaining limitation
- EsiMCP's C# Dev Kit command tools are present in the source tree, but installing/reloading the packaged VSIX and running an end-to-end command invocation requires an Extension Host reload.
