# Session

## User request
Test the new models with LLama using CUDA 12 and SYCL.

## Action plan
- [ ] Inspect the current monitored Studio/debug state and available model/backend controls.
- [ ] Start exactly one monitored Studio session and verify host readiness.
- [x] Load a new local GGUF model through LLama with CUDA 12 and record the result.
- [x] Restart the monitored Studio session for the SYCL route and record the result or prerequisite blocker.
- [x] Stop the Studio session and watchdog cleanly.
- [x] Summarize runtime results and remaining blockers.
- [ ] Add LLama CUDA 12 and SYCL 16 rows to the GPU compatibility matrix.

## Test notes
- CUDA 12: `Qwen3.8-27B-GSQ-RCO-IQ3_XXS.gguf` loaded successfully through LLama at 32k context with 20 GPU layers. Runtime status reported `LLama | CUDA | 32768 tokens | 9.39 GiB | 20 GPU layers`; requirements were ready.
- SYCL 16: Requirements are not met. The output has no `runtimes/linux-x64/native/sycl` directory or SYCL native libraries, and `icpx`, `sycl-ls`, and `ze_info` are absent. The loader nevertheless reported `LLama | SYCL | 32768 tokens | 9.39 GiB | 20 GPU layers`, but only exposed a CPU model buffer and no SYCL device, so this is not confirmed Intel Arc GPU execution and indicates the load gate/status path needs tightening.
