# Session Log

## User Request
A simple `Hallo` chat message immediately caused an OpenVINO exception.

## Initial Hypothesis
The selected Qwen3.8 OpenVINO model is detected as a vision-language model and the server routes every text-only chat request through `VLMPipeline.GenerateWithHistoryCore`, which fails with OpenVINO error -17. The discriminating check is the OpenVINO session's pipeline selection and text-only generation path.

## Action Plan
- [x] Inspect OpenVINO model loading and chat generation routing.
- [x] Confirm the exact failing call and whether text-only requests can use a non-VLM path.
- [x] Implement the smallest root-cause fix while preserving vision support.
- [x] Validate the existing native generation regression path with focused tests.
- [x] Build, test, and exercise the chat endpoint with `Hallo`.

## Status
`OpenVinoChatSession` now routes text-only VLM requests through `VLMPipeline.Generate(prompt)` rather than `GenerateWithHistory`. The Studio build succeeded, 18 focused tests passed, and both non-streaming and streaming requests against `Qwen3.8-27B-int4-ov` completed successfully.
