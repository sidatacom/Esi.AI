# Sitzungsbericht

## Benutzeranfrage
Systematische Validierung und Abschluss der OpenVINO-/LLama-Trennung in Esi.AI: OpenVINO Qwen3.8 laden, Tokenmetriken anzeigen, irreführenden Load-Fehler beheben, SignalR-only-Anwendungsaktionen sicherstellen, LLama- und OpenVINO-Pfade getrennt testen und keine vollständige Erfolgsmeldung geben, bevor Hoststart, Laden, Chat, Persistenz und relevante Grenzen geprüft sind.

## Aktionsplan

| Status | Aufgabe | Abhängigkeiten / Check |
|---|---|---|
| [x] | Core-Tests und Lösungsbuild ausführen | Vorhandene Testprojekte und Restore müssen verfügbar sein |
| [x] | Host mit expliziter kompatibler OpenVINO-Runtime starten | Lokale 2026.4-Runtime muss vorhanden sein |
| [x] | Hauptseiten und Formfreiheit prüfen | Host muss erreichbar sein; keine Form-/Submit-Elemente |
| [x] | OpenVINO-Diagnose und Qwen3-Modell auf GPU.1 laden | GPU.1 muss diagnostisch bereit sein |
| [x] | OpenVINO-Chat, Tokenmetriken und Persistenz prüfen | Modell muss geladen und SignalR-Verbindung aktiv sein |
| [x] | LLama-GGUF laden und Chat prüfen | Lokales GGUF geladen; native CPU-Inferenz und Unload erfolgreich |
| [x] | Normalstart ohne OPENVINO_RUNTIME_DIR stabil reproduzieren | Host erreichte `Application started` ohne gesetzte Runtime-Variable |
| [x] | Abschlussprüfung und Bericht vervollständigen | Browser-, Test- und Build-Prüfungen abgeschlossen |

## Ausführung

- Core-Tests: 18 gesamt, 16 erfolgreich, 0 fehlgeschlagen, 2 übersprungen.
- Gesamtlösung: erfolgreich gebaut.
- Expliziter Hoststart mit lokaler OpenVINO-2026.4-Runtime: erfolgreich auf `http://127.0.0.1:5207`.
- Seiten `/`, `/backends`, `/models`, `/webapi`, `/chats`: ohne unhandled error; Formulare 0; Submit-Buttons 0.
- OpenVINO Qwen3 geladen: erfolgreich auf `GPU.1`; Status `OpenVINO model loaded on GPU.1.`.
- Chat über SignalR: erfolgreich; Antwort erzeugt und gespeichert; UI zeigte `39 tokens · 28.6 tok/s`.
- Normalstart ohne `OPENVINO_RUNTIME_DIR`: erneuter Start auf `http://127.0.0.1:5208` erreichte `Application started`; der vorherige Exit 134 konnte damit nicht reproduziert werden.
- Lokales GGUF gefunden: `/home/llm/.cache/esi-ai/models/SmolLM2-135M-BF16.gguf`.
- Studio-Host ohne `OPENVINO_RUNTIME_DIR`: erfolgreich auf `http://127.0.0.1:5211` gestartet; `Application started` erreicht.
- LLama native load: `SmolLM2-135M-BF16.gguf` erfolgreich mit CPU, 0 GPU-Layern und 32768 Kontext geladen; alle AVX2-Nativbibliotheken wurden gefunden.
- LLama-Chat: erfolgreich über SignalR; Antwort `LLAMA_OK`, Status `18 tokens · 86.9 tok/s`, Persistenz im Chatarchiv bestätigt.
- LLama-Unload: erfolgreich; Status `NO MODEL LOADED` und `Unloaded: SmolLM2-135M-BF16.gguf`.
- OpenVINO-Regressionsprüfung nach Lifecycle-Fix: Qwen3.8 erfolgreich auf `GPU.1` geladen; Chatantwort `OPENVINO_OK`, Status `46 tokens · 29.3 tok/s`.
- Alle geprüften Browserseiten bleiben formularfrei: 0 Formulare und 0 Submit-Buttons.
- Nach dem neuen Chat-Fix wird die Modellselektion beim Erzeugen eines Chats explizit synchronisiert.
- Abschlusskommandos: `dotnet test src/Esi.AI/Esi.AI.Core.Tests/Esi.AI.Core.Tests.csproj --no-restore` erfolgreich (18 gesamt, 16 erfolgreich, 2 übersprungen); `dotnet build Esi.AI.sln --no-restore` erfolgreich.

## Offene Punkte

Keine offenen technischen Prüfungen.

## Zusammenfassung

Die LLama- und OpenVINO-Pfade sind getrennt validiert. Beide Backends laden und antworten über die zentrale SignalR-Verbindung, zeigen Tokenmetriken, persistieren Chatnachrichten und unterstützen den vorgesehenen Lade-/Unload-Ablauf. Die Browseranwendung verwendet bei den geprüften Seiten keine Formulare oder Submit-Handler. Der Testhost wurde nach der Validierung beendet.

Sitzungslog: `Copilot-Processing/20260828-223019.md`

Bitte den Bericht kurz prüfen und die Sitzungsdatei anschließend entfernen, damit sie nicht ins Repository gelangt.
