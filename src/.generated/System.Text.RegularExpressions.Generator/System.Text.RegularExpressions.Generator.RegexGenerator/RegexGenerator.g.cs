// <auto-generated/>
#nullable enable
#pragma warning disable CS0162 // Unreachable code
#pragma warning disable CS0164 // Unreferenced label
#pragma warning disable CS0219 // Variable assigned but never used

namespace Microsoft.FluentUI.AspNetCore.Components
{
    partial class FluentColorPickerInput
    {
        /// <remarks>
        /// Pattern:<br/>
        /// <code>^#(?:[0-9a-fA-F]{3}|[0-9a-fA-F]{6})$</code><br/>
        /// Explanation:<br/>
        /// <code>
        /// ○ Match if at the beginning of the string.<br/>
        /// ○ Match '#'.<br/>
        /// ○ Match with 2 alternative expressions.<br/>
        ///     ○ Match a character in the set [0-9A-Fa-f] exactly 3 times.<br/>
        ///     ○ Match a character in the set [0-9A-Fa-f] exactly 6 times.<br/>
        /// ○ Match if at the end of the string or if before an ending newline.<br/>
        /// </code>
        /// </remarks>
        [global::System.CodeDom.Compiler.GeneratedCodeAttribute("System.Text.RegularExpressions.Generator", "8.0.14.36720")]
        private static partial global::System.Text.RegularExpressions.Regex HexColorRegex() => global::System.Text.RegularExpressions.Generated.HexColorRegex_0.Instance;
    }
}

namespace Microsoft.FluentUI.AspNetCore.Components.Utilities
{
    partial struct CssBuilder
    {
        /// <remarks>
        /// Pattern:<br/>
        /// <code>^-?[_a-zA-Z]+[_a-zA-Z0-9-]*$</code><br/>
        /// Options:<br/>
        /// <code>RegexOptions.Compiled</code><br/>
        /// Explanation:<br/>
        /// <code>
        /// ○ Match if at the beginning of the string.<br/>
        /// ○ Match '-' atomically, optionally.<br/>
        /// ○ Match a character in the set [A-Z_a-z] greedily at least once.<br/>
        /// ○ Match a character in the set [-0-9A-Z_a-z] atomically any number of times.<br/>
        /// ○ Match if at the end of the string or if before an ending newline.<br/>
        /// </code>
        /// </remarks>
        [global::System.CodeDom.Compiler.GeneratedCodeAttribute("System.Text.RegularExpressions.Generator", "8.0.14.36720")]
        private static partial global::System.Text.RegularExpressions.Regex GenerateValidClassNameRegex() => global::System.Text.RegularExpressions.Generated.GenerateValidClassNameRegex_1.Instance;
    }
}

namespace Microsoft.FluentUI.AspNetCore.Components.Utilities
{
    partial struct MarkupStringSanitized
    {
        /// <remarks>
        /// Pattern:<br/>
        /// <code>[^a-zA-Z0-9\\-.:_]</code><br/>
        /// Explanation:<br/>
        /// <code>
        /// ○ Match a character in the set [^-.0-:A-Z_a-z].<br/>
        /// </code>
        /// </remarks>
        [global::System.CodeDom.Compiler.GeneratedCodeAttribute("System.Text.RegularExpressions.Generator", "8.0.14.36720")]
        private static partial global::System.Text.RegularExpressions.Regex TagNameSanitizerRegex() => global::System.Text.RegularExpressions.Generated.TagNameSanitizerRegex_2.Instance;
    }
}

namespace Microsoft.FluentUI.AspNetCore.Components.Utilities
{
    partial struct MarkupStringSanitized
    {
        /// <remarks>
        /// Pattern:<br/>
        /// <code>^(?!.*(behavior\\s*:|expression\\s*\\(|-moz-binding\\s*:|javascript\\s*:|url\\s*\\(\\s*['"]?\\s*javascript\\s*:))[a-zA-Z0-9\\s,.:;_%\\-+()#{}'"pxemremvhsmsdegradturn]+$</code><br/>
        /// Options:<br/>
        /// <code>RegexOptions.IgnoreCase | RegexOptions.ExplicitCapture</code><br/>
        /// Explanation:<br/>
        /// <code>
        /// ○ Match if at the beginning of the string.<br/>
        /// ○ Zero-width negative lookahead.<br/>
        ///     ○ Match a character other than '\n' greedily any number of times.<br/>
        ///     ○ Match with 5 alternative expressions, atomically.<br/>
        ///         ○ Match a sequence of expressions.<br/>
        ///             ○ Match a character in the set [Bb].<br/>
        ///             ○ Match a character in the set [Ee].<br/>
        ///             ○ Match a character in the set [Hh].<br/>
        ///             ○ Match a character in the set [Aa].<br/>
        ///             ○ Match a character in the set [Vv].<br/>
        ///             ○ Match a character in the set [Ii].<br/>
        ///             ○ Match a character in the set [Oo].<br/>
        ///             ○ Match a character in the set [Rr].<br/>
        ///             ○ Match a whitespace character atomically any number of times.<br/>
        ///             ○ Match ':'.<br/>
        ///         ○ Match a sequence of expressions.<br/>
        ///             ○ Match a character in the set [Ee].<br/>
        ///             ○ Match a character in the set [Xx].<br/>
        ///             ○ Match a character in the set [Pp].<br/>
        ///             ○ Match a character in the set [Rr].<br/>
        ///             ○ Match a character in the set [Ee].<br/>
        ///             ○ Match a character in the set [Ss] exactly 2 times.<br/>
        ///             ○ Match a character in the set [Ii].<br/>
        ///             ○ Match a character in the set [Oo].<br/>
        ///             ○ Match a character in the set [Nn].<br/>
        ///             ○ Match a whitespace character atomically any number of times.<br/>
        ///             ○ Match '('.<br/>
        ///         ○ Match a sequence of expressions.<br/>
        ///             ○ Match '-'.<br/>
        ///             ○ Match a character in the set [Mm].<br/>
        ///             ○ Match a character in the set [Oo].<br/>
        ///             ○ Match a character in the set [Zz].<br/>
        ///             ○ Match '-'.<br/>
        ///             ○ Match a character in the set [Bb].<br/>
        ///             ○ Match a character in the set [Ii].<br/>
        ///             ○ Match a character in the set [Nn].<br/>
        ///             ○ Match a character in the set [Dd].<br/>
        ///             ○ Match a character in the set [Ii].<br/>
        ///             ○ Match a character in the set [Nn].<br/>
        ///             ○ Match a character in the set [Gg].<br/>
        ///             ○ Match a whitespace character atomically any number of times.<br/>
        ///             ○ Match ':'.<br/>
        ///         ○ Match a sequence of expressions.<br/>
        ///             ○ Match a character in the set [Jj].<br/>
        ///             ○ Match a character in the set [Aa].<br/>
        ///             ○ Match a character in the set [Vv].<br/>
        ///             ○ Match a character in the set [Aa].<br/>
        ///             ○ Match a character in the set [Ss].<br/>
        ///             ○ Match a character in the set [Cc].<br/>
        ///             ○ Match a character in the set [Rr].<br/>
        ///             ○ Match a character in the set [Ii].<br/>
        ///             ○ Match a character in the set [Pp].<br/>
        ///             ○ Match a character in the set [Tt].<br/>
        ///             ○ Match a whitespace character atomically any number of times.<br/>
        ///             ○ Match ':'.<br/>
        ///         ○ Match a sequence of expressions.<br/>
        ///             ○ Match a character in the set [Uu].<br/>
        ///             ○ Match a character in the set [Rr].<br/>
        ///             ○ Match a character in the set [Ll].<br/>
        ///             ○ Match a whitespace character atomically any number of times.<br/>
        ///             ○ Match '('.<br/>
        ///             ○ Match a whitespace character greedily any number of times.<br/>
        ///             ○ Match a character in the set ["'] atomically, optionally.<br/>
        ///             ○ Match a whitespace character atomically any number of times.<br/>
        ///             ○ Match a character in the set [Jj].<br/>
        ///             ○ Match a character in the set [Aa].<br/>
        ///             ○ Match a character in the set [Vv].<br/>
        ///             ○ Match a character in the set [Aa].<br/>
        ///             ○ Match a character in the set [Ss].<br/>
        ///             ○ Match a character in the set [Cc].<br/>
        ///             ○ Match a character in the set [Rr].<br/>
        ///             ○ Match a character in the set [Ii].<br/>
        ///             ○ Match a character in the set [Pp].<br/>
        ///             ○ Match a character in the set [Tt].<br/>
        ///             ○ Match a whitespace character atomically any number of times.<br/>
        ///             ○ Match ':'.<br/>
        /// ○ Match a character in the set ["#%'-)+-.0-;A-Z_a-{}\u212A\s] greedily at least once.<br/>
        /// ○ Match if at the end of the string or if before an ending newline.<br/>
        /// </code>
        /// </remarks>
        [global::System.CodeDom.Compiler.GeneratedCodeAttribute("System.Text.RegularExpressions.Generator", "8.0.14.36720")]
        private static partial global::System.Text.RegularExpressions.Regex InlineStyleRegex() => global::System.Text.RegularExpressions.Generated.InlineStyleRegex_3.Instance;
    }
}

namespace Microsoft.FluentUI.AspNetCore.Components.Utilities
{
    partial struct MarkupStringSanitized
    {
        /// <remarks>
        /// Pattern:<br/>
        /// <code>^(?:(?:[^&lt;&gt;]|&lt;(?:p|div|span|strong|em|b|i|u|ul|ol|li|h[1-6]|br|hr)(?:\\s+(?:class|id|title|data-[a-z0-9\\-]+)\\s*=\\s*(?:"[^"&lt;&gt;]*"|'[^'&lt;&gt;]*'))*\\s*\\/?&gt;|&lt;\\/(?:p|div|span|strong|em|b|i|u|ul|ol|li|h[1-6])&gt;))*$</code><br/>
        /// Options:<br/>
        /// <code>RegexOptions.IgnoreCase | RegexOptions.ExplicitCapture | RegexOptions.Singleline</code><br/>
        /// Explanation:<br/>
        /// <code>
        /// ○ Match if at the beginning of the string.<br/>
        /// ○ Loop greedily any number of times.<br/>
        ///     ○ Match with 3 alternative expressions.<br/>
        ///         ○ Match a character in the set [^&lt;&gt;].<br/>
        ///         ○ Match a sequence of expressions.<br/>
        ///             ○ Match '&lt;'.<br/>
        ///             ○ Match with 12 alternative expressions.<br/>
        ///                 ○ Match a character in the set [Pp].<br/>
        ///                 ○ Match a sequence of expressions.<br/>
        ///                     ○ Match a character in the set [Dd].<br/>
        ///                     ○ Match a character in the set [Ii].<br/>
        ///                     ○ Match a character in the set [Vv].<br/>
        ///                 ○ Match a sequence of expressions.<br/>
        ///                     ○ Match a character in the set [Ss].<br/>
        ///                     ○ Match a character in the set [Pp].<br/>
        ///                     ○ Match a character in the set [Aa].<br/>
        ///                     ○ Match a character in the set [Nn].<br/>
        ///                 ○ Match a sequence of expressions.<br/>
        ///                     ○ Match a character in the set [Ss].<br/>
        ///                     ○ Match a character in the set [Tt].<br/>
        ///                     ○ Match a character in the set [Rr].<br/>
        ///                     ○ Match a character in the set [Oo].<br/>
        ///                     ○ Match a character in the set [Nn].<br/>
        ///                     ○ Match a character in the set [Gg].<br/>
        ///                 ○ Match a sequence of expressions.<br/>
        ///                     ○ Match a character in the set [Ee].<br/>
        ///                     ○ Match a character in the set [Mm].<br/>
        ///                 ○ Match a character in the set [BIUbiu].<br/>
        ///                 ○ Match a sequence of expressions.<br/>
        ///                     ○ Match a character in the set [Uu].<br/>
        ///                     ○ Match a character in the set [Ll].<br/>
        ///                 ○ Match a sequence of expressions.<br/>
        ///                     ○ Match a character in the set [Oo].<br/>
        ///                     ○ Match a character in the set [Ll].<br/>
        ///                 ○ Match a sequence of expressions.<br/>
        ///                     ○ Match a character in the set [Ll].<br/>
        ///                     ○ Match a character in the set [Ii].<br/>
        ///                 ○ Match a sequence of expressions.<br/>
        ///                     ○ Match a character in the set [Hh].<br/>
        ///                     ○ Match a character in the set [1-6].<br/>
        ///                 ○ Match a sequence of expressions.<br/>
        ///                     ○ Match a character in the set [Bb].<br/>
        ///                     ○ Match a character in the set [Rr].<br/>
        ///                 ○ Match a sequence of expressions.<br/>
        ///                     ○ Match a character in the set [Hh].<br/>
        ///                     ○ Match a character in the set [Rr].<br/>
        ///             ○ Loop greedily any number of times.<br/>
        ///                 ○ Match a whitespace character atomically at least once.<br/>
        ///                 ○ Match with 4 alternative expressions.<br/>
        ///                     ○ Match a sequence of expressions.<br/>
        ///                         ○ Match a character in the set [Cc].<br/>
        ///                         ○ Match a character in the set [Ll].<br/>
        ///                         ○ Match a character in the set [Aa].<br/>
        ///                         ○ Match a character in the set [Ss] exactly 2 times.<br/>
        ///                     ○ Match a sequence of expressions.<br/>
        ///                         ○ Match a character in the set [Ii].<br/>
        ///                         ○ Match a character in the set [Dd].<br/>
        ///                     ○ Match a sequence of expressions.<br/>
        ///                         ○ Match a character in the set [Tt].<br/>
        ///                         ○ Match a character in the set [Ii].<br/>
        ///                         ○ Match a character in the set [Tt].<br/>
        ///                         ○ Match a character in the set [Ll].<br/>
        ///                         ○ Match a character in the set [Ee].<br/>
        ///                     ○ Match a sequence of expressions.<br/>
        ///                         ○ Match a character in the set [Dd].<br/>
        ///                         ○ Match a character in the set [Aa].<br/>
        ///                         ○ Match a character in the set [Tt].<br/>
        ///                         ○ Match a character in the set [Aa].<br/>
        ///                         ○ Match '-'.<br/>
        ///                         ○ Match a character in the set [-0-9A-Za-z\u212A] atomically at least once.<br/>
        ///                 ○ Match a whitespace character atomically any number of times.<br/>
        ///                 ○ Match '='.<br/>
        ///                 ○ Match a whitespace character atomically any number of times.<br/>
        ///                 ○ Match with 2 alternative expressions.<br/>
        ///                     ○ Match a sequence of expressions.<br/>
        ///                         ○ Match '"'.<br/>
        ///                         ○ Match a character in the set [^"&lt;&gt;] atomically any number of times.<br/>
        ///                         ○ Match '"'.<br/>
        ///                     ○ Match a sequence of expressions.<br/>
        ///                         ○ Match '\''.<br/>
        ///                         ○ Match a character in the set [^'&lt;&gt;] atomically any number of times.<br/>
        ///                         ○ Match '\''.<br/>
        ///             ○ Match a whitespace character atomically any number of times.<br/>
        ///             ○ Match '/' atomically, optionally.<br/>
        ///             ○ Match '&gt;'.<br/>
        ///         ○ Match a sequence of expressions.<br/>
        ///             ○ Match the string "&lt;/".<br/>
        ///             ○ Match with 10 alternative expressions.<br/>
        ///                 ○ Match a character in the set [Pp].<br/>
        ///                 ○ Match a sequence of expressions.<br/>
        ///                     ○ Match a character in the set [Dd].<br/>
        ///                     ○ Match a character in the set [Ii].<br/>
        ///                     ○ Match a character in the set [Vv].<br/>
        ///                 ○ Match a sequence of expressions.<br/>
        ///                     ○ Match a character in the set [Ss].<br/>
        ///                     ○ Match a character in the set [Pp].<br/>
        ///                     ○ Match a character in the set [Aa].<br/>
        ///                     ○ Match a character in the set [Nn].<br/>
        ///                 ○ Match a sequence of expressions.<br/>
        ///                     ○ Match a character in the set [Ss].<br/>
        ///                     ○ Match a character in the set [Tt].<br/>
        ///                     ○ Match a character in the set [Rr].<br/>
        ///                     ○ Match a character in the set [Oo].<br/>
        ///                     ○ Match a character in the set [Nn].<br/>
        ///                     ○ Match a character in the set [Gg].<br/>
        ///                 ○ Match a sequence of expressions.<br/>
        ///                     ○ Match a character in the set [Ee].<br/>
        ///                     ○ Match a character in the set [Mm].<br/>
        ///                 ○ Match a character in the set [BIUbiu].<br/>
        ///                 ○ Match a sequence of expressions.<br/>
        ///                     ○ Match a character in the set [Uu].<br/>
        ///                     ○ Match a character in the set [Ll].<br/>
        ///                 ○ Match a sequence of expressions.<br/>
        ///                     ○ Match a character in the set [Oo].<br/>
        ///                     ○ Match a character in the set [Ll].<br/>
        ///                 ○ Match a sequence of expressions.<br/>
        ///                     ○ Match a character in the set [Ll].<br/>
        ///                     ○ Match a character in the set [Ii].<br/>
        ///                 ○ Match a sequence of expressions.<br/>
        ///                     ○ Match a character in the set [Hh].<br/>
        ///                     ○ Match a character in the set [1-6].<br/>
        ///             ○ Match '&gt;'.<br/>
        /// ○ Match if at the end of the string or if before an ending newline.<br/>
        /// </code>
        /// </remarks>
        [global::System.CodeDom.Compiler.GeneratedCodeAttribute("System.Text.RegularExpressions.Generator", "8.0.14.36720")]
        private static partial global::System.Text.RegularExpressions.Regex HtmlSanitizerRegex() => global::System.Text.RegularExpressions.Generated.HtmlSanitizerRegex_4.Instance;
    }
}

namespace Microsoft.FluentUI.AspNetCore.Components.Utilities
{
    partial struct MarkupStringSanitized
    {
        /// <remarks>
        /// Pattern:<br/>
        /// <code>^&lt;(?&lt;tag&gt;[a-zA-Z][a-zA-Z0-9]*?)(?:\\s+(?&lt;attribute&gt;[a-zA-Z][a-zA-Z0-9\\-]*?)\\s*=\\s*(?&lt;quote&gt;["'])(?&lt;attributevalue&gt;.*?)\\k&lt;quote&gt;)?(?:\\s*/)?&gt;(?&lt;content&gt;[\\s\\S]*?)(?:&lt;/\\k&lt;tag&gt;&gt;)?$</code><br/>
        /// Options:<br/>
        /// <code>RegexOptions.ExplicitCapture | RegexOptions.Singleline</code><br/>
        /// Explanation:<br/>
        /// <code>
        /// ○ Match if at the beginning of the string.<br/>
        /// ○ Match '&lt;'.<br/>
        /// ○ "tag" capture group.<br/>
        ///     ○ Match a character in the set [A-Za-z].<br/>
        ///     ○ Match a character in the set [0-9A-Za-z] lazily any number of times.<br/>
        /// ○ Optional (greedy).<br/>
        ///     ○ Match a whitespace character atomically at least once.<br/>
        ///     ○ "attribute" capture group.<br/>
        ///         ○ Match a character in the set [A-Za-z].<br/>
        ///         ○ Match a character in the set [-0-9A-Za-z] lazily any number of times.<br/>
        ///     ○ Match a whitespace character atomically any number of times.<br/>
        ///     ○ Match '='.<br/>
        ///     ○ Match a whitespace character atomically any number of times.<br/>
        ///     ○ "quote" capture group.<br/>
        ///         ○ Match a character in the set ["'].<br/>
        ///     ○ "attributevalue" capture group.<br/>
        ///         ○ Match any character lazily any number of times.<br/>
        ///     ○ Match the same text as matched by the "quote" capture group.<br/>
        /// ○ Optional (greedy).<br/>
        ///     ○ Match a whitespace character atomically any number of times.<br/>
        ///     ○ Match '/'.<br/>
        /// ○ Match '&gt;'.<br/>
        /// ○ "content" capture group.<br/>
        ///     ○ Match any character lazily any number of times.<br/>
        /// ○ Optional (greedy).<br/>
        ///     ○ Match the string "&lt;/".<br/>
        ///     ○ Match the same text as matched by the "tag" capture group.<br/>
        ///     ○ Match '&gt;'.<br/>
        /// ○ Match if at the end of the string or if before an ending newline.<br/>
        /// </code>
        /// </remarks>
        [global::System.CodeDom.Compiler.GeneratedCodeAttribute("System.Text.RegularExpressions.Generator", "8.0.14.36720")]
        private static partial global::System.Text.RegularExpressions.Regex RegExWithTagAndStyle() => global::System.Text.RegularExpressions.Generated.RegExWithTagAndStyle_5.Instance;
    }
}

namespace System.Text.RegularExpressions.Generated
{
    using System;
    using System.Buffers;
    using System.CodeDom.Compiler;
    using System.Collections;
    using System.ComponentModel;
    using System.Globalization;
    using System.Runtime.CompilerServices;
    using System.Text.RegularExpressions;
    using System.Threading;

    /// <summary>Custom <see cref="Regex"/>-derived type for the HexColorRegex method.</summary>
    [GeneratedCodeAttribute("System.Text.RegularExpressions.Generator", "8.0.14.36720")]
    file sealed class HexColorRegex_0 : Regex
    {
        /// <summary>Cached, thread-safe singleton instance.</summary>
        internal static readonly HexColorRegex_0 Instance = new();
    
        /// <summary>Initializes the instance.</summary>
        private HexColorRegex_0()
        {
            base.pattern = "^#(?:[0-9a-fA-F]{3}|[0-9a-fA-F]{6})$";
            base.roptions = RegexOptions.None;
            base.internalMatchTimeout = TimeSpan.FromMilliseconds(100);
            base.factory = new RunnerFactory();
            base.capsize = 1;
        }
            
        /// <summary>Provides a factory for creating <see cref="RegexRunner"/> instances to be used by methods on <see cref="Regex"/>.</summary>
        private sealed class RunnerFactory : RegexRunnerFactory
        {
            /// <summary>Creates an instance of a <see cref="RegexRunner"/> used by methods on <see cref="Regex"/>.</summary>
            protected override RegexRunner CreateInstance() => new Runner();
        
            /// <summary>Provides the runner that contains the custom logic implementing the specified regular expression.</summary>
            private sealed class Runner : RegexRunner
            {
                /// <summary>Scan the <paramref name="inputSpan"/> starting from base.runtextstart for the next match.</summary>
                /// <param name="inputSpan">The text being scanned by the regular expression.</param>
                protected override void Scan(ReadOnlySpan<char> inputSpan)
                {
                    // The pattern is anchored.  Validate the current position and try to match at it only.
                    if (TryFindNextPossibleStartingPosition(inputSpan) && !TryMatchAtCurrentPosition(inputSpan))
                    {
                        base.runtextpos = inputSpan.Length;
                    }
                }
        
                /// <summary>Search <paramref name="inputSpan"/> starting from base.runtextpos for the next location a match could possibly start.</summary>
                /// <param name="inputSpan">The text being scanned by the regular expression.</param>
                /// <returns>true if a possible match was found; false if no more matches are possible.</returns>
                private bool TryFindNextPossibleStartingPosition(ReadOnlySpan<char> inputSpan)
                {
                    int pos = base.runtextpos;
                    
                    // Any possible match is at least 4 characters.
                    if (pos <= inputSpan.Length - 4)
                    {
                        // The pattern leads with a beginning (\A) anchor.
                        if (pos == 0)
                        {
                            return true;
                        }
                    }
                    
                    // No match found.
                    base.runtextpos = inputSpan.Length;
                    return false;
                }
        
                /// <summary>Determine whether <paramref name="inputSpan"/> at base.runtextpos is a match for the regular expression.</summary>
                /// <param name="inputSpan">The text being scanned by the regular expression.</param>
                /// <returns>true if the regular expression matches at the current position; otherwise, false.</returns>
                private bool TryMatchAtCurrentPosition(ReadOnlySpan<char> inputSpan)
                {
                    int pos = base.runtextpos;
                    int matchStart = pos;
                    int alternation_branch = 0;
                    int alternation_starting_pos = 0;
                    ReadOnlySpan<char> slice = inputSpan.Slice(pos);
                    
                    // Match if at the beginning of the string.
                    if (pos != 0)
                    {
                        return false; // The input didn't match.
                    }
                    
                    // Match '#'.
                    if (slice.IsEmpty || slice[0] != '#')
                    {
                        return false; // The input didn't match.
                    }
                    
                    // Match with 2 alternative expressions.
                    //{
                        alternation_starting_pos = pos;
                        
                        // Branch 0
                        //{
                            // Match a character in the set [0-9A-Fa-f] exactly 3 times.
                            {
                                if ((uint)slice.Length < 4 ||
                                    !char.IsAsciiHexDigit(slice[1]) ||
                                    !char.IsAsciiHexDigit(slice[2]) ||
                                    !char.IsAsciiHexDigit(slice[3]))
                                {
                                    goto AlternationBranch;
                                }
                            }
                            
                            alternation_branch = 0;
                            pos += 4;
                            slice = inputSpan.Slice(pos);
                            goto AlternationMatch;
                            
                            AlternationBranch:
                            pos = alternation_starting_pos;
                            slice = inputSpan.Slice(pos);
                        //}
                        
                        // Branch 1
                        //{
                            // Match a character in the set [0-9A-Fa-f] exactly 6 times.
                            {
                                if ((uint)slice.Length < 7 ||
                                    !char.IsAsciiHexDigit(slice[1]) ||
                                    !char.IsAsciiHexDigit(slice[2]) ||
                                    !char.IsAsciiHexDigit(slice[3]) ||
                                    !char.IsAsciiHexDigit(slice[4]) ||
                                    !char.IsAsciiHexDigit(slice[5]) ||
                                    !char.IsAsciiHexDigit(slice[6]))
                                {
                                    return false; // The input didn't match.
                                }
                            }
                            
                            alternation_branch = 1;
                            pos += 7;
                            slice = inputSpan.Slice(pos);
                            goto AlternationMatch;
                        //}
                        
                        AlternationBacktrack:
                        base.CheckTimeout();
                        
                        switch (alternation_branch)
                        {
                            case 0:
                                goto AlternationBranch;
                            case 1:
                                return false; // The input didn't match.
                        }
                        
                        AlternationMatch:;
                    //}
                    
                    // Match if at the end of the string or if before an ending newline.
                    if (pos < inputSpan.Length - 1 || ((uint)pos < (uint)inputSpan.Length && inputSpan[pos] != '\n'))
                    {
                        goto AlternationBacktrack;
                    }
                    
                    // The input matched.
                    base.runtextpos = pos;
                    base.Capture(0, matchStart, pos);
                    return true;
                }
            }
        }

    }
    
    /// <summary>Custom <see cref="Regex"/>-derived type for the GenerateValidClassNameRegex method.</summary>
    [GeneratedCodeAttribute("System.Text.RegularExpressions.Generator", "8.0.14.36720")]
    file sealed class GenerateValidClassNameRegex_1 : Regex
    {
        /// <summary>Cached, thread-safe singleton instance.</summary>
        internal static readonly GenerateValidClassNameRegex_1 Instance = new();
    
        /// <summary>Initializes the instance.</summary>
        private GenerateValidClassNameRegex_1()
        {
            base.pattern = "^-?[_a-zA-Z]+[_a-zA-Z0-9-]*$";
            base.roptions = RegexOptions.Compiled;
            base.internalMatchTimeout = TimeSpan.FromMilliseconds(1000);
            base.factory = new RunnerFactory();
            base.capsize = 1;
        }
            
        /// <summary>Provides a factory for creating <see cref="RegexRunner"/> instances to be used by methods on <see cref="Regex"/>.</summary>
        private sealed class RunnerFactory : RegexRunnerFactory
        {
            /// <summary>Creates an instance of a <see cref="RegexRunner"/> used by methods on <see cref="Regex"/>.</summary>
            protected override RegexRunner CreateInstance() => new Runner();
        
            /// <summary>Provides the runner that contains the custom logic implementing the specified regular expression.</summary>
            private sealed class Runner : RegexRunner
            {
                /// <summary>Scan the <paramref name="inputSpan"/> starting from base.runtextstart for the next match.</summary>
                /// <param name="inputSpan">The text being scanned by the regular expression.</param>
                protected override void Scan(ReadOnlySpan<char> inputSpan)
                {
                    // The pattern is anchored.  Validate the current position and try to match at it only.
                    if (TryFindNextPossibleStartingPosition(inputSpan) && !TryMatchAtCurrentPosition(inputSpan))
                    {
                        base.runtextpos = inputSpan.Length;
                    }
                }
        
                /// <summary>Search <paramref name="inputSpan"/> starting from base.runtextpos for the next location a match could possibly start.</summary>
                /// <param name="inputSpan">The text being scanned by the regular expression.</param>
                /// <returns>true if a possible match was found; false if no more matches are possible.</returns>
                private bool TryFindNextPossibleStartingPosition(ReadOnlySpan<char> inputSpan)
                {
                    int pos = base.runtextpos;
                    
                    // Empty matches aren't possible.
                    if ((uint)pos < (uint)inputSpan.Length)
                    {
                        // The pattern leads with a beginning (\A) anchor.
                        if (pos == 0)
                        {
                            return true;
                        }
                    }
                    
                    // No match found.
                    base.runtextpos = inputSpan.Length;
                    return false;
                }
        
                /// <summary>Determine whether <paramref name="inputSpan"/> at base.runtextpos is a match for the regular expression.</summary>
                /// <param name="inputSpan">The text being scanned by the regular expression.</param>
                /// <returns>true if the regular expression matches at the current position; otherwise, false.</returns>
                private bool TryMatchAtCurrentPosition(ReadOnlySpan<char> inputSpan)
                {
                    int pos = base.runtextpos;
                    int matchStart = pos;
                    int charloop_starting_pos = 0, charloop_ending_pos = 0;
                    ReadOnlySpan<char> slice = inputSpan.Slice(pos);
                    
                    // Match if at the beginning of the string.
                    if (pos != 0)
                    {
                        return false; // The input didn't match.
                    }
                    
                    // Match '-' atomically, optionally.
                    {
                        if (!slice.IsEmpty && slice[0] == '-')
                        {
                            slice = slice.Slice(1);
                            pos++;
                        }
                    }
                    
                    // Match a character in the set [A-Z_a-z] greedily at least once.
                    //{
                        charloop_starting_pos = pos;
                        
                        int iteration = slice.IndexOfAnyExcept(Utilities.s_ascii_FEFFFF87FEFFFF07);
                        if (iteration < 0)
                        {
                            iteration = slice.Length;
                        }
                        
                        if (iteration == 0)
                        {
                            return false; // The input didn't match.
                        }
                        
                        slice = slice.Slice(iteration);
                        pos += iteration;
                        
                        charloop_ending_pos = pos;
                        charloop_starting_pos++;
                        goto CharLoopEnd;
                        
                        CharLoopBacktrack:
                        
                        base.CheckTimeout();
                        
                        if (charloop_starting_pos >= charloop_ending_pos)
                        {
                            return false; // The input didn't match.
                        }
                        pos = --charloop_ending_pos;
                        slice = inputSpan.Slice(pos);
                        
                        CharLoopEnd:
                    //}
                    
                    // Match a character in the set [-0-9A-Z_a-z] atomically any number of times.
                    {
                        int iteration1 = slice.IndexOfAnyExcept(Utilities.s_ascii_20FF03FEFFFF87FEFFFF07);
                        if (iteration1 < 0)
                        {
                            iteration1 = slice.Length;
                        }
                        
                        slice = slice.Slice(iteration1);
                        pos += iteration1;
                    }
                    
                    // Match if at the end of the string or if before an ending newline.
                    if (pos < inputSpan.Length - 1 || ((uint)pos < (uint)inputSpan.Length && inputSpan[pos] != '\n'))
                    {
                        goto CharLoopBacktrack;
                    }
                    
                    // The input matched.
                    base.runtextpos = pos;
                    base.Capture(0, matchStart, pos);
                    return true;
                }
            }
        }

    }
    
    /// <summary>Custom <see cref="Regex"/>-derived type for the TagNameSanitizerRegex method.</summary>
    [GeneratedCodeAttribute("System.Text.RegularExpressions.Generator", "8.0.14.36720")]
    file sealed class TagNameSanitizerRegex_2 : Regex
    {
        /// <summary>Cached, thread-safe singleton instance.</summary>
        internal static readonly TagNameSanitizerRegex_2 Instance = new();
    
        /// <summary>Initializes the instance.</summary>
        private TagNameSanitizerRegex_2()
        {
            base.pattern = "[^a-zA-Z0-9\\-.:_]";
            base.roptions = RegexOptions.None;
            base.internalMatchTimeout = TimeSpan.FromMilliseconds(400);
            base.factory = new RunnerFactory();
            base.capsize = 1;
        }
            
        /// <summary>Provides a factory for creating <see cref="RegexRunner"/> instances to be used by methods on <see cref="Regex"/>.</summary>
        private sealed class RunnerFactory : RegexRunnerFactory
        {
            /// <summary>Creates an instance of a <see cref="RegexRunner"/> used by methods on <see cref="Regex"/>.</summary>
            protected override RegexRunner CreateInstance() => new Runner();
        
            /// <summary>Provides the runner that contains the custom logic implementing the specified regular expression.</summary>
            private sealed class Runner : RegexRunner
            {
                /// <summary>Scan the <paramref name="inputSpan"/> starting from base.runtextstart for the next match.</summary>
                /// <param name="inputSpan">The text being scanned by the regular expression.</param>
                protected override void Scan(ReadOnlySpan<char> inputSpan)
                {
                    if (TryFindNextPossibleStartingPosition(inputSpan))
                    {
                        // The search in TryFindNextPossibleStartingPosition performed the entire match.
                        int start = base.runtextpos;
                        int end = base.runtextpos = start + 1;
                        base.Capture(0, start, end);
                    }
                }
        
                /// <summary>Search <paramref name="inputSpan"/> starting from base.runtextpos for the next location a match could possibly start.</summary>
                /// <param name="inputSpan">The text being scanned by the regular expression.</param>
                /// <returns>true if a possible match was found; false if no more matches are possible.</returns>
                private bool TryFindNextPossibleStartingPosition(ReadOnlySpan<char> inputSpan)
                {
                    int pos = base.runtextpos;
                    
                    // Empty matches aren't possible.
                    if ((uint)pos < (uint)inputSpan.Length)
                    {
                        // The pattern begins with a character in the set [^-.0-:A-Z_a-z].
                        // Find the next occurrence. If it can't be found, there's no match.
                        int i = inputSpan.Slice(pos).IndexOfAnyExcept(Utilities.s_ascii_60FF07FEFFFF87FEFFFF07);
                        if (i >= 0)
                        {
                            base.runtextpos = pos + i;
                            return true;
                        }
                    }
                    
                    // No match found.
                    base.runtextpos = inputSpan.Length;
                    return false;
                }
            }
        }

    }
    
    /// <summary>Custom <see cref="Regex"/>-derived type for the InlineStyleRegex method.</summary>
    [GeneratedCodeAttribute("System.Text.RegularExpressions.Generator", "8.0.14.36720")]
    file sealed class InlineStyleRegex_3 : Regex
    {
        /// <summary>Cached, thread-safe singleton instance.</summary>
        internal static readonly InlineStyleRegex_3 Instance = new();
    
        /// <summary>Initializes the instance.</summary>
        private InlineStyleRegex_3()
        {
            base.pattern = "^(?!.*(behavior\\s*:|expression\\s*\\(|-moz-binding\\s*:|javascript\\s*:|url\\s*\\(\\s*['\"]?\\s*javascript\\s*:))[a-zA-Z0-9\\s,.:;_%\\-+()#{}'\"pxemremvhsmsdegradturn]+$";
            base.roptions = RegexOptions.IgnoreCase | RegexOptions.ExplicitCapture;
            base.internalMatchTimeout = TimeSpan.FromMilliseconds(400);
            base.factory = new RunnerFactory();
            base.capsize = 1;
        }
            
        /// <summary>Provides a factory for creating <see cref="RegexRunner"/> instances to be used by methods on <see cref="Regex"/>.</summary>
        private sealed class RunnerFactory : RegexRunnerFactory
        {
            /// <summary>Creates an instance of a <see cref="RegexRunner"/> used by methods on <see cref="Regex"/>.</summary>
            protected override RegexRunner CreateInstance() => new Runner();
        
            /// <summary>Provides the runner that contains the custom logic implementing the specified regular expression.</summary>
            private sealed class Runner : RegexRunner
            {
                /// <summary>Scan the <paramref name="inputSpan"/> starting from base.runtextstart for the next match.</summary>
                /// <param name="inputSpan">The text being scanned by the regular expression.</param>
                protected override void Scan(ReadOnlySpan<char> inputSpan)
                {
                    // The pattern is anchored.  Validate the current position and try to match at it only.
                    if (TryFindNextPossibleStartingPosition(inputSpan) && !TryMatchAtCurrentPosition(inputSpan))
                    {
                        base.runtextpos = inputSpan.Length;
                    }
                }
        
                /// <summary>Search <paramref name="inputSpan"/> starting from base.runtextpos for the next location a match could possibly start.</summary>
                /// <param name="inputSpan">The text being scanned by the regular expression.</param>
                /// <returns>true if a possible match was found; false if no more matches are possible.</returns>
                private bool TryFindNextPossibleStartingPosition(ReadOnlySpan<char> inputSpan)
                {
                    int pos = base.runtextpos;
                    
                    // Empty matches aren't possible.
                    if ((uint)pos < (uint)inputSpan.Length)
                    {
                        // The pattern leads with a beginning (\A) anchor.
                        if (pos == 0)
                        {
                            return true;
                        }
                    }
                    
                    // No match found.
                    base.runtextpos = inputSpan.Length;
                    return false;
                }
        
                /// <summary>Determine whether <paramref name="inputSpan"/> at base.runtextpos is a match for the regular expression.</summary>
                /// <param name="inputSpan">The text being scanned by the regular expression.</param>
                /// <returns>true if the regular expression matches at the current position; otherwise, false.</returns>
                private bool TryMatchAtCurrentPosition(ReadOnlySpan<char> inputSpan)
                {
                    int pos = base.runtextpos;
                    int matchStart = pos;
                    char ch;
                    int charloop_starting_pos = 0, charloop_ending_pos = 0;
                    int charloop_starting_pos1 = 0, charloop_ending_pos1 = 0;
                    int charloop_starting_pos2 = 0, charloop_ending_pos2 = 0;
                    int stackpos = 0;
                    ReadOnlySpan<char> slice = inputSpan.Slice(pos);
                    
                    // Match if at the beginning of the string.
                    if (pos != 0)
                    {
                        return false; // The input didn't match.
                    }
                    
                    // Zero-width negative lookahead.
                    {
                        int negativelookahead_starting_pos = pos;
                        base.CheckTimeout();
                        
                        int atomic_stackpos = stackpos;
                        
                        // Match a character other than '\n' greedily any number of times.
                        //{
                            charloop_starting_pos = pos;
                            
                            int iteration = slice.IndexOf('\n');
                            if (iteration < 0)
                            {
                                iteration = slice.Length;
                            }
                            
                            slice = slice.Slice(iteration);
                            pos += iteration;
                            
                            charloop_ending_pos = pos;
                            goto CharLoopEnd;
                            
                            CharLoopBacktrack:
                            
                            base.CheckTimeout();
                            
                            if (charloop_starting_pos >= charloop_ending_pos)
                            {
                                goto NegativeLookaroundMatch;
                            }
                            pos = --charloop_ending_pos;
                            slice = inputSpan.Slice(pos);
                            
                            CharLoopEnd:
                        //}
                        
                        // Atomic group.
                        {
                            int atomic_stackpos1 = stackpos;
                            
                            // Match with 5 alternative expressions, atomically.
                            //{
                                if (slice.IsEmpty)
                                {
                                    goto CharLoopBacktrack;
                                }
                                
                                switch (slice[0])
                                {
                                    case 'B' or 'b':
                                        if ((uint)slice.Length < 8 ||
                                            !slice.Slice(1).StartsWith("ehavior", StringComparison.OrdinalIgnoreCase)) // Match the string "ehavior" (ordinal case-insensitive)
                                        {
                                            goto CharLoopBacktrack;
                                        }
                                        
                                        // Match a whitespace character atomically any number of times.
                                        {
                                            int iteration1 = 8;
                                            while ((uint)iteration1 < (uint)slice.Length && char.IsWhiteSpace(slice[iteration1]))
                                            {
                                                iteration1++;
                                            }
                                            
                                            slice = slice.Slice(iteration1);
                                            pos += iteration1;
                                        }
                                        
                                        // Match ':'.
                                        if (slice.IsEmpty || slice[0] != ':')
                                        {
                                            goto CharLoopBacktrack;
                                        }
                                        
                                        pos++;
                                        slice = inputSpan.Slice(pos);
                                        break;
                                        
                                    case 'E' or 'e':
                                        if ((uint)slice.Length < 10 ||
                                            !slice.Slice(1).StartsWith("xpression", StringComparison.OrdinalIgnoreCase)) // Match the string "xpression" (ordinal case-insensitive)
                                        {
                                            goto CharLoopBacktrack;
                                        }
                                        
                                        // Match a whitespace character atomically any number of times.
                                        {
                                            int iteration2 = 10;
                                            while ((uint)iteration2 < (uint)slice.Length && char.IsWhiteSpace(slice[iteration2]))
                                            {
                                                iteration2++;
                                            }
                                            
                                            slice = slice.Slice(iteration2);
                                            pos += iteration2;
                                        }
                                        
                                        // Match '('.
                                        if (slice.IsEmpty || slice[0] != '(')
                                        {
                                            goto CharLoopBacktrack;
                                        }
                                        
                                        pos++;
                                        slice = inputSpan.Slice(pos);
                                        break;
                                        
                                    case '-':
                                        if ((uint)slice.Length < 12 ||
                                            !slice.Slice(1).StartsWith("moz-binding", StringComparison.OrdinalIgnoreCase)) // Match the string "moz-binding" (ordinal case-insensitive)
                                        {
                                            goto CharLoopBacktrack;
                                        }
                                        
                                        // Match a whitespace character atomically any number of times.
                                        {
                                            int iteration3 = 12;
                                            while ((uint)iteration3 < (uint)slice.Length && char.IsWhiteSpace(slice[iteration3]))
                                            {
                                                iteration3++;
                                            }
                                            
                                            slice = slice.Slice(iteration3);
                                            pos += iteration3;
                                        }
                                        
                                        // Match ':'.
                                        if (slice.IsEmpty || slice[0] != ':')
                                        {
                                            goto CharLoopBacktrack;
                                        }
                                        
                                        pos++;
                                        slice = inputSpan.Slice(pos);
                                        break;
                                        
                                    case 'J' or 'j':
                                        if ((uint)slice.Length < 10 ||
                                            !slice.Slice(1).StartsWith("avascript", StringComparison.OrdinalIgnoreCase)) // Match the string "avascript" (ordinal case-insensitive)
                                        {
                                            goto CharLoopBacktrack;
                                        }
                                        
                                        // Match a whitespace character atomically any number of times.
                                        {
                                            int iteration4 = 10;
                                            while ((uint)iteration4 < (uint)slice.Length && char.IsWhiteSpace(slice[iteration4]))
                                            {
                                                iteration4++;
                                            }
                                            
                                            slice = slice.Slice(iteration4);
                                            pos += iteration4;
                                        }
                                        
                                        // Match ':'.
                                        if (slice.IsEmpty || slice[0] != ':')
                                        {
                                            goto CharLoopBacktrack;
                                        }
                                        
                                        pos++;
                                        slice = inputSpan.Slice(pos);
                                        break;
                                        
                                    case 'U' or 'u':
                                        if ((uint)slice.Length < 3 ||
                                            !slice.Slice(1).StartsWith("rl", StringComparison.OrdinalIgnoreCase)) // Match the string "rl" (ordinal case-insensitive)
                                        {
                                            goto CharLoopBacktrack;
                                        }
                                        
                                        // Match a whitespace character atomically any number of times.
                                        {
                                            int iteration5 = 3;
                                            while ((uint)iteration5 < (uint)slice.Length && char.IsWhiteSpace(slice[iteration5]))
                                            {
                                                iteration5++;
                                            }
                                            
                                            slice = slice.Slice(iteration5);
                                            pos += iteration5;
                                        }
                                        
                                        // Match '('.
                                        if (slice.IsEmpty || slice[0] != '(')
                                        {
                                            goto CharLoopBacktrack;
                                        }
                                        
                                        // Match a whitespace character greedily any number of times.
                                        //{
                                            pos++;
                                            slice = inputSpan.Slice(pos);
                                            charloop_starting_pos1 = pos;
                                            
                                            int iteration6 = 0;
                                            while ((uint)iteration6 < (uint)slice.Length && char.IsWhiteSpace(slice[iteration6]))
                                            {
                                                iteration6++;
                                            }
                                            
                                            slice = slice.Slice(iteration6);
                                            pos += iteration6;
                                            
                                            charloop_ending_pos1 = pos;
                                            goto CharLoopEnd1;
                                            
                                            CharLoopBacktrack1:
                                            
                                            base.CheckTimeout();
                                            
                                            if (charloop_starting_pos1 >= charloop_ending_pos1)
                                            {
                                                goto CharLoopBacktrack;
                                            }
                                            pos = --charloop_ending_pos1;
                                            slice = inputSpan.Slice(pos);
                                            
                                            CharLoopEnd1:
                                        //}
                                        
                                        // Match a character in the set ["'] atomically, optionally.
                                        {
                                            if (!slice.IsEmpty && (((ch = slice[0]) == '"') | (ch == '\'')))
                                            {
                                                slice = slice.Slice(1);
                                                pos++;
                                            }
                                        }
                                        
                                        // Match a whitespace character atomically any number of times.
                                        {
                                            int iteration7 = 0;
                                            while ((uint)iteration7 < (uint)slice.Length && char.IsWhiteSpace(slice[iteration7]))
                                            {
                                                iteration7++;
                                            }
                                            
                                            slice = slice.Slice(iteration7);
                                            pos += iteration7;
                                        }
                                        
                                        if ((uint)slice.Length < 10 ||
                                            !slice.StartsWith("javascript", StringComparison.OrdinalIgnoreCase)) // Match the string "javascript" (ordinal case-insensitive)
                                        {
                                            goto CharLoopBacktrack1;
                                        }
                                        
                                        // Match a whitespace character atomically any number of times.
                                        {
                                            int iteration8 = 10;
                                            while ((uint)iteration8 < (uint)slice.Length && char.IsWhiteSpace(slice[iteration8]))
                                            {
                                                iteration8++;
                                            }
                                            
                                            slice = slice.Slice(iteration8);
                                            pos += iteration8;
                                        }
                                        
                                        // Match ':'.
                                        if (slice.IsEmpty || slice[0] != ':')
                                        {
                                            goto CharLoopBacktrack1;
                                        }
                                        
                                        pos++;
                                        slice = inputSpan.Slice(pos);
                                        break;
                                        
                                    default:
                                        goto CharLoopBacktrack;
                                }
                            //}
                            
                            stackpos = atomic_stackpos1;
                        }
                        
                        stackpos = atomic_stackpos;
                        
                        return false; // The input didn't match.
                        
                        NegativeLookaroundMatch:
                        pos = negativelookahead_starting_pos;
                        slice = inputSpan.Slice(pos);
                    }
                    
                    // Match a character in the set ["#%'-)+-.0-;A-Z_a-{}\u212A\s] greedily at least once.
                    //{
                        charloop_starting_pos2 = pos;
                        
                        int iteration9 = 0;
                        while ((uint)iteration9 < (uint)slice.Length && ((ch = slice[iteration9]) < 128 ? ("㸀\0箭\u0fff\ufffe蟿\ufffe⿿"[ch >> 4] & (1 << (ch & 0xF))) != 0 : RegexRunner.CharInClass((char)ch, "\0\u0014\u0001\"$%&'*+/0<A[_`a|}~KÅd")))
                        {
                            iteration9++;
                        }
                        
                        if (iteration9 == 0)
                        {
                            return false; // The input didn't match.
                        }
                        
                        slice = slice.Slice(iteration9);
                        pos += iteration9;
                        
                        charloop_ending_pos2 = pos;
                        charloop_starting_pos2++;
                        goto CharLoopEnd2;
                        
                        CharLoopBacktrack2:
                        
                        base.CheckTimeout();
                        
                        if (charloop_starting_pos2 >= charloop_ending_pos2)
                        {
                            return false; // The input didn't match.
                        }
                        pos = --charloop_ending_pos2;
                        slice = inputSpan.Slice(pos);
                        
                        CharLoopEnd2:
                    //}
                    
                    // Match if at the end of the string or if before an ending newline.
                    if (pos < inputSpan.Length - 1 || ((uint)pos < (uint)inputSpan.Length && inputSpan[pos] != '\n'))
                    {
                        goto CharLoopBacktrack2;
                    }
                    
                    // The input matched.
                    base.runtextpos = pos;
                    base.Capture(0, matchStart, pos);
                    return true;
                }
            }
        }

    }
    
    /// <summary>Custom <see cref="Regex"/>-derived type for the HtmlSanitizerRegex method.</summary>
    [GeneratedCodeAttribute("System.Text.RegularExpressions.Generator", "8.0.14.36720")]
    file sealed class HtmlSanitizerRegex_4 : Regex
    {
        /// <summary>Cached, thread-safe singleton instance.</summary>
        internal static readonly HtmlSanitizerRegex_4 Instance = new();
    
        /// <summary>Initializes the instance.</summary>
        private HtmlSanitizerRegex_4()
        {
            base.pattern = "^(?:(?:[^<>]|<(?:p|div|span|strong|em|b|i|u|ul|ol|li|h[1-6]|br|hr)(?:\\s+(?:class|id|title|data-[a-z0-9\\-]+)\\s*=\\s*(?:\"[^\"<>]*\"|'[^'<>]*'))*\\s*\\/?>|<\\/(?:p|div|span|strong|em|b|i|u|ul|ol|li|h[1-6])>))*$";
            base.roptions = RegexOptions.IgnoreCase | RegexOptions.ExplicitCapture | RegexOptions.Singleline;
            base.internalMatchTimeout = TimeSpan.FromMilliseconds(400);
            base.factory = new RunnerFactory();
            base.capsize = 1;
        }
            
        /// <summary>Provides a factory for creating <see cref="RegexRunner"/> instances to be used by methods on <see cref="Regex"/>.</summary>
        private sealed class RunnerFactory : RegexRunnerFactory
        {
            /// <summary>Creates an instance of a <see cref="RegexRunner"/> used by methods on <see cref="Regex"/>.</summary>
            protected override RegexRunner CreateInstance() => new Runner();
        
            /// <summary>Provides the runner that contains the custom logic implementing the specified regular expression.</summary>
            private sealed class Runner : RegexRunner
            {
                /// <summary>Scan the <paramref name="inputSpan"/> starting from base.runtextstart for the next match.</summary>
                /// <param name="inputSpan">The text being scanned by the regular expression.</param>
                protected override void Scan(ReadOnlySpan<char> inputSpan)
                {
                    // The pattern is anchored.  Validate the current position and try to match at it only.
                    if (TryFindNextPossibleStartingPosition(inputSpan) && !TryMatchAtCurrentPosition(inputSpan))
                    {
                        base.runtextpos = inputSpan.Length;
                    }
                }
        
                /// <summary>Search <paramref name="inputSpan"/> starting from base.runtextpos for the next location a match could possibly start.</summary>
                /// <param name="inputSpan">The text being scanned by the regular expression.</param>
                /// <returns>true if a possible match was found; false if no more matches are possible.</returns>
                private bool TryFindNextPossibleStartingPosition(ReadOnlySpan<char> inputSpan)
                {
                    int pos = base.runtextpos;
                    
                    // The pattern leads with a beginning (\A) anchor.
                    if (pos == 0)
                    {
                        return true;
                    }
                    
                    // No match found.
                    base.runtextpos = inputSpan.Length;
                    return false;
                }
        
                /// <summary>Determine whether <paramref name="inputSpan"/> at base.runtextpos is a match for the regular expression.</summary>
                /// <param name="inputSpan">The text being scanned by the regular expression.</param>
                /// <returns>true if the regular expression matches at the current position; otherwise, false.</returns>
                private bool TryMatchAtCurrentPosition(ReadOnlySpan<char> inputSpan)
                {
                    int pos = base.runtextpos;
                    int matchStart = pos;
                    char ch;
                    int loop_iteration = 0;
                    int loop_iteration1 = 0;
                    int stackpos = 0;
                    ulong charMinusLowUInt64;
                    ReadOnlySpan<char> slice = inputSpan.Slice(pos);
                    
                    // Match if at the beginning of the string.
                    if (pos != 0)
                    {
                        return false; // The input didn't match.
                    }
                    
                    // Loop greedily any number of times.
                    //{
                        loop_iteration = 0;
                        
                        LoopBody:
                        Utilities.StackPush(ref base.runstack!, ref stackpos, pos);
                        
                        loop_iteration++;
                        
                        // Match with 3 alternative expressions.
                        //{
                            int alternation_starting_pos = pos;
                            
                            // Branch 0
                            //{
                                // Match a character in the set [^<>].
                                if (slice.IsEmpty || ((slice[0] | 0x2) == '>'))
                                {
                                    goto AlternationBranch;
                                }
                                
                                Utilities.StackPush(ref base.runstack!, ref stackpos, 0, alternation_starting_pos);
                                pos++;
                                slice = inputSpan.Slice(pos);
                                goto AlternationMatch;
                                
                                AlternationBranch:
                                pos = alternation_starting_pos;
                                slice = inputSpan.Slice(pos);
                            //}
                            
                            // Branch 1
                            //{
                                // Match '<'.
                                if (slice.IsEmpty || slice[0] != '<')
                                {
                                    goto AlternationBranch1;
                                }
                                
                                // Match with 12 alternative expressions.
                                //{
                                    int alternation_starting_pos1 = pos;
                                    
                                    // Branch 0
                                    //{
                                        // Match a character in the set [Pp].
                                        if ((uint)slice.Length < 2 || ((slice[1] | 0x20) != 'p'))
                                        {
                                            goto AlternationBranch2;
                                        }
                                        
                                        Utilities.StackPush(ref base.runstack!, ref stackpos, 0, alternation_starting_pos1);
                                        pos += 2;
                                        slice = inputSpan.Slice(pos);
                                        goto AlternationMatch1;
                                        
                                        AlternationBranch2:
                                        pos = alternation_starting_pos1;
                                        slice = inputSpan.Slice(pos);
                                    //}
                                    
                                    // Branch 1
                                    //{
                                        if ((uint)slice.Length < 4 ||
                                            !slice.Slice(1).StartsWith("div", StringComparison.OrdinalIgnoreCase)) // Match the string "div" (ordinal case-insensitive)
                                        {
                                            goto AlternationBranch3;
                                        }
                                        
                                        Utilities.StackPush(ref base.runstack!, ref stackpos, 1, alternation_starting_pos1);
                                        pos += 4;
                                        slice = inputSpan.Slice(pos);
                                        goto AlternationMatch1;
                                        
                                        AlternationBranch3:
                                        pos = alternation_starting_pos1;
                                        slice = inputSpan.Slice(pos);
                                    //}
                                    
                                    // Branch 2
                                    //{
                                        if ((uint)slice.Length < 5 ||
                                            !slice.Slice(1).StartsWith("span", StringComparison.OrdinalIgnoreCase)) // Match the string "span" (ordinal case-insensitive)
                                        {
                                            goto AlternationBranch4;
                                        }
                                        
                                        Utilities.StackPush(ref base.runstack!, ref stackpos, 2, alternation_starting_pos1);
                                        pos += 5;
                                        slice = inputSpan.Slice(pos);
                                        goto AlternationMatch1;
                                        
                                        AlternationBranch4:
                                        pos = alternation_starting_pos1;
                                        slice = inputSpan.Slice(pos);
                                    //}
                                    
                                    // Branch 3
                                    //{
                                        if ((uint)slice.Length < 7 ||
                                            !slice.Slice(1).StartsWith("strong", StringComparison.OrdinalIgnoreCase)) // Match the string "strong" (ordinal case-insensitive)
                                        {
                                            goto AlternationBranch5;
                                        }
                                        
                                        Utilities.StackPush(ref base.runstack!, ref stackpos, 3, alternation_starting_pos1);
                                        pos += 7;
                                        slice = inputSpan.Slice(pos);
                                        goto AlternationMatch1;
                                        
                                        AlternationBranch5:
                                        pos = alternation_starting_pos1;
                                        slice = inputSpan.Slice(pos);
                                    //}
                                    
                                    // Branch 4
                                    //{
                                        if ((uint)slice.Length < 3 ||
                                            !slice.Slice(1).StartsWith("em", StringComparison.OrdinalIgnoreCase)) // Match the string "em" (ordinal case-insensitive)
                                        {
                                            goto AlternationBranch6;
                                        }
                                        
                                        Utilities.StackPush(ref base.runstack!, ref stackpos, 4, alternation_starting_pos1);
                                        pos += 3;
                                        slice = inputSpan.Slice(pos);
                                        goto AlternationMatch1;
                                        
                                        AlternationBranch6:
                                        pos = alternation_starting_pos1;
                                        slice = inputSpan.Slice(pos);
                                    //}
                                    
                                    // Branch 5
                                    //{
                                        // Match a character in the set [BIUbiu].
                                        if ((uint)slice.Length < 2 || ((long)((0x8100100081001000UL << (int)(charMinusLowUInt64 = (uint)slice[1] - 'B')) & (charMinusLowUInt64 - 64)) >= 0))
                                        {
                                            goto AlternationBranch7;
                                        }
                                        
                                        Utilities.StackPush(ref base.runstack!, ref stackpos, 5, alternation_starting_pos1);
                                        pos += 2;
                                        slice = inputSpan.Slice(pos);
                                        goto AlternationMatch1;
                                        
                                        AlternationBranch7:
                                        pos = alternation_starting_pos1;
                                        slice = inputSpan.Slice(pos);
                                    //}
                                    
                                    // Branch 6
                                    //{
                                        if ((uint)slice.Length < 3 ||
                                            !slice.Slice(1).StartsWith("ul", StringComparison.OrdinalIgnoreCase)) // Match the string "ul" (ordinal case-insensitive)
                                        {
                                            goto AlternationBranch8;
                                        }
                                        
                                        Utilities.StackPush(ref base.runstack!, ref stackpos, 6, alternation_starting_pos1);
                                        pos += 3;
                                        slice = inputSpan.Slice(pos);
                                        goto AlternationMatch1;
                                        
                                        AlternationBranch8:
                                        pos = alternation_starting_pos1;
                                        slice = inputSpan.Slice(pos);
                                    //}
                                    
                                    // Branch 7
                                    //{
                                        if ((uint)slice.Length < 3 ||
                                            !slice.Slice(1).StartsWith("ol", StringComparison.OrdinalIgnoreCase)) // Match the string "ol" (ordinal case-insensitive)
                                        {
                                            goto AlternationBranch9;
                                        }
                                        
                                        Utilities.StackPush(ref base.runstack!, ref stackpos, 7, alternation_starting_pos1);
                                        pos += 3;
                                        slice = inputSpan.Slice(pos);
                                        goto AlternationMatch1;
                                        
                                        AlternationBranch9:
                                        pos = alternation_starting_pos1;
                                        slice = inputSpan.Slice(pos);
                                    //}
                                    
                                    // Branch 8
                                    //{
                                        if ((uint)slice.Length < 3 ||
                                            !slice.Slice(1).StartsWith("li", StringComparison.OrdinalIgnoreCase)) // Match the string "li" (ordinal case-insensitive)
                                        {
                                            goto AlternationBranch10;
                                        }
                                        
                                        Utilities.StackPush(ref base.runstack!, ref stackpos, 8, alternation_starting_pos1);
                                        pos += 3;
                                        slice = inputSpan.Slice(pos);
                                        goto AlternationMatch1;
                                        
                                        AlternationBranch10:
                                        pos = alternation_starting_pos1;
                                        slice = inputSpan.Slice(pos);
                                    //}
                                    
                                    // Branch 9
                                    //{
                                        if ((uint)slice.Length < 3 ||
                                            ((slice[1] | 0x20) != 'h') || // Match a character in the set [Hh].
                                            !char.IsBetween(slice[2], '1', '6')) // Match a character in the set [1-6].
                                        {
                                            goto AlternationBranch11;
                                        }
                                        
                                        Utilities.StackPush(ref base.runstack!, ref stackpos, 9, alternation_starting_pos1);
                                        pos += 3;
                                        slice = inputSpan.Slice(pos);
                                        goto AlternationMatch1;
                                        
                                        AlternationBranch11:
                                        pos = alternation_starting_pos1;
                                        slice = inputSpan.Slice(pos);
                                    //}
                                    
                                    // Branch 10
                                    //{
                                        if ((uint)slice.Length < 3 ||
                                            !slice.Slice(1).StartsWith("br", StringComparison.OrdinalIgnoreCase)) // Match the string "br" (ordinal case-insensitive)
                                        {
                                            goto AlternationBranch12;
                                        }
                                        
                                        Utilities.StackPush(ref base.runstack!, ref stackpos, 10, alternation_starting_pos1);
                                        pos += 3;
                                        slice = inputSpan.Slice(pos);
                                        goto AlternationMatch1;
                                        
                                        AlternationBranch12:
                                        pos = alternation_starting_pos1;
                                        slice = inputSpan.Slice(pos);
                                    //}
                                    
                                    // Branch 11
                                    //{
                                        if ((uint)slice.Length < 3 ||
                                            !slice.Slice(1).StartsWith("hr", StringComparison.OrdinalIgnoreCase)) // Match the string "hr" (ordinal case-insensitive)
                                        {
                                            goto AlternationBranch1;
                                        }
                                        
                                        Utilities.StackPush(ref base.runstack!, ref stackpos, 11, alternation_starting_pos1);
                                        pos += 3;
                                        slice = inputSpan.Slice(pos);
                                        goto AlternationMatch1;
                                    //}
                                    
                                    AlternationBacktrack1:
                                    base.CheckTimeout();
                                    
                                    alternation_starting_pos1 = base.runstack![--stackpos];
                                    switch (base.runstack![--stackpos])
                                    {
                                        case 0:
                                            goto AlternationBranch2;
                                        case 1:
                                            goto AlternationBranch3;
                                        case 2:
                                            goto AlternationBranch4;
                                        case 3:
                                            goto AlternationBranch5;
                                        case 4:
                                            goto AlternationBranch6;
                                        case 5:
                                            goto AlternationBranch7;
                                        case 6:
                                            goto AlternationBranch8;
                                        case 7:
                                            goto AlternationBranch9;
                                        case 8:
                                            goto AlternationBranch10;
                                        case 9:
                                            goto AlternationBranch11;
                                        case 10:
                                            goto AlternationBranch12;
                                        case 11:
                                            goto AlternationBranch1;
                                    }
                                    
                                    AlternationMatch1:;
                                //}
                                
                                // Loop greedily any number of times.
                                //{
                                    loop_iteration1 = 0;
                                    
                                    LoopBody1:
                                    Utilities.StackPush(ref base.runstack!, ref stackpos, pos);
                                    
                                    loop_iteration1++;
                                    
                                    // Match a whitespace character atomically at least once.
                                    {
                                        int iteration = 0;
                                        while ((uint)iteration < (uint)slice.Length && char.IsWhiteSpace(slice[iteration]))
                                        {
                                            iteration++;
                                        }
                                        
                                        if (iteration == 0)
                                        {
                                            goto LoopIterationNoMatch1;
                                        }
                                        
                                        slice = slice.Slice(iteration);
                                        pos += iteration;
                                    }
                                    
                                    // Match with 4 alternative expressions.
                                    //{
                                        if (slice.IsEmpty)
                                        {
                                            goto LoopIterationNoMatch1;
                                        }
                                        
                                        switch (slice[0])
                                        {
                                            case 'C' or 'c':
                                                if ((uint)slice.Length < 5 ||
                                                    !slice.Slice(1).StartsWith("lass", StringComparison.OrdinalIgnoreCase)) // Match the string "lass" (ordinal case-insensitive)
                                                {
                                                    goto LoopIterationNoMatch1;
                                                }
                                                
                                                pos += 5;
                                                slice = inputSpan.Slice(pos);
                                                break;
                                                
                                            case 'I' or 'i':
                                                // Match a character in the set [Dd].
                                                if ((uint)slice.Length < 2 || ((slice[1] | 0x20) != 'd'))
                                                {
                                                    goto LoopIterationNoMatch1;
                                                }
                                                
                                                pos += 2;
                                                slice = inputSpan.Slice(pos);
                                                break;
                                                
                                            case 'T' or 't':
                                                if ((uint)slice.Length < 5 ||
                                                    !slice.Slice(1).StartsWith("itle", StringComparison.OrdinalIgnoreCase)) // Match the string "itle" (ordinal case-insensitive)
                                                {
                                                    goto LoopIterationNoMatch1;
                                                }
                                                
                                                pos += 5;
                                                slice = inputSpan.Slice(pos);
                                                break;
                                                
                                            case 'D' or 'd':
                                                if ((uint)slice.Length < 5 ||
                                                    !slice.Slice(1).StartsWith("ata-", StringComparison.OrdinalIgnoreCase)) // Match the string "ata-" (ordinal case-insensitive)
                                                {
                                                    goto LoopIterationNoMatch1;
                                                }
                                                
                                                // Match a character in the set [-0-9A-Za-z\u212A] atomically at least once.
                                                {
                                                    pos += 5;
                                                    slice = inputSpan.Slice(pos);
                                                    int iteration1 = 0;
                                                    while ((uint)iteration1 < (uint)slice.Length && ((ch = slice[iteration1]) < 128 ? ("\0\0 Ͽ\ufffe߿\ufffe߿"[ch >> 4] & (1 << (ch & 0xF))) != 0 : RegexRunner.CharInClass((char)ch, "\0\n\0-.0:A[a{KÅ")))
                                                    {
                                                        iteration1++;
                                                    }
                                                    
                                                    if (iteration1 == 0)
                                                    {
                                                        goto LoopIterationNoMatch1;
                                                    }
                                                    
                                                    slice = slice.Slice(iteration1);
                                                    pos += iteration1;
                                                }
                                                
                                                break;
                                                
                                            default:
                                                goto LoopIterationNoMatch1;
                                        }
                                    //}
                                    
                                    // Match a whitespace character atomically any number of times.
                                    {
                                        int iteration2 = 0;
                                        while ((uint)iteration2 < (uint)slice.Length && char.IsWhiteSpace(slice[iteration2]))
                                        {
                                            iteration2++;
                                        }
                                        
                                        slice = slice.Slice(iteration2);
                                        pos += iteration2;
                                    }
                                    
                                    // Match '='.
                                    if (slice.IsEmpty || slice[0] != '=')
                                    {
                                        goto LoopIterationNoMatch1;
                                    }
                                    
                                    // Match a whitespace character atomically any number of times.
                                    {
                                        int iteration3 = 1;
                                        while ((uint)iteration3 < (uint)slice.Length && char.IsWhiteSpace(slice[iteration3]))
                                        {
                                            iteration3++;
                                        }
                                        
                                        slice = slice.Slice(iteration3);
                                        pos += iteration3;
                                    }
                                    
                                    // Match with 2 alternative expressions.
                                    //{
                                        if (slice.IsEmpty)
                                        {
                                            goto LoopIterationNoMatch1;
                                        }
                                        
                                        switch (slice[0])
                                        {
                                            case '"':
                                                // Match a character in the set [^"<>] atomically any number of times.
                                                {
                                                    int iteration4 = slice.Slice(1).IndexOfAny('"', '<', '>');
                                                    if (iteration4 < 0)
                                                    {
                                                        iteration4 = slice.Length - 1;
                                                    }
                                                    
                                                    slice = slice.Slice(iteration4);
                                                    pos += iteration4;
                                                }
                                                
                                                // Match '"'.
                                                if ((uint)slice.Length < 2 || slice[1] != '"')
                                                {
                                                    goto LoopIterationNoMatch1;
                                                }
                                                
                                                pos += 2;
                                                slice = inputSpan.Slice(pos);
                                                break;
                                                
                                            case '\'':
                                                // Match a character in the set [^'<>] atomically any number of times.
                                                {
                                                    int iteration5 = slice.Slice(1).IndexOfAny('\'', '<', '>');
                                                    if (iteration5 < 0)
                                                    {
                                                        iteration5 = slice.Length - 1;
                                                    }
                                                    
                                                    slice = slice.Slice(iteration5);
                                                    pos += iteration5;
                                                }
                                                
                                                // Match '\''.
                                                if ((uint)slice.Length < 2 || slice[1] != '\'')
                                                {
                                                    goto LoopIterationNoMatch1;
                                                }
                                                
                                                pos += 2;
                                                slice = inputSpan.Slice(pos);
                                                break;
                                                
                                            default:
                                                goto LoopIterationNoMatch1;
                                        }
                                    //}
                                    
                                    
                                    // The loop has no upper bound. Continue iterating greedily.
                                    goto LoopBody1;
                                    
                                    // The loop iteration failed. Put state back to the way it was before the iteration.
                                    LoopIterationNoMatch1:
                                    if (--loop_iteration1 < 0)
                                    {
                                        // Unable to match the remainder of the expression after exhausting the loop.
                                        goto AlternationBacktrack1;
                                    }
                                    pos = base.runstack![--stackpos];
                                    slice = inputSpan.Slice(pos);
                                    LoopEnd1:
                                    
                                    Utilities.StackPush(ref base.runstack!, ref stackpos, loop_iteration1);
                                    goto LoopSkipBacktrack;
                                    
                                    LoopBacktrack:
                                    loop_iteration1 = base.runstack![--stackpos];
                                    base.CheckTimeout();
                                    
                                    goto LoopIterationNoMatch1;
                                    
                                    LoopSkipBacktrack:;
                                //}
                                
                                // Match a whitespace character atomically any number of times.
                                {
                                    int iteration6 = 0;
                                    while ((uint)iteration6 < (uint)slice.Length && char.IsWhiteSpace(slice[iteration6]))
                                    {
                                        iteration6++;
                                    }
                                    
                                    slice = slice.Slice(iteration6);
                                    pos += iteration6;
                                }
                                
                                // Match '/' atomically, optionally.
                                {
                                    if (!slice.IsEmpty && slice[0] == '/')
                                    {
                                        slice = slice.Slice(1);
                                        pos++;
                                    }
                                }
                                
                                // Match '>'.
                                if (slice.IsEmpty || slice[0] != '>')
                                {
                                    goto LoopBacktrack;
                                }
                                
                                Utilities.StackPush(ref base.runstack!, ref stackpos, 1, alternation_starting_pos);
                                pos++;
                                slice = inputSpan.Slice(pos);
                                goto AlternationMatch;
                                
                                AlternationBranch1:
                                pos = alternation_starting_pos;
                                slice = inputSpan.Slice(pos);
                            //}
                            
                            // Branch 2
                            //{
                                // Match the string "</".
                                if (!slice.StartsWith("</"))
                                {
                                    goto LoopIterationNoMatch;
                                }
                                
                                // Match with 10 alternative expressions.
                                //{
                                    int alternation_starting_pos2 = pos;
                                    
                                    // Branch 0
                                    //{
                                        // Match a character in the set [Pp].
                                        if ((uint)slice.Length < 3 || ((slice[2] | 0x20) != 'p'))
                                        {
                                            goto AlternationBranch13;
                                        }
                                        
                                        Utilities.StackPush(ref base.runstack!, ref stackpos, 0, alternation_starting_pos2);
                                        pos += 3;
                                        slice = inputSpan.Slice(pos);
                                        goto AlternationMatch2;
                                        
                                        AlternationBranch13:
                                        pos = alternation_starting_pos2;
                                        slice = inputSpan.Slice(pos);
                                    //}
                                    
                                    // Branch 1
                                    //{
                                        if ((uint)slice.Length < 5 ||
                                            !slice.Slice(2).StartsWith("div", StringComparison.OrdinalIgnoreCase)) // Match the string "div" (ordinal case-insensitive)
                                        {
                                            goto AlternationBranch14;
                                        }
                                        
                                        Utilities.StackPush(ref base.runstack!, ref stackpos, 1, alternation_starting_pos2);
                                        pos += 5;
                                        slice = inputSpan.Slice(pos);
                                        goto AlternationMatch2;
                                        
                                        AlternationBranch14:
                                        pos = alternation_starting_pos2;
                                        slice = inputSpan.Slice(pos);
                                    //}
                                    
                                    // Branch 2
                                    //{
                                        if ((uint)slice.Length < 6 ||
                                            !slice.Slice(2).StartsWith("span", StringComparison.OrdinalIgnoreCase)) // Match the string "span" (ordinal case-insensitive)
                                        {
                                            goto AlternationBranch15;
                                        }
                                        
                                        Utilities.StackPush(ref base.runstack!, ref stackpos, 2, alternation_starting_pos2);
                                        pos += 6;
                                        slice = inputSpan.Slice(pos);
                                        goto AlternationMatch2;
                                        
                                        AlternationBranch15:
                                        pos = alternation_starting_pos2;
                                        slice = inputSpan.Slice(pos);
                                    //}
                                    
                                    // Branch 3
                                    //{
                                        if ((uint)slice.Length < 8 ||
                                            !slice.Slice(2).StartsWith("strong", StringComparison.OrdinalIgnoreCase)) // Match the string "strong" (ordinal case-insensitive)
                                        {
                                            goto AlternationBranch16;
                                        }
                                        
                                        Utilities.StackPush(ref base.runstack!, ref stackpos, 3, alternation_starting_pos2);
                                        pos += 8;
                                        slice = inputSpan.Slice(pos);
                                        goto AlternationMatch2;
                                        
                                        AlternationBranch16:
                                        pos = alternation_starting_pos2;
                                        slice = inputSpan.Slice(pos);
                                    //}
                                    
                                    // Branch 4
                                    //{
                                        if ((uint)slice.Length < 4 ||
                                            !slice.Slice(2).StartsWith("em", StringComparison.OrdinalIgnoreCase)) // Match the string "em" (ordinal case-insensitive)
                                        {
                                            goto AlternationBranch17;
                                        }
                                        
                                        Utilities.StackPush(ref base.runstack!, ref stackpos, 4, alternation_starting_pos2);
                                        pos += 4;
                                        slice = inputSpan.Slice(pos);
                                        goto AlternationMatch2;
                                        
                                        AlternationBranch17:
                                        pos = alternation_starting_pos2;
                                        slice = inputSpan.Slice(pos);
                                    //}
                                    
                                    // Branch 5
                                    //{
                                        // Match a character in the set [BIUbiu].
                                        if ((uint)slice.Length < 3 || ((long)((0x8100100081001000UL << (int)(charMinusLowUInt64 = (uint)slice[2] - 'B')) & (charMinusLowUInt64 - 64)) >= 0))
                                        {
                                            goto AlternationBranch18;
                                        }
                                        
                                        Utilities.StackPush(ref base.runstack!, ref stackpos, 5, alternation_starting_pos2);
                                        pos += 3;
                                        slice = inputSpan.Slice(pos);
                                        goto AlternationMatch2;
                                        
                                        AlternationBranch18:
                                        pos = alternation_starting_pos2;
                                        slice = inputSpan.Slice(pos);
                                    //}
                                    
                                    // Branch 6
                                    //{
                                        if ((uint)slice.Length < 4 ||
                                            !slice.Slice(2).StartsWith("ul", StringComparison.OrdinalIgnoreCase)) // Match the string "ul" (ordinal case-insensitive)
                                        {
                                            goto AlternationBranch19;
                                        }
                                        
                                        Utilities.StackPush(ref base.runstack!, ref stackpos, 6, alternation_starting_pos2);
                                        pos += 4;
                                        slice = inputSpan.Slice(pos);
                                        goto AlternationMatch2;
                                        
                                        AlternationBranch19:
                                        pos = alternation_starting_pos2;
                                        slice = inputSpan.Slice(pos);
                                    //}
                                    
                                    // Branch 7
                                    //{
                                        if ((uint)slice.Length < 4 ||
                                            !slice.Slice(2).StartsWith("ol", StringComparison.OrdinalIgnoreCase)) // Match the string "ol" (ordinal case-insensitive)
                                        {
                                            goto AlternationBranch20;
                                        }
                                        
                                        Utilities.StackPush(ref base.runstack!, ref stackpos, 7, alternation_starting_pos2);
                                        pos += 4;
                                        slice = inputSpan.Slice(pos);
                                        goto AlternationMatch2;
                                        
                                        AlternationBranch20:
                                        pos = alternation_starting_pos2;
                                        slice = inputSpan.Slice(pos);
                                    //}
                                    
                                    // Branch 8
                                    //{
                                        if ((uint)slice.Length < 4 ||
                                            !slice.Slice(2).StartsWith("li", StringComparison.OrdinalIgnoreCase)) // Match the string "li" (ordinal case-insensitive)
                                        {
                                            goto AlternationBranch21;
                                        }
                                        
                                        Utilities.StackPush(ref base.runstack!, ref stackpos, 8, alternation_starting_pos2);
                                        pos += 4;
                                        slice = inputSpan.Slice(pos);
                                        goto AlternationMatch2;
                                        
                                        AlternationBranch21:
                                        pos = alternation_starting_pos2;
                                        slice = inputSpan.Slice(pos);
                                    //}
                                    
                                    // Branch 9
                                    //{
                                        if ((uint)slice.Length < 4 ||
                                            ((slice[2] | 0x20) != 'h') || // Match a character in the set [Hh].
                                            !char.IsBetween(slice[3], '1', '6')) // Match a character in the set [1-6].
                                        {
                                            goto LoopIterationNoMatch;
                                        }
                                        
                                        Utilities.StackPush(ref base.runstack!, ref stackpos, 9, alternation_starting_pos2);
                                        pos += 4;
                                        slice = inputSpan.Slice(pos);
                                        goto AlternationMatch2;
                                    //}
                                    
                                    AlternationBacktrack2:
                                    base.CheckTimeout();
                                    
                                    alternation_starting_pos2 = base.runstack![--stackpos];
                                    switch (base.runstack![--stackpos])
                                    {
                                        case 0:
                                            goto AlternationBranch13;
                                        case 1:
                                            goto AlternationBranch14;
                                        case 2:
                                            goto AlternationBranch15;
                                        case 3:
                                            goto AlternationBranch16;
                                        case 4:
                                            goto AlternationBranch17;
                                        case 5:
                                            goto AlternationBranch18;
                                        case 6:
                                            goto AlternationBranch19;
                                        case 7:
                                            goto AlternationBranch20;
                                        case 8:
                                            goto AlternationBranch21;
                                        case 9:
                                            goto LoopIterationNoMatch;
                                    }
                                    
                                    AlternationMatch2:;
                                //}
                                
                                // Match '>'.
                                if (slice.IsEmpty || slice[0] != '>')
                                {
                                    goto AlternationBacktrack2;
                                }
                                
                                Utilities.StackPush(ref base.runstack!, ref stackpos, 2, alternation_starting_pos);
                                pos++;
                                slice = inputSpan.Slice(pos);
                                goto AlternationMatch;
                            //}
                            
                            AlternationBacktrack:
                            base.CheckTimeout();
                            
                            alternation_starting_pos = base.runstack![--stackpos];
                            switch (base.runstack![--stackpos])
                            {
                                case 0:
                                    goto AlternationBranch;
                                case 1:
                                    goto LoopBacktrack;
                                case 2:
                                    goto AlternationBacktrack2;
                            }
                            
                            AlternationMatch:;
                        //}
                        
                        
                        // The loop has no upper bound. Continue iterating greedily.
                        goto LoopBody;
                        
                        // The loop iteration failed. Put state back to the way it was before the iteration.
                        LoopIterationNoMatch:
                        if (--loop_iteration < 0)
                        {
                            // Unable to match the remainder of the expression after exhausting the loop.
                            return false; // The input didn't match.
                        }
                        pos = base.runstack![--stackpos];
                        slice = inputSpan.Slice(pos);
                        goto LoopEnd;
                        
                        LoopBacktrack1:
                        base.CheckTimeout();
                        
                        if (loop_iteration == 0)
                        {
                            // No iterations of the loop remain to backtrack into. Fail the loop.
                            return false; // The input didn't match.
                        }
                        goto AlternationBacktrack;
                        LoopEnd:;
                    //}
                    
                    // Match if at the end of the string or if before an ending newline.
                    if (pos < inputSpan.Length - 1 || ((uint)pos < (uint)inputSpan.Length && inputSpan[pos] != '\n'))
                    {
                        goto LoopBacktrack1;
                    }
                    
                    // The input matched.
                    base.runtextpos = pos;
                    base.Capture(0, matchStart, pos);
                    return true;
                }
            }
        }

    }
    
    /// <summary>Custom <see cref="Regex"/>-derived type for the RegExWithTagAndStyle method.</summary>
    [GeneratedCodeAttribute("System.Text.RegularExpressions.Generator", "8.0.14.36720")]
    file sealed class RegExWithTagAndStyle_5 : Regex
    {
        /// <summary>Cached, thread-safe singleton instance.</summary>
        internal static readonly RegExWithTagAndStyle_5 Instance = new();
    
        /// <summary>Initializes the instance.</summary>
        private RegExWithTagAndStyle_5()
        {
            base.pattern = "^<(?<tag>[a-zA-Z][a-zA-Z0-9]*?)(?:\\s+(?<attribute>[a-zA-Z][a-zA-Z0-9\\-]*?)\\s*=\\s*(?<quote>[\"'])(?<attributevalue>.*?)\\k<quote>)?(?:\\s*/)?>(?<content>[\\s\\S]*?)(?:</\\k<tag>>)?$";
            base.roptions = RegexOptions.ExplicitCapture | RegexOptions.Singleline;
            base.internalMatchTimeout = TimeSpan.FromMilliseconds(300);
            base.factory = new RunnerFactory();
            base.CapNames = new Hashtable { { "0", 0 } ,  { "attribute", 2 } ,  { "attributevalue", 4 } ,  { "content", 5 } ,  { "quote", 3 } ,  { "tag", 1 }  };
            base.capslist = new string[] {"0", "tag", "attribute", "quote", "attributevalue", "content" };
            base.capsize = 6;
        }
            
        /// <summary>Provides a factory for creating <see cref="RegexRunner"/> instances to be used by methods on <see cref="Regex"/>.</summary>
        private sealed class RunnerFactory : RegexRunnerFactory
        {
            /// <summary>Creates an instance of a <see cref="RegexRunner"/> used by methods on <see cref="Regex"/>.</summary>
            protected override RegexRunner CreateInstance() => new Runner();
        
            /// <summary>Provides the runner that contains the custom logic implementing the specified regular expression.</summary>
            private sealed class Runner : RegexRunner
            {
                /// <summary>Scan the <paramref name="inputSpan"/> starting from base.runtextstart for the next match.</summary>
                /// <param name="inputSpan">The text being scanned by the regular expression.</param>
                protected override void Scan(ReadOnlySpan<char> inputSpan)
                {
                    // The pattern is anchored.  Validate the current position and try to match at it only.
                    if (TryFindNextPossibleStartingPosition(inputSpan) && !TryMatchAtCurrentPosition(inputSpan))
                    {
                        base.runtextpos = inputSpan.Length;
                    }
                }
        
                /// <summary>Search <paramref name="inputSpan"/> starting from base.runtextpos for the next location a match could possibly start.</summary>
                /// <param name="inputSpan">The text being scanned by the regular expression.</param>
                /// <returns>true if a possible match was found; false if no more matches are possible.</returns>
                private bool TryFindNextPossibleStartingPosition(ReadOnlySpan<char> inputSpan)
                {
                    int pos = base.runtextpos;
                    
                    // Any possible match is at least 3 characters.
                    if (pos <= inputSpan.Length - 3)
                    {
                        // The pattern leads with a beginning (\A) anchor.
                        if (pos == 0)
                        {
                            return true;
                        }
                    }
                    
                    // No match found.
                    base.runtextpos = inputSpan.Length;
                    return false;
                }
        
                /// <summary>Determine whether <paramref name="inputSpan"/> at base.runtextpos is a match for the regular expression.</summary>
                /// <param name="inputSpan">The text being scanned by the regular expression.</param>
                /// <returns>true if the regular expression matches at the current position; otherwise, false.</returns>
                private bool TryMatchAtCurrentPosition(ReadOnlySpan<char> inputSpan)
                {
                    int pos = base.runtextpos;
                    int matchStart = pos;
                    char ch;
                    int capture_starting_pos = 0;
                    int capture_starting_pos4 = 0;
                    int lazyloop_capturepos = 0;
                    int lazyloop_capturepos1 = 0;
                    int lazyloop_capturepos2 = 0;
                    int lazyloop_capturepos3 = 0;
                    int lazyloop_pos = 0;
                    int lazyloop_pos1 = 0;
                    int lazyloop_pos2 = 0;
                    int lazyloop_pos3 = 0;
                    int loop_iteration = 0;
                    int loop_iteration1 = 0;
                    int loop_iteration2 = 0;
                    int matchLength = 0;
                    int stackpos = 0;
                    ReadOnlySpan<char> slice = inputSpan.Slice(pos);
                    
                    // Match if at the beginning of the string.
                    if (pos != 0)
                    {
                        UncaptureUntil(0);
                        return false; // The input didn't match.
                    }
                    
                    // Match '<'.
                    if (slice.IsEmpty || slice[0] != '<')
                    {
                        UncaptureUntil(0);
                        return false; // The input didn't match.
                    }
                    
                    // "tag" capture group.
                    //{
                        pos++;
                        slice = inputSpan.Slice(pos);
                        capture_starting_pos = pos;
                        
                        // Match a character in the set [A-Za-z].
                        if (slice.IsEmpty || !char.IsAsciiLetter(slice[0]))
                        {
                            UncaptureUntil(0);
                            return false; // The input didn't match.
                        }
                        
                        // Match a character in the set [0-9A-Za-z] lazily any number of times.
                        //{
                            pos++;
                            slice = inputSpan.Slice(pos);
                            lazyloop_pos = pos;
                            goto LazyLoopEnd;
                            
                            LazyLoopBacktrack:
                            UncaptureUntil(lazyloop_capturepos);
                            base.CheckTimeout();
                            
                            pos = lazyloop_pos;
                            slice = inputSpan.Slice(pos);
                            if (slice.IsEmpty || !char.IsAsciiLetterOrDigit(slice[0]))
                            {
                                UncaptureUntil(0);
                                return false; // The input didn't match.
                            }
                            pos++;
                            slice = inputSpan.Slice(pos);
                            lazyloop_pos = pos;
                            
                            LazyLoopEnd:
                            lazyloop_capturepos = base.Crawlpos();
                        //}
                        
                        base.Capture(1, capture_starting_pos, pos);
                        
                        goto CaptureSkipBacktrack;
                        
                        CaptureBacktrack:
                        goto LazyLoopBacktrack;
                        
                        CaptureSkipBacktrack:;
                    //}
                    
                    // Optional (greedy).
                    //{
                        loop_iteration = 0;
                        
                        LoopBody:
                        Utilities.StackPush(ref base.runstack!, ref stackpos, base.Crawlpos(), pos);
                        
                        loop_iteration++;
                        
                        // Match a whitespace character atomically at least once.
                        {
                            int iteration = 0;
                            while ((uint)iteration < (uint)slice.Length && char.IsWhiteSpace(slice[iteration]))
                            {
                                iteration++;
                            }
                            
                            if (iteration == 0)
                            {
                                goto LoopIterationNoMatch;
                            }
                            
                            slice = slice.Slice(iteration);
                            pos += iteration;
                        }
                        
                        // "attribute" capture group.
                        //{
                            int capture_starting_pos1 = pos;
                            
                            // Match a character in the set [A-Za-z].
                            if (slice.IsEmpty || !char.IsAsciiLetter(slice[0]))
                            {
                                goto LoopIterationNoMatch;
                            }
                            
                            // Match a character in the set [-0-9A-Za-z] lazily any number of times.
                            //{
                                pos++;
                                slice = inputSpan.Slice(pos);
                                lazyloop_pos1 = pos;
                                goto LazyLoopEnd1;
                                
                                LazyLoopBacktrack1:
                                UncaptureUntil(lazyloop_capturepos1);
                                base.CheckTimeout();
                                
                                pos = lazyloop_pos1;
                                slice = inputSpan.Slice(pos);
                                if (slice.IsEmpty || ((ch = slice[0]) >= '{' || ("\0\0 Ͽ\ufffe߿\ufffe߿"[ch >> 4] & (1 << (ch & 0xF))) == 0))
                                {
                                    goto LoopIterationNoMatch;
                                }
                                pos++;
                                slice = inputSpan.Slice(pos);
                                lazyloop_pos1 = pos;
                                
                                LazyLoopEnd1:
                                lazyloop_capturepos1 = base.Crawlpos();
                                
                                Utilities.StackPush(ref base.runstack!, ref stackpos, lazyloop_pos1, lazyloop_capturepos1);
                                goto LazyLoopSkipBacktrack;
                                
                                CharLazyBacktrack:
                                Utilities.StackPop(base.runstack!, ref stackpos, out lazyloop_capturepos1, out lazyloop_pos1);
                                goto LazyLoopBacktrack1;
                                
                                LazyLoopSkipBacktrack:;
                            //}
                            
                            base.Capture(2, capture_starting_pos1, pos);
                            
                            Utilities.StackPush(ref base.runstack!, ref stackpos, capture_starting_pos1);
                            goto CaptureSkipBacktrack1;
                            
                            CaptureBacktrack1:
                            capture_starting_pos1 = base.runstack![--stackpos];
                            goto CharLazyBacktrack;
                            
                            CaptureSkipBacktrack1:;
                        //}
                        
                        // Match a whitespace character atomically any number of times.
                        {
                            int iteration1 = 0;
                            while ((uint)iteration1 < (uint)slice.Length && char.IsWhiteSpace(slice[iteration1]))
                            {
                                iteration1++;
                            }
                            
                            slice = slice.Slice(iteration1);
                            pos += iteration1;
                        }
                        
                        // Match '='.
                        if (slice.IsEmpty || slice[0] != '=')
                        {
                            goto CaptureBacktrack1;
                        }
                        
                        // Match a whitespace character atomically any number of times.
                        {
                            int iteration2 = 1;
                            while ((uint)iteration2 < (uint)slice.Length && char.IsWhiteSpace(slice[iteration2]))
                            {
                                iteration2++;
                            }
                            
                            slice = slice.Slice(iteration2);
                            pos += iteration2;
                        }
                        
                        // "quote" capture group.
                        {
                            int capture_starting_pos2 = pos;
                            
                            // Match a character in the set ["'].
                            if (slice.IsEmpty || (((ch = slice[0]) != '"') & (ch != '\'')))
                            {
                                goto CaptureBacktrack1;
                            }
                            
                            pos++;
                            slice = inputSpan.Slice(pos);
                            base.Capture(3, capture_starting_pos2, pos);
                        }
                        
                        // "attributevalue" capture group.
                        //{
                            int capture_starting_pos3 = pos;
                            
                            // Match any character lazily any number of times.
                            //{
                                lazyloop_pos2 = pos;
                                goto LazyLoopEnd2;
                                
                                LazyLoopBacktrack2:
                                UncaptureUntil(lazyloop_capturepos2);
                                base.CheckTimeout();
                                
                                pos = lazyloop_pos2;
                                slice = inputSpan.Slice(pos);
                                if (slice.IsEmpty || false)
                                {
                                    goto CaptureBacktrack1;
                                }
                                pos++;
                                slice = inputSpan.Slice(pos);
                                lazyloop_pos2 = pos;
                                
                                LazyLoopEnd2:
                                lazyloop_capturepos2 = base.Crawlpos();
                                
                                Utilities.StackPush(ref base.runstack!, ref stackpos, lazyloop_pos2, lazyloop_capturepos2);
                                goto LazyLoopSkipBacktrack1;
                                
                                CharLazyBacktrack1:
                                Utilities.StackPop(base.runstack!, ref stackpos, out lazyloop_capturepos2, out lazyloop_pos2);
                                goto LazyLoopBacktrack2;
                                
                                LazyLoopSkipBacktrack1:;
                            //}
                            
                            base.Capture(4, capture_starting_pos3, pos);
                            
                            Utilities.StackPush(ref base.runstack!, ref stackpos, capture_starting_pos3);
                            goto CaptureSkipBacktrack2;
                            
                            CaptureBacktrack2:
                            capture_starting_pos3 = base.runstack![--stackpos];
                            goto CharLazyBacktrack1;
                            
                            CaptureSkipBacktrack2:;
                        //}
                        
                        // Match the same text as matched by the "quote" capture group.
                        {
                            // If the "quote" capture group hasn't matched, the backreference doesn't match.
                            if (!base.IsMatched(3))
                            {
                                goto CaptureBacktrack2;
                            }
                            
                            // Get the captured text.  If it doesn't match at the current position, the backreference doesn't match.
                            matchLength = base.MatchLength(3);
                            if (slice.Length < matchLength || 
                                !inputSpan.Slice(base.MatchIndex(3), matchLength).SequenceEqual(slice.Slice(0, matchLength)))
                            {
                                goto CaptureBacktrack2;
                            }
                            
                            pos += matchLength;
                            slice = inputSpan.Slice(pos);
                        }
                        
                        
                        // The loop has an upper bound of 1. Continue iterating greedily if it hasn't yet been reached.
                        if (loop_iteration == 0)
                        {
                            goto LoopBody;
                        }
                        goto LoopEnd;
                        
                        // The loop iteration failed. Put state back to the way it was before the iteration.
                        LoopIterationNoMatch:
                        if (--loop_iteration < 0)
                        {
                            // Unable to match the remainder of the expression after exhausting the loop.
                            goto CaptureBacktrack;
                        }
                        pos = base.runstack![--stackpos];
                        UncaptureUntil(base.runstack![--stackpos]);
                        slice = inputSpan.Slice(pos);
                        goto LoopEnd;
                        
                        LoopBacktrack:
                        base.CheckTimeout();
                        
                        if (loop_iteration == 0)
                        {
                            // No iterations of the loop remain to backtrack into. Fail the loop.
                            goto CaptureBacktrack;
                        }
                        goto CaptureBacktrack2;
                        LoopEnd:;
                    //}
                    
                    // Optional (greedy).
                    //{
                        loop_iteration1 = 0;
                        
                        LoopBody1:
                        Utilities.StackPush(ref base.runstack!, ref stackpos, base.Crawlpos(), pos);
                        
                        loop_iteration1++;
                        
                        // Match a whitespace character atomically any number of times.
                        {
                            int iteration3 = 0;
                            while ((uint)iteration3 < (uint)slice.Length && char.IsWhiteSpace(slice[iteration3]))
                            {
                                iteration3++;
                            }
                            
                            slice = slice.Slice(iteration3);
                            pos += iteration3;
                        }
                        
                        // Match '/'.
                        if (slice.IsEmpty || slice[0] != '/')
                        {
                            goto LoopIterationNoMatch1;
                        }
                        
                        pos++;
                        slice = inputSpan.Slice(pos);
                        
                        // The loop has an upper bound of 1. Continue iterating greedily if it hasn't yet been reached.
                        if (loop_iteration1 == 0)
                        {
                            goto LoopBody1;
                        }
                        goto LoopEnd1;
                        
                        // The loop iteration failed. Put state back to the way it was before the iteration.
                        LoopIterationNoMatch1:
                        if (--loop_iteration1 < 0)
                        {
                            // Unable to match the remainder of the expression after exhausting the loop.
                            goto LoopBacktrack;
                        }
                        pos = base.runstack![--stackpos];
                        UncaptureUntil(base.runstack![--stackpos]);
                        slice = inputSpan.Slice(pos);
                        LoopEnd1:;
                    //}
                    
                    // Match '>'.
                    if (slice.IsEmpty || slice[0] != '>')
                    {
                        goto LoopIterationNoMatch1;
                    }
                    
                    // "content" capture group.
                    //{
                        pos++;
                        slice = inputSpan.Slice(pos);
                        capture_starting_pos4 = pos;
                        
                        // Match any character lazily any number of times.
                        //{
                            lazyloop_pos3 = pos;
                            goto LazyLoopEnd3;
                            
                            LazyLoopBacktrack3:
                            UncaptureUntil(lazyloop_capturepos3);
                            base.CheckTimeout();
                            
                            pos = lazyloop_pos3;
                            slice = inputSpan.Slice(pos);
                            if (slice.IsEmpty || false)
                            {
                                goto LoopIterationNoMatch1;
                            }
                            pos++;
                            slice = inputSpan.Slice(pos);
                            lazyloop_pos3 = pos;
                            
                            LazyLoopEnd3:
                            lazyloop_capturepos3 = base.Crawlpos();
                        //}
                        
                        base.Capture(5, capture_starting_pos4, pos);
                        
                        goto CaptureSkipBacktrack3;
                        
                        CaptureBacktrack3:
                        goto LazyLoopBacktrack3;
                        
                        CaptureSkipBacktrack3:;
                    //}
                    
                    // Optional (greedy).
                    //{
                        loop_iteration2 = 0;
                        
                        LoopBody2:
                        Utilities.StackPush(ref base.runstack!, ref stackpos, base.Crawlpos(), pos);
                        
                        loop_iteration2++;
                        
                        // Match the string "</".
                        if (!slice.StartsWith("</"))
                        {
                            goto LoopIterationNoMatch2;
                        }
                        
                        // Match the same text as matched by the "tag" capture group.
                        {
                            pos += 2;
                            slice = inputSpan.Slice(pos);
                            
                            // If the "tag" capture group hasn't matched, the backreference doesn't match.
                            if (!base.IsMatched(1))
                            {
                                goto LoopIterationNoMatch2;
                            }
                            
                            // Get the captured text.  If it doesn't match at the current position, the backreference doesn't match.
                            matchLength = base.MatchLength(1);
                            if (slice.Length < matchLength || 
                                !inputSpan.Slice(base.MatchIndex(1), matchLength).SequenceEqual(slice.Slice(0, matchLength)))
                            {
                                goto LoopIterationNoMatch2;
                            }
                            
                            pos += matchLength;
                            slice = inputSpan.Slice(pos);
                        }
                        
                        // Match '>'.
                        if (slice.IsEmpty || slice[0] != '>')
                        {
                            goto LoopIterationNoMatch2;
                        }
                        
                        pos++;
                        slice = inputSpan.Slice(pos);
                        
                        // The loop has an upper bound of 1. Continue iterating greedily if it hasn't yet been reached.
                        if (loop_iteration2 == 0)
                        {
                            goto LoopBody2;
                        }
                        goto LoopEnd2;
                        
                        // The loop iteration failed. Put state back to the way it was before the iteration.
                        LoopIterationNoMatch2:
                        if (--loop_iteration2 < 0)
                        {
                            // Unable to match the remainder of the expression after exhausting the loop.
                            goto CaptureBacktrack3;
                        }
                        pos = base.runstack![--stackpos];
                        UncaptureUntil(base.runstack![--stackpos]);
                        slice = inputSpan.Slice(pos);
                        LoopEnd2:;
                    //}
                    
                    // Match if at the end of the string or if before an ending newline.
                    if (pos < inputSpan.Length - 1 || ((uint)pos < (uint)inputSpan.Length && inputSpan[pos] != '\n'))
                    {
                        goto LoopIterationNoMatch2;
                    }
                    
                    // The input matched.
                    base.runtextpos = pos;
                    base.Capture(0, matchStart, pos);
                    return true;
                    
                    // <summary>Undo captures until it reaches the specified capture position.</summary>
                    [MethodImpl(MethodImplOptions.AggressiveInlining)]
                    void UncaptureUntil(int capturePosition)
                    {
                        while (base.Crawlpos() > capturePosition)
                        {
                            base.Uncapture();
                        }
                    }
                }
            }
        }

    }
    
    /// <summary>Helper methods used by generated <see cref="Regex"/>-derived implementations.</summary>
    [GeneratedCodeAttribute("System.Text.RegularExpressions.Generator", "8.0.14.36720")]
    file static class Utilities
    {
        /// <summary>Pops 2 values from the backtracking stack.</summary>
        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        internal static void StackPop(int[] stack, ref int pos, out int arg0, out int arg1)
        {
            arg0 = stack[--pos];
            arg1 = stack[--pos];
        }
        
        /// <summary>Pushes 1 value onto the backtracking stack.</summary>
        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        internal static void StackPush(ref int[] stack, ref int pos, int arg0)
        {
            // If there's space available for the value, store it.
            int[] s = stack;
            int p = pos;
            if ((uint)p < (uint)s.Length)
            {
                s[p] = arg0;
                pos++;
                return;
            }
        
            // Otherwise, resize the stack to make room and try again.
            WithResize(ref stack, ref pos, arg0);
        
            // <summary>Resize the backtracking stack array and push 1 value onto the stack.</summary>
            [MethodImpl(MethodImplOptions.NoInlining)]
            static void WithResize(ref int[] stack, ref int pos, int arg0)
            {
                Array.Resize(ref stack, (pos + 0) * 2);
                StackPush(ref stack, ref pos, arg0);
            }
        }
        
        /// <summary>Pushes 2 values onto the backtracking stack.</summary>
        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        internal static void StackPush(ref int[] stack, ref int pos, int arg0, int arg1)
        {
            // If there's space available for all 2 values, store them.
            int[] s = stack;
            int p = pos;
            if ((uint)(p + 1) < (uint)s.Length)
            {
                s[p] = arg0;
                s[p + 1] = arg1;
                pos += 2;
                return;
            }
        
            // Otherwise, resize the stack to make room and try again.
            WithResize(ref stack, ref pos, arg0, arg1);
        
            // <summary>Resize the backtracking stack array and push 2 values onto the stack.</summary>
            [MethodImpl(MethodImplOptions.NoInlining)]
            static void WithResize(ref int[] stack, ref int pos, int arg0, int arg1)
            {
                Array.Resize(ref stack, (pos + 1) * 2);
                StackPush(ref stack, ref pos, arg0, arg1);
            }
        }
        
        /// <summary>Supports searching for characters in or not in "-0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZ_abcdefghijklmnopqrstuvwxyz".</summary>
        internal static readonly SearchValues<char> s_ascii_20FF03FEFFFF87FEFFFF07 = SearchValues.Create("-0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZ_abcdefghijklmnopqrstuvwxyz");
        
        /// <summary>Supports searching for characters in or not in "-.0123456789:ABCDEFGHIJKLMNOPQRSTUVWXYZ_abcdefghijklmnopqrstuvwxyz".</summary>
        internal static readonly SearchValues<char> s_ascii_60FF07FEFFFF87FEFFFF07 = SearchValues.Create("-.0123456789:ABCDEFGHIJKLMNOPQRSTUVWXYZ_abcdefghijklmnopqrstuvwxyz");
        
        /// <summary>Supports searching for characters in or not in "ABCDEFGHIJKLMNOPQRSTUVWXYZ_abcdefghijklmnopqrstuvwxyz".</summary>
        internal static readonly SearchValues<char> s_ascii_FEFFFF87FEFFFF07 = SearchValues.Create("ABCDEFGHIJKLMNOPQRSTUVWXYZ_abcdefghijklmnopqrstuvwxyz");
    }
}
