@page "/auth"

@using Microsoft.AspNetCore.Authorization
@using Esi.AI.Studio.Client.State

@attribute [Authorize]

<h1>You are authenticated</h1>

<AuthorizeView>
    Hello @context.User.Identity?.Name!
</AuthorizeView>

@code {
    private readonly AuthPageState pageState = new();
}
